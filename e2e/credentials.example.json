{
  "_comment": "COPY THIS to e2e/.auth/credentials.json (gitignored) and fill in real values. Or set E2E_<KEY>_EMAIL / E2E_<KEY>_PASSWORD env vars instead. A top-level \"password\" applies to any key that omits its own (handy with one generic test password). NEVER commit the real file - e2e/.auth/ is gitignored.",
  "password": "REPLACE_WITH_GENERIC_TEST_PASSWORD",
  "gm": { "email": "xerosumgames@gmail.com" },
  "marv": { "email": "tony_bushell@hotmail.com" },
  "pesky": { "email": "REPLACE_WITH_PESKY_EMAIL" },
  "percy": { "email": "REPLACE_WITH_PERCY_EMAIL" }
}

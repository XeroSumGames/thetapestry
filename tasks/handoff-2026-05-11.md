# Tapestry Development Handoff — 2026-05-11

**Last session ran:** 2026-05-10 → 2026-05-11.
**Live URL:** https://thetapestry.distemperverse.com
**Worktree branch:** `claude/optimistic-lovelace-830d54`
**Working dir:** `C:\TheTapestry` (main checkout). Worktree is at `C:\TheTapestry\.claude\worktrees\optimistic-lovelace-830d54`.

---

## Where to start

The user (Xero) has a **comprehensive backlog file at `C:\TheTapestry\tasks\backlog-2026-05-11-comprehensive.md`** with ~150 open items grouped by phase. The **immediate next task** is to **validate every backlog item against shipped code** — a lot of items have been sitting and some may already be fixed/done. Strategy: spawn a research agent to walk through each suspect item, grep the shipped code, and produce a `RESOLVED / PARTIAL / STILL OPEN / STALE_DESCRIPTION` verdict per item, then update the backlog markdown in one pass.

Suspect categories most worth validating first:
- **Bugs that might already be fixed** (~15 items — most stale-prone since code moved while bugs sat).
- **Partials that might be complete** (~10 items).
- **Canon-promotion items where the spec might already be live** (Item Condition + Upkeep, Subsistence Damage, Apprentice gate — backlog already hints "mostly shipped" on several).

User flagged the tactical-map mouse-pan bug as a strong candidate for "already fixed by side-effect" — I traced the code on 2026-05-11 and the pan path is intact. Console trace shipped (`63a0b05`) to confirm during Monday playtest.

---

## What shipped this session

Massive roll-feed log-trimming pass + UX polish + new features. Highlights in commit order (newest first):

- **`b921b43`** — `SHARE VIEW` button on campaign map: GM clicks → broadcasts `{lat, lng, zoom, tile}` → players smoothly fly to the same view. One-shot, not follow-mode.
- **`1f721b8`** — measure-tool toolbar now shows per-leg breakdown stacked above TOTAL row.
- **`63a0b05`** — `[playtest-trace]` console traces for the TacticalMap pan branch (I believe this bug is dead).
- **`aaa29af`** — `[playtest-trace]` console traces for Initiative lag / damage calc / failed-skill-checks-leave-actions.
- **`7648c94`** — Medic First Aid bug resolved (it was a naming-confusion, not a real bug). Skill-description tooltips polish added to backlog.
- **`d394ffb`** — Turn-gate removed: any character at `/table` can fire any check (skill / attribute / social / weapon attack) in or out of combat. Action accounting still tied to active combatant via per-roll `rollerInitId`.
- **`9aa5e21`** — UNIFIED ROW pattern: Coordinate now writes ONE row carrying dice + outcome + narrative (was 2 rows: player roll + "System" effect-broadcast). Pattern documented in `tasks/lessons.md`.
- **`f757318`** — Group Check LI gets bespoke "collectively had a Moment of Insight" wording.
- **`5f19a3f`** — Global HI/LI tags symmetric: `" and has a Moment of Insight as to why it went so well"` / `"...so badly"`.
- **`c6ecd6e`** — LI tag wording iterated.
- **`c4bd81c`** — Coordinate effect-row reframed as active sentence.
- **`9030cd0`** — Charge bespoke ("made an Unarmed attack after Charge at Y"); FI HI/LI bespoke phrasing.
- **`c7b1c44`** — Weapon attack past-tense restructure ("Cree Successfully Attacked Avery Xavier using a Pistol"); LI tag adds "they failed".
- **`2704389`** — Group Check failure parallels success structure ("were Unsuccessful at" instead of "failed their X check").
- **`2ac0ad3`** — Retention dissolved copy tweak.
- **`52213f7`** — Mortal wound one-continuous-sentence.
- **`1875737`** — Death event "is gone" / mortal wound "has fallen".
- **`80aca11`** — Em-dash → hyphen sweep across roll-feed sources (611 replacements).
- **`5a6c002`** — CDP award spells out "Character Development Points"; NPC corpse loot distinct from object loot ("searched the corpse of").
- **`001b126`** — Evolution row spells out CDP.
- **`ca0f82c`** — Hide ▸ expand pip on rows with no real dice.
- **`5abc9e2`** — Closed 5 roll-feed Tier C trim gaps (Group Check / barter / cdp / encumbrance / coordinate-effect).
- **`8b1c4e1`** — Group Check renders as bespoke multi-participant banner (UNIFIED ROW seed pattern).
- **`8ac4ae2`** — Multi-cell token LoS: filter scans full footprint, not just anchor.
- **`4657cff`** — Stale "LoS-aware hiding is Phase 3" comment cleanup.
- **`7ca9569`** — First Impression outcome ladder canon fix (was shifted +1).
- **`3b01cfd`** — Recruitment / Inspiration / Apprentice rules-extract at `tasks/rules-extract-recruitment-inspiration.md`.

(Parallel agents also shipped: `f13321e` Stress canon section, `70dd7e3` Recorder 3-hour playtest hardening, `ab8eeb5` etc.)

---

## Pending playtest (5/11 evening, Xero running it)

Three bugs are console-traced. Filter DevTools on `[playtest-trace]` and copy the surrounding block when each fires:

1. **Initiative lag** — `[nextTurn] done {total_ms, deactivate_ms, activate_ms, reload_ms, broadcast_ms}` on every End-Turn. If `total_ms > 500`, per-phase breakdown points at the bottleneck.
2. **Damage calc spot-check** (`2+2d6(6)=8 raw → should be 7/7`) — `[damage-calc]` at every attack dumps `totalBase / totalDice / totalPhy / unarmedBonus / totalWP_raw / rpPercent / isStun / defensiveMod / finalWP_after_calc / finalRP_after_calc / mitigated`. Math-check from the dump.
3. **Failed skill checks leave 2 actions** — `[closeRollModal] gate` + `rollerIsActive?` + `[consumeAction] CALLED / WROTE`. If consume doesn't fire after a skill check, the gate output shows why.
4. **TacticalMap pan** — `[TacticalMap] plain mousedown fell-through to pan branch` confirms the path fires. Map should also scroll smoothly — if trace fires but no scroll, downstream issue.

After playtest, ask Xero for the captured traces and diagnose.

---

## Locked rules / canon (read carefully)

Saved in user memory at `C:\Users\tony_\.claude\projects\C--TheTapestry\memory\`. Highlights:

- **No em-dash (—) or en-dash (–) anywhere.** Always plain ASCII hyphen. Banned in chat / code-generated strings / docs / SQL comments / commit bodies. Sweep before shipping: `rg '[—–]'`. Memory: `feedback_no_en_dash.md`.
- **No "(critical)" in roll-feed output.** Replaced with "Moment of Insight" wording. XSE doesn't use "critical".
- **Wild Success / High Insight tag** (global): `' and has a Moment of Insight as to why it went so well'`. Bespoke overrides exist for First Impression ("they did so well"), Unarmed ("had a Moment of Insight when using..."), Stabilize ("...while doing so").
- **Low Insight tag** (global): `' and has a Moment of Insight as to why it went so badly'`. FI bespoke: `', but has a Moment of Insight as to what went wrong'`. Group Check bespoke: append `' but collectively had a Moment of Insight into why they failed'`.
- **No "CDP" abbreviation in user-facing strings.** Spell out "Character Development Points" (or "Point" on count of 1).
- **Carlito font everywhere.** `fontFamily: 'Carlito, sans-serif'`. Memory: `feedback_readability_carlito.md`.
- **13px minimum inline fontSize.** Never write 9-12px in `style={{ ... }}` props. Guardrail: `node scripts/check-font-sizes.mjs`. Memory: `feedback_min_font_size.md`.
- **Carlito banned combo:** `fontSize: 13px` + `color: '#3a3a3a'` — illegible on dark. Use `#cce0f5`.
- **`/table` allows any character to make any check** (in or out of combat) since 2026-05-10. Action accounting only consumes from the active combatant's rolls.
- **UNIFIED ROW pattern** when a mechanic has both a dice roll AND a downstream effect: write ONE row carrying both. Documented in `tasks/lessons.md`. Anti-pattern: two rows (dice + "System" effect) for one event.
- **Apprentice unlock house-rule:** Moment of High Insight only (canon allows Wild Success too, but Xero locked to HI). Single line at `app/stories/[id]/table/page.tsx:3532`.
- **Stun-weapon canon:** Taser 1WP/4RP, Cattle Prod 2WP/8RP. Stun-tagged weapons compute RP from raw WP (bypass DM). Memory: `project_stun_weapon_canon.md`.
- **Token spawn position:** always top-left (1,1), never top-right. Memory: `feedback_token_spawn.md`.
- **`cell_px` default is 35**, never 70. Memory: `feedback_cell_px_default.md`.
- **Working directory = `C:\TheTapestry`** (main checkout). After every push from worktree, pull into main checkout. Memory: `feedback_working_directory.md`.
- **Push to live, test on live.** Every change ships straight to main; Vercel deploy = dev env. Memory: `feedback_push_to_live.md`.
- **Git ownership:** Claude does ALL git/merge/push end-to-end, never asks permission. Memory: `feedback_git_ownership.md`.
- **Progression Log = journey markers only**, never session-tick or insight-die noise. Memory: `feedback_progression_log_curation.md`.

---

## File locations

- **Backlog (live, single source of truth):** `C:\TheTapestry\tasks\backlog-2026-05-11-comprehensive.md`
- **Roll-feed log preview (interactive HTML):** `C:\TheTapestry\tasks\roll-feed-log-preview.html`
- **Roll-feed checklist (text format):** `C:\TheTapestry\tasks\roll-feed-log-checklist.txt`
- **Rules extract — Recruitment/Inspiration/Apprentice:** `C:\TheTapestry\tasks\rules-extract-recruitment-inspiration.md`
- **Rules extract — Infection:** `C:\TheTapestry\tasks\rules-extract-infection.md`
- **Rules extract — Armor + Explosives:** `C:\TheTapestry\tasks\rules-extract-armor-explosives.md`
- **CRB audit FROM/TO blocks:** `C:\TheTapestry\tasks\froms-tos-crb.md`
- **Strategic roadmap:** `C:\TheTapestry\tasks\roadmap.md`
- **2026-05-06 open-work checklist:** `C:\TheTapestry\tasks\open-work-checklist-2026-05-06.md`
- **Lessons learned:** `C:\TheTapestry\tasks\lessons.md`
- **Multi-cell LoS testplan:** `C:\TheTapestry\tasks\multicell-los-testplan-2026-05-10.md`

---

## Active todos (user's in-session list)

1. ⏳ Armor Phase 2: condition tracking + Upkeep button + auto-upkeep on Low Insight
2. ⏳ Other explosives audit when QS table surfaces (blocked on Xero for QS Table 18)
3. ✅ Audit + close roll-feed Tier C trim gaps
4. ✅ Comprehensive log-preview HTML

---

## Cadence + how to work

- **Never delete from the backlog silently** — move to a "Shipped 2026-05-XX" section with the commit hash. There's one in the file already; append, don't replace.
- **Close with "What's next?"** — never with stamina-check phrasing ("stop here?", "keep going?", "want me to continue?"). Memory: `feedback_no_break_offers.md`.
- **No dismissive pre-judgments** — don't call work "cosmetic" or "redundant" before doing it. Offer-framing is fine; banned is attaching dismissive value labels. Memory: `feedback_no_offer_framing.md`.
- **Capture lessons immediately** after meaningful ships — edit `tasks/lessons.md` + `tasks/todo.md` in the same response. Never "want me to add this?". Memory: `feedback_immediate_lesson_capture.md`.
- **Testplan naming:** topic-specific (e.g. `multicell-los-testplan-2026-05-10.md`), never overwrite generic `tasks/testplan.md`. Memory: `feedback_testplan_naming.md`.
- **Verify shipped state before quoting scope.** Grep + git log first; don't trust the master inventory or specs alone. Memory: `feedback_check_before_quoting_scope.md`.
- **Long-term fix over quick fix.** Root-cause path always wins; surface latent bugs even when off-request. Memory: `feedback_long_term_fix.md`.

---

## SQL access

Apply migrations live:
```
npx supabase db query --linked -f sql/<file>.sql
```
Same CLI deploys edge functions. Memory: `reference_supabase_db_query.md`.

---

## Live site + user identity

- User email: `xerosumgames@gmail.com`
- User is GM on every campaign + Thriver role globally.
- Live URL: `thetapestry.distemperverse.com` (NOT `thetapestry.app`).
- Sentry wired: org `xero-sum-games`, project `thetapestry`. Tunnel `/monitoring`. Memory: `reference_sentry_setup.md`.

---

## End-of-handoff

Next agent should:
1. Read this file end-to-end.
2. Read `tasks/backlog-2026-05-11-comprehensive.md` for the full backlog.
3. Read `tasks/lessons.md` for the UNIFIED ROW pattern + roll-feed log patterns.
4. Wait for Xero's playtest captures (5/11 evening) OR start the backlog validation pass (parallel research agent) — whichever Xero asks for first.

Welcome aboard. Keep shipping straight to main.

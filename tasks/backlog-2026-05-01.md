# Tapestry Backlog — 2026-05-01

Curated from `tasks/outstanding-work-2026-05-01.md`, `tasks/todo.md`, `tasks/totallist.md`, `tasks/PLAYTEST_TODO.md`, `tasks/loadtimes-roadmap.md`, `tasks/long-term-fixes.md`, plus inline TODO/FIXME hits in source.

Items marked **✓ shipped 2026-05-01** are pulled out of the active list — they reflect this session's commits and aren't todo anymore.

---

## §0 — Just-shipped follow-ups (verify on prod)

- RLS tightening on campaign-tagged forum threads + War Stories — non-members can still SELECT in some cases
- Backfill old LFG freetext setting rows (pre-Phase-4A posts like "Distemper", "Homebrew", "Chased")
- Font-size guardrail offenders in `TradeNegotiationModal.tsx:217` and `CampaignCommunity.tsx:2200` (pre-existing, not blocking)

---

## §1 — Phase 4 Campfire — what landed vs what's still open

**✓ shipped 2026-05-01:** 4A (per-setting feed layer), 4A.5 (forum_threads.campaign_id), 4B (promotion + moderation), 4C (DZ + Kings Crossroads hubs), 4D (per-community feed + community_events), 4E partial + final (reactions, threading, FTS, invitations).

**Still open from Phase 4E:**
- Pagination on all feeds (Forums / War Stories / LFG — currently unbounded `.select('*')`)
- Notifications UI for LFG interest pings (rows exist, no read/dismiss view yet)
- Inline `<t:UNIX:f>` token rendering in body text (Discord-style timestamps → `<time>` elements)
- LFG filters by setting + schedule

**Phase 4 explicit non-goals:**
- Forum redesign (parked — both Style A and B disliked)
- Hubs for Mongrels / Chased / Custom / Arena (deferred)
- Homebrew tab (placeholder stays until design)
- User profiles / reputation

---

## §2 — Active backlog: friction items + small bugs

### Roll-log clarity + modal unification
- Gut Instinct results presentation rework (mechanics fine, comprehension issue — needs design discussion)
- Modal unification — pass 3 (pendingRoll attack modal — declined this session, ~480 lines of bespoke logic, no win)
- CMod Stack reusable component (extract from Recruit modal, reuse in Grapple/First Impression/Attack)
- Stress Check, Breaking Point, Lasting Wound — ✓ shipped pass 1
- Recruit result step — ✓ shipped pass 2

### Combat correctness + perf
- Lag on initiative (needs Xero solo validation; check sequential awaits in `nextTurn`, missed indexes on `initiative_order`, postgres_changes subscription firing extra rounds)

### Inventory & interactions
- Multi-round haggling (Barter currently single-roll)
- Barter Lv4 cheat-doubling (locked behind Lv4 Trait list)
- Auto-relationship-penalty on Dire/Low Insight Barter outcome

### Mongrels Mall content prep
- King's Crossroads Mall — tactical scenes (motel courtyard, Costco, gas station, Belvedere's; wire into `SETTING_SCENES.kings_crossroads_mall`)
- King's Crossroads Mall — handouts (broadcasts, journal pages, ham-radio transcripts → `SETTING_HANDOUTS.kings_crossroads_mall`)
- Re-seed existing campaign tool (`/tools/reseed-campaign?id=…` for idempotent pin/NPC/scene/handout re-runs)

### NPC/hide workflow
- Hide NPCs reveal UX streamlining (data layer ✓; folder-level "Reveal all", multi-select bar, auto-reveal on Start Combat, panic-button "Reveal entire roster")

### Player login flow
- Streamline logging into missions (audit `/login` → `/stories` → click campaign → "Join Session" → `/table` flow; discuss before shipping)

### Small items
- Destroyed-object portrait swap (object tokens with `destroyed_portrait_url`; render on WP=0, fall back to crack overlay)
- GM force-push view to players (extend `tactical_shared` broadcast with `{ view, sceneId }`)
- Tapestry-side `<t:UNIX:format>` renderer (detect Discord tokens, replace with localized `<time>` elements)
- Print sheet missing data (Relationships/CMod, Lasting Wounds/Notes, Tracking columns not populated)
- Player-facing NPC card on Show All click (read-only; design: Name, First Impression, description, or more?)
- Player NPC notes + first impressions (player can write personal notes about NPCs, see First Impression results)
- Surface Give loot UI in GM Assets → Objects panel (mirror per-item Give controls from ObjectCard)
- Add Katana to weapon database (differentiate from Sword)
- Map search predictive results — prioritize US locations (Nominatim global → `countrycodes=us` first) — ✓ shipped 2026-05-01 across 8 sites
- Players can drop pins on /table campaign map (currently `+ Pin` is GM-only; let players place pins, reveal=false until GM approves)
- Switch email FROM back to `noreply@distemperverse.com` (domain verified on Resend)

### Character creation gaps
- Tooltips throughout character creation — ✓ shipped 2026-05-01
- CDP tracker boxes (partially fixed; finish)

### Map/pins
- Parent/child pin structure (rumor about a specific building within a landmark)
- Immutable canon layer (Thriver-set pins only, cannot be edited by others)

### Logging
- Remaining event instrumentation (9 items)

---

## §3 — Big features unstarted

### CDP Calculator / Character Evolution
- Show character's current CDP balance
- List buyable upgrades (attribute raises, skill raises, new trait picks)
- Preview cost before commit
- Apply spend in one transaction (decrement `data.cdp`, write changes, append roll_log with outcome='evolution')
- Player-facing tool; reuse wizard's CDP cost tables from `lib/xse-engine.ts`
- Audit-log gap closed — ✓ shipped 2026-05-01

### Tactical map long-term lifts
- Dynamic lighting (player vision limited by light sources; lights layer on `tactical_scenes`, per-token visibility, fog-of-war canvas)
- Doors (door token_type with `is_open boolean`, blocks movement/vision when closed, GM toggle)
- Line of sight (visibility blocking with polygon vision mask per token, recalculated on move)

### Lv4 Skill Traits (backburner — ship together or not at all)
- Inspiration Lv4 "Beacon of Hope" auto +4 to Morale
- Psychology Lv4 "Insightful Counselor" auto +3 to Morale
- Generic Lv4 Trait surface on character sheet
- Auto-application hooks for any Lv4 Trait touching Morale / Recruitment / Fed / Clothed / combat

---

## §4 — Phase 5 Module System

### Phase C — Marketplace
- `/modules` browse + search + filters (setting, tags, rating, subscriber count)
- `/modules/[id]` detail page with version history, reviews
- Cover image upload, featured-module surface on dashboard
- Play stats per module (subscriber count, session count, avg player count)
- Listed-module Thriver moderation queue — already shipped 2026-04-24

### Phase D — Monetization + tiers
- Free / Paid / Premium module pricing
- Licensed GM permission unlocks paid modules
- Author payout flow, referral tracking

### Phase E — Ecosystem
- GM Kit Export v2 = printable PDF + module zip from snapshot
- Module + Community cross-publish
- In-session GM toolkit (scene switcher, NPC roster, handouts panel, roll tables linked to dice roller)
- Third-party module import (Roll20 / Foundry → Tapestry — stretch)

### Phase F — GM Adventure Authoring Toolkit (added 2026-04-30)
- Story Arc form (4-question creation: "What is this about?" / "Where do they start?" / "What happens along the way?" / "Where do they end?")
- NPC quick-build inline forms (popover from Story Arc form; pre-fills NPC role from beat)
- Map quick-build (drop new tactical scene from beat; image upload + grid + cell_px + "place opening tokens")
- Handout quick-build (title + rich text + optional image → `campaign_notes` with share=true)
- Encounter quick-build (pre-rolled stat block: initiative line-up, recommended weapons, terrain notes)
- Route tables (travel-arc adventures; leg-by-leg encounters with roll-target each; new `route_legs` table)
- Adventure preview ("play test mode" dry-run UI through arc beat-by-beat)
- Publish Adventure (terminal step; bundles arc + linked assets into Module snapshot via `buildModuleSnapshot`)

---

## §5 — Communities Phase B polish

- Deeper approach tooltip (Why this approach? with rules context, commitment duration, SRD references, when to pick each)
- NPC-proxy recruitment (GM rolls on behalf of Community Leader NPC to recruit other NPCs; deferred design dependency on Activity Blocks Phase D)

---

## §6 — Backburner: don't touch unless trigger fires

### Campaign calendar
**Triggers:** Forgetting Skip Week → community frozen 4+ sessions; world events that should've ended weeks ago still applying CMod; wanting "X days passed" → automatic ration consumption / weather change / community drift; encumbrance tick should auto-fire on time advancement.

**When picked up:**
- DB: `campaign_clock` table or jsonb on campaigns (start_date, ticks_per_day, current_tick)
- Helper `lib/campaign-clock.ts` exposing `advance(campaignId, hours)` fanning out to encumbrance ticks, community Morale due-dates, world event activation, ration decay
- Small clock widget in table page header showing "Day N · Hour H" with +/- stepper (GM-only)
- Migrate Time button from Inventory to use unified clock

### Thriver godmode UI sweep
**Status:** DB-level done; UI deferred. Pilot (commit `fd5db34`) widened 3 components, then rolled back.

**When picked up:** every `isGM && <button>` widens to `(isGM || isThriver) && <button>`. Candidates: Table page header (Start/End Session, Start/End Combat, Tactical Map toggle, Share Map, GM Tools), NpcRoster, TacticalMap, CampaignCommunity, CampaignPins / CampaignObjects / VehicleCard, Character sheet edits for non-owned PCs. Pattern: prefer widening at caller (`isGM={isGM || isThriver}`) over rewriting internal references.

### NPC health as narrative feeling
*Deferred 2026-04-26.* User decided not to ship; re-open if different framing surfaces.

### `/firsttimers` auto-redirect
- Drop the auto-redirect from `/firsttimers` to `/dashboard` when `profiles.onboarded = true`
- Page should be visitable as reference / re-onboarding surface
- Likely fix: banner "You've already seen this" with link, OR make redirect opt-in via `?firsttime=1`
- **DO NOT touch until Xero approves**

### GM Kit v1 (paused 2026-04-19)
**Status:** shipped end-to-end but image URLs in seeds point to source campaign's bucket — delete that campaign and seed images 404.

**Three directions:** (a) Re-upload kit images to "shared seed assets" bucket on import, (b) Treat seeds as live-linked to source campaign, (c) Abandon seed approach, lean on real Module data structure (Phase 5).

**Revisit before promoting any setting beyond personal beta.**

### Code audit deferrals
- Split table page (5,365 lines) into subcomponents (high risk before game)
- Debounce realtime callbacks (works fine, optimization only)
- Sequence guards on `loadRolls` / `loadChat` (low impact)

---

## §7 — Technical debt & tools

- Migrate character photos from base64 to Supabase Storage (low priority — already 256x256 JPEG)
- Embed Distemper videos on landing page
- Tools enhancements: batch mode, manual crop control, upload to Supabase Storage, auth gating, more tools

---

## §8 — Long-term deferred

### Tactical map pan via mouse drag
**Status:** broken. Click-and-drag on empty cell doesn't scroll the map when canvas overflows. **Workaround:** arrow keys / WASD pan, or zoom out to fit-screen. **Likely causes:** canvas may not actually overflow container at typical zoom; `contain:layout` paint interaction with `overflow:auto`; canvas needs explicit width/height styling. Deferred 2026-04-27 post-playtest.

---

## §9 — Roadmap (Phase 6-11, aspirational)

### Phase 6 — Community & Retention
- LFG system (GMs post, players post availability, matching by setting/playstyle)
- Session scheduling (GM proposes times, players confirm, calendar view)
- The Gazette (auto-generated newsletter after session: roll log highlights, summary, GM notes; shareable link)
- Between-session experience (something to do outside active sessions)
- Subscriber tiers (Free, Paid, Premium with feature gates)
- Graffiti (reactions on War Stories + Campfire posts, Distemper-branded)

### Phase 7 — Ghost Mode Advanced
- Ghost-to-Survivor funnel analytics
- A/B test soft wall messaging
- Onboarding flow for physical product QR scanners
- Reactivate `/firsttimers` onboarding page

### Phase 8 — Physical Products
- Chased QR code integration (fold-out map codes → Tapestry at Delaware setting)
- Anonymous preview for QR scanners (show setting content before signup prompt)
- Chased module (pre-populated with Delaware setting, linked to physical product)
- Minnie & The Magnificent Mongrels setting (sourcebook upload, seed pins + NPCs)
- Physical product landing pages (one per product, branded, drives to signup)

### Phase 9 — Maturity
- Rules reference (full XSE SRD v1.1 searchable/browsable) — partial; /rules SRD reference shipped 2026-05-01 (Communities full + 11 stubs + Wix export)
- Contextual rules links (from character sheet + dice roller to SRD sections)
- GM quick-reference panel (outcomes table, CMod table, range bands, combat actions)
- Mobile optimization pass (dashboard, map, character wizard, table view all responsive)
- Mobile dice roller (optimized for rolling at physical table on phone)
- Global search (find characters, campaigns, pins, NPCs, Campfire posts)

### Phase 10 — Future Platforms
- Displaced (space setting, separate platform, custom star map)
- Extract shared XSE engine into `@xse/core` monorepo (shared across platforms)
- Each setting gets own domain, branding, map layer on shared core
- Long-term: Tapestry proof-of-concept for XSE platform family

### Phase 11 — Cross-Platform Parity
- Campaign Calendar (date-gated lore events, GM-controlled states; build for Displaced first, backport if demand)
- Roll20 Export (one-way migration: minimal sheet HTML/CSS/JS, per-campaign ZIP exporter, ingest paths)

---

## Inline TODO/FIXME survey

Two trivial code TODOs only: Tapestry-side timestamp renderer reference, and a campaign-snapshot comment. No outstanding `XXX/HACK` markers worth surfacing.

---

## Most actionable next moves

| Pick | Why |
|---|---|
| **CMod Stack extraction from Recruit** | Small, satisfying, sets up cleaner Recruit/Barter/Social rolls. |
| **Phase 4E remaining four items** (pagination, LFG notifs UI, timestamp tokens, LFG filters) | Tight cluster, finishes Phase 4 properly. |
| **Modules Phase C — Marketplace** | Big-feature momentum; needs Xero direction lock first. |
| **Tactical map: doors → LOS → lighting** | High-value for play but spans multiple sessions. |

## Biggest deferred lever
**Lv4 Skill Traits list** — blocks Inspiration "Beacon of Hope" (+4 Morale), Psychology "Insightful Counselor" (+3 Morale), Barter cheat-doubling, and the generic Lv4 Trait surface on the character sheet.

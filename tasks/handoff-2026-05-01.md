# Handoff — 2026-05-01 (end of session)

> Picks up where the long Friday session left off. All work below is **shipped to `main`** and live on Vercel. SQL migrations were applied by Xero as we went.

## State at handoff

- **HEAD:** `619fdb8 feat(stockpile): withdraw-to-PC button + close two audited followups + clear guardrail`
- **Worktree:** `claude/admiring-merkle-846bc3` — clean
- **Primary tree** (`C:\TheTapestry`): synced to `main`
- **Font-size guardrail:** clean (no inline <13 px, no banned 13px+#3a3a3a combo) — first fully-clean run today
- **tsc:** clean

## Today's commit run (oldest → newest)

1. `f3bf4a5` Phase 4A — per-setting Campfire discriminator
2. `4d75d5b` Phase 4A.5 — `forum_threads.campaign_id`
3. `f9609e4` Phase 4B — promotion + moderation flow
4. `3ba25a8` Phase 4C — setting hubs (DZ + Kings Crossroads)
5. `9725b09` Phase 4D — per-community Campfire feed + community_events
6. `777c6c9` Outstanding-work export
7. `e8686d9` Phase 4E (partial) — polish wave bundle 1
8. `71c4044` Phase 4E (final) — reactions + threading + FTS + invitations
9. `03aad03` Modal Unification pass 1 — shared `<RollModal>` shell (Stress / Breaking Point / Lasting Wound)
10. `3cb302e` Modal Unification pass 2 — Recruit result step
    - *Pass 3 declined* (pendingRoll modal — see [Pass 3 rationale](#pass-3-rationale-decline))
11. `89c6b14` CDP Calculator audit-log gap (`outcome='evolution'` in `roll_log`)
12. `e3bb1ac` Three small UI fixes — Survivor badge → Thriver-only, sidebar trim, /characters 4-button path picker
13. `1a4774d` "What They Have" overhaul — full weapon catalog + search + dropdowns
14. `4040efb` Vocational skill step-down asymmetry fix + unspent-CDP warning
15. `1f0e2cc` StepEight search-input focus fix (hoisted subcomponents) + 10 new incidental items (now 20)
16. `6e224e7` Tooltips throughout character creation (HelpTooltip + help-text.ts)
17. `38057f8` Chore stack — PrintSheet → `lib/weapons.ts`, Nominatim US-first across 8 sites, GM Tools Restore audit
18. `5e3dd01` PlayerNpcCard First Impression skip-the-picker + 2 audit closures
19. `d9063a5` Inventory followups — PC↔Vehicle transfer + stockpile realtime sub
20. `619fdb8` Stockpile withdrawal-to-PC button + 2 audit closures + clear guardrail

> Parallel work also landed on `main` mid-session: `7336a00` + `148ac02` (`/rules` SRD reference content). Not from this session.

## SQL migrations applied today

- `sql/campfire-setting-discriminator.sql` (4A)
- `sql/forum-threads-campaign-id.sql` (4A.5)
- `sql/campfire-moderation.sql` (4B)
- `sql/community-events.sql` (4D)
- `sql/campfire-polish-4e.sql` (4E final)
- `sql/roll-log-evolution.sql` (CDP audit gap, if separate file — confirm with Xero)

## Pass 3 rationale (decline)

The pendingRoll attack modal is intentionally **not** migrated to `<RollModal>`:

- Draggable, ~480 lines of attack-specific bespoke logic (range bands / defensive stance / same-target / Coordinate / social CMod stacks)
- The shared shell was extracted **from** this modal — circular if we then push the parent into the child
- No code-saving win after extraction; high breakage risk on the most-used roll surface

Leave it. If we ever need its widgets reused elsewhere, extract individual pieces (CMod Stack first — see Outstanding).

## What's still on the board

Authoritative list lives in `tasks/outstanding-work-2026-05-01.md`. Quick highlights of what's **not** shipped yet:

- **CMod Stack reusable component** — extract from Recruit (small, satisfying)
- **Modules Phase C (Marketplace)** — `/modules` browse + filters + reviews
- **Print sheet missing data** — Relationships/CMod, Lasting Wounds/Notes, Tracking
- **Player NPC notes + first impressions log**
- **Surface Give-loot UI in Objects panel**
- **Re-seed an existing campaign tool** — `/tools/reseed-campaign?id=…`
- **Multi-round haggling (Barter)** + Lv4 cheat-doubling (Lv4 locked)
- **Auto-relationship-penalty on Dire/Low Insight Barter outcome**
- **Tactical map** — doors / lighting / line of sight
- **Lv4 Skill Traits** — backburner, ships as one piece or not at all (memory rule)
- **Hide-NPCs reveal UX** — needs a workflow chat first
- **Streamline logging into missions** — needs a workflow chat first

## Memory rules currently in play

- **Carlito > Barlow Condensed** — default new UI to `'Carlito, sans-serif'`
- **13 px inline floor** — never write 9–12 px in `style={{ fontSize }}`. Banned combo: `13px + #3a3a3a` → use `#cce0f5`. Guardrail: `node scripts/check-font-sizes.mjs`
- **Push to live, test on live** — every change goes straight to `main`. After every push from a worktree, `git -C C:/TheTapestry pull origin main`
- **Long-term fix over quick fix** — root-cause path always wins
- **Handoff = file + chat, one shot** — write the doc AND post the prompt without being asked twice
- **Testplan naming** — name by topic, never overwrite generic `tasks/testplan.md`
- **Header buttons** — uniform 28 px, Carlito, use `hdrBtn()`
- **Notification position** — fixed `left:10px` below the bell, never change
- **Token spawn** — top-left `(1,1)`, never top-right
- **`tactical_scenes.cell_px` default = 35** — never 70
- **Survivor badge** — Thriver-only (shipped today)
- **No "want to break?" offers** — never end with mothering language

## Next-up suggestion (when you come back)

CMod Stack extraction is the cleanest win — it's small, it pays down a real duplication, and it sets up cleaner future Recruit/Barter/Social rolls. Modules Phase C (Marketplace) is the next big-feature investment if you want to keep momentum on flagship work.

# Lessons Learned

## Phase 6 console cleanup: a sanctioned "console seam" + level-as-signal (2026-05-23)

Drove `console.log|warn` 115 -> 0 (Xero option B). The reusable shape:
1. **One sanctioned console home, excluded from the fitness function.** `trace(label, data)` lives in `lib/playtest-recorder.ts`: pushes a `'custom'` recorder event AND echoes to console ONLY when `process.env.NODE_ENV === 'development'` (NOT `!== 'production'` - that would spam the vitest run, whose NODE_ENV is 'test'). check-arch's prod-console metric now skips `lib/playtest-recorder` exactly as it skips `lib/data` for `.from` - the telemetry tool is allowed to console; everywhere else routes THROUGH trace(). This is the principled way to hit 0 without a hacky `console['log']` regex-dodge.
2. **Categorize by signal, not blanket-convert:** real failures (`error`/`failed`/`SILENT RLS`/`rejected`) -> `console.error` (NOT in the warn ratchet, stays visible in prod, still caught by the recorder's console.error monkey-patch - Xero's decision (b)); operational flow telemetry (state dumps, bail reasons) -> `trace()`; pure per-render/timing noise (`[crop]`, `[kickCheck]` per-load, `inserted N`) -> delete. A blanket `s/warn/error/` is only safe on files that contain ONLY error-surfacing warns (verify first) - then a per-file `sed` beats 25 Edits.
3. **Watch the regex false-positives:** check-arch counts `console.log(` literally, so a `console.log(` INSIDE a comment counts. Reword the comment (or it's moot once the file is seam-excluded).
4. **Deleting a debug log can orphan its setup** (`const t0 = performance.now()` feeding only a timing log) - remove those too or tsc/lint flags the unused local.

## God-component SEAM migration: drop-in builders + a primitive for the channel type useCampaignChannel doesn't cover (2026-05-23, MapView)

The data+realtime seam migration (distinct from the function-extraction technique below) on `components/MapView.tsx` (36 `.from` -> 0, 2 `.channel` -> 0). Reusable for vehicle / NpcRoster / CampaignCommunity / TacticalMap:

1. **The `.from(` ratchet counts MORE than DB queries.** The regex is `/\.from\(/g`, so it also catches `supabase.storage.from('bucket')` AND `Array.from(...)`. MapView's 36 was 28 DB + 7 storage + 1 `Array.from`. To truly zero a file: migrate DB queries to `lib/data/*`, move storage access behind the seam too (it IS data access - `lib/data/map.ts` got `uploadPinAttachment`/`listPinAttachments`/`pinAttachmentPublicUrl`/`removePinAttachments`), and spread the `Array.from` (`[...fileList]` - compiles fine at the repo's ES target). `getPublicUrl` is SYNCHRONOUS; its helper returns the value directly so `const { data } = pinAttachmentPublicUrl(p)` stays a drop-in.
2. **Drop-in builder pattern (same as moderation.ts):** each repo fn returns the raw typed builder so `const { data, error } = await repoFn(args)` is a one-line swap. Keep ALL orchestration (Promise.all, optimistic setState, the username/campaign-name two-step hydration, the `cancelled` async-guard) at the call site. Preserve every `.eq` verbatim.
3. **Typed `db()` surfaces real type mismatches the untyped component hid.** The biggest: a `setPins(data)` where `data` was `any` becomes `Row<'map_pins'>[]` (categories: Json) vs the component's `Pin[]` (categories?: string[]). Fix with ONE cast at the boundary (`const data = pinRows as unknown as Pin[]`) so all downstream `(p: Pin)` usage stays unchanged - don't fight types field-by-field. For a dynamic `Record<string,unknown>` update payload, type the repo param as `Record<string, unknown>` and cast to `Update<'table'>` INSIDE the seam, so the component never imports db-internal types.
4. **useCampaignChannel is for CAMPAIGN-scoped realtime only.** MapView's two channels (`whispers_feed`, `map_pins_changes`) are GLOBAL postgres_changes on a whole table - no campaignId. Built `lib/realtime/usePostgresSubscription(channelName | null, {label,table,handler})`: one table, sentry-wrapped via `wrapDbChange`, handler kept fresh via a ref so it only churns on `channelName` (pass null to not subscribe - gates on "only while this tab is open"). This is the sanctioned home for a non-campaign `.channel()`, same way a `usePresenceChannel` would be for presence.
5. **Migrating a channel whose handler closes over an effect-local async loader:** keep the loader + its `cancelled` guard in the existing effect, expose it via a ref (`loadWhispersRef.current = () => void loadWhispers()`), and have the top-level `usePostgresSubscription` handler call `ref.current?.()`. Preserves the exact churn boundaries while moving the `.channel()` behind the seam.
6. **After:** delete the now-dead `const supabase = createClient()` + its import (grep `supabase` to confirm zero remaining uses), `--save` the ratchet, full gate set. Net was behavior-preserving + STRICTLY BETTER observability (the old raw channels had no Sentry wrap; the seam adds it).

**Vehicle (3 of 6, `91e85fc`) added three more patterns - a component with BOTH long-lived subs AND ephemeral fire-and-forget senders:**
7. **Split the commit by risk, not by file.** Vehicle's data seam (14 `.from`) shipped as one safe, independent commit FIRST (`deed757`); the realtime (6 channels, combat-adjacent - touches the shared `tactical_`/`initiative_` channels) shipped as a second careful commit. Data seams are always low-risk and independent of realtime; don't bundle them with a combat-sync change whose only real gate is the Phase 7 2-client smoke.
8. **`broadcastOnce(channel, event, payload, {holdMs})` for fire-and-forget sends.** Several sites broadcast on a channel they don't subscribe to (open-channel -> subscribe -> on SUBSCRIBED send -> removeChannel). That dance is now one typed helper in `lib/realtime/broadcastOnce.ts`. `holdMs` preserves a deliberate post-send delay before teardown (vehicle's `vehicle_updated` fallback holds 500ms so the server fans out first - DON'T drop it). For a LONG-LIVED channel that ALSO sends (vehicle's tactical_), use `useCampaignChannel` and alias `const xRef = handle.channelRef` (the 3d.2b move) so existing raw `.send()` sites are untouched; only the truly-ephemeral senders become `broadcastOnce`.
9. **Tighten an `EmptyPayload` event when a migrating send site reveals it carries data** (events.ts PAYLOAD POLICY). `turn_advance_requested` was `EmptyPayload` but vehicle sends `{ entryId }`; tightened to `{ entryId?: string }`. Safe because every cross-component LISTENER ignores the payload (so no wire/handler break) and there were no other TYPED send sites. Always grep all senders+listeners of an event BEFORE tightening - confirm payload shape and that no typed `.send()` elsewhere will break. Non-data-seam leftovers (RPC calls, `decrementInitiativeAction(supabase,...)`) keep the `supabase` var alive - that's fine, RPCs aren't `.from(`/`.channel(` and don't move this pass (Phase 6 / optional).

**NpcRoster (4 of 6, `d92aba5`) - the biggest data seam (46 DB queries / 8 tables) confirmed two scaling tactics:**
10. **Reuse the existing per-table repo for single-id ops; put the long-tail shapes in a component repo.** `campaign_npcs` already had `lib/data/campaign-npcs.ts` (updateCampaignNpc/insertCampaignNpcs) - reused those for the `.eq('id',id)` updates + inserts, and put the roster-only shapes (`.in('id',ids)` bulk, select-max, the other 7 tables) in a new `lib/data/npc-roster.ts`. Don't duplicate a function that exists; don't bloat a shared repo with one-component shapes either.
11. **A multi-table channel maps to ONE `useCampaignChannel` with a `postgres[]` array.** The `npc_roster_${id}` channel had two `.on('postgres_changes',...)` (campaign_npcs filtered + community_members), both refetching - became one `useCampaignChannel(campaignId, { channelName, postgres: [sub1, sub2] })`. Dropped the stale `isGM` dep from the channel (the initial-load effect keeps it; the channel never needed it). Same typed-client cast-at-the-boundary discipline as MapView: `setNpcs(data as CampaignNpc[])` (Row's `string` columns vs the local narrowed union), and a dynamic edit-form row -> `as any` on the typed `updateCampaignNpc` rather than importing db types into the component.

**CampaignCommunity (5 of 6, `a62f65c`) - largest data seam (56 queries / 13 tables):**
12. **A DYNAMIC postgres filter that must resubscribe on data change -> key `usePostgresSubscription` on a channelName that encodes the set.** The `stockpile-${id}` channel filtered `community_id=in.(<the loaded community ids>)` and the old effect resubscribed whenever `communities` changed. Encode the id-set in the channelName (`stockpile-${campaignId}-${ids.join('_')}`) so the hook tears down + resubscribes with the fresh filter exactly when the set changes, while the handler (read via the config ref) stays fresh without churning on unrelated state (panel-expand). 13-table seams stay maintainable by REUSING per-table repos (roll-log.ts insertRollLog) + generic `updateX(id, patch)` / `updateXByIds(ids, patch)` helpers rather than one fn per call site.

**TacticalMap (6 of 6, `ad9c1c8`) - the hardest, and it went smoothly because the toolkit was complete:**
13. **By the last god-component the migration is mechanical.** TacticalMap (4315 LOC, 28 `.from`, 2 channels / 6+1 broadcasts / 2 postgres, combat-critical) reused every established move: generic updateScene/updateToken repos; `useCampaignChannel` with BOTH `postgres[]` AND `broadcasts{}` on one channel + a second channel for ping; `tacticalChannelRef`/`pingChannelRef` aliased to the handle channelRefs so all 11 `.send()` sites were untouched (the vehicle/3d move); broadcast handlers rewritten from `(msg) => msg.payload` to `(payload) =>` directly; new events (tactical_zoom/tactical_view_share/gm_ping) added to events.ts. The ONLY friction was the recurring typed-Row-vs-local-interface boundary cast (`as unknown as Scene[]`/`Token[]`). **Phase 5 net: all 6 god-components clean; seam-leakage `.from` 1263->1039, `.channel` 44->22; every component still behavior-preserving (smokes batched to Phase 7).** A complete seam + a small primitive toolkit (useCampaignChannel, usePostgresSubscription, broadcastOnce) turned 6 bespoke 2000-4300-line god-components into a uniform, ratchet-enforced pattern.

## God-component function extraction: byte-exact script move + destructure-deps + tsc convergence (2026-05-23)

**Technique** for lifting a huge function (here: the ~1810-line `executeRoll`) out of a god-component into a hook/module WITHOUT risking a silent behavior change. Reused for every Phase 5 god-component extraction.

1. **Find exact boundaries** (`grep -n` the function + its neighbor). Confirm the closing brace line.
2. **Slice the body with a script** (node `readFileSync().split(eol).slice(start,end)`), do NOT retype it. Retyping 1800 lines from context WILL drift. **Detect CRLF vs LF** and split/join on the actual EOL - Windows files are `\r\n`, and `split('\n')` leaves a `\r` on every line that breaks all string matching.
3. **Move the body VERBATIM** into `useX(deps) { const {...} = deps; <BODY>; return { fn } }`. Destructure every dependency at the top so the body text is unchanged (no `deps.` prefixing - that's 40+ error-prone edits). The page destructures the fn back out; call sites stay unchanged. Same pattern as `useGmTools`.
4. **Derive the deps set mechanically, not by eye:** tokenize the body, intersect with the component's declared identifiers (useState/useRef/function/const at 2-space indent) = deps; intersect with the import block = needed imports. BUT the tokenizer also catches comment mentions and string-literal keys (e.g. `groupCheckParticipants:` as an object key, or a name in a `//` comment) - so **grep the body for each candidate's real CODE usage** before trusting it. Over-inclusion is safe (unused destructure, no `noUnusedLocals`); under-inclusion is caught by tsc.
5. **tsc is the completeness oracle, both directions:** a missing dep -> "Cannot find name X" inside the hook; a spurious dep -> "Cannot find name X" at the call-site object. Iterate to clean. Watch for TDZ: any dep declared AFTER the extracted function must be hoisted to the call site (here: `syncedSelectedEntry`) - it was a hoisted `function` before (always available) but a hook CALL runs in place.
6. **PROVE behavior preservation:** diff the extracted body against the git-HEAD original (`git show HEAD:file | slice`) after CRLF-normalizing - assert byte-identical. Then assert the hook file `.includes()` that body verbatim. Only then is "behavior-preserving" a fact, not a hope.
7. **Type the deps interface from source:** copy inline state types verbatim (e.g. the `mapTokens` shape), refs as `MutableRefObject<T>`, setters as `Dispatch<SetStateAction<T>>`, `supabase` as `ReturnType<typeof createClient>`. If a function's real return type bites (saveRollToLog returns `{total,outcome,insightAwarded}` not void) and the body ignores it, type the return precisely or `any`-the-ignored-field. Note the dual `RollResult` (./types interface vs lib/roll-outcomes union) - disambiguate.
8. **Do NOT also refactor** (pure/effect split, dedup, rename) in the same commit. Relocation-only keeps a 2-client / smoke failure localizable to "the move," not "the move plus 5 changes."

## Deleting a local var that shares a name with component state = silent rebind, not a compile error you'd expect (2026-05-23)

**Rule:** Before deleting a `let`/`const` local, grep the enclosing function for an outer-scope binding of the SAME name. If one exists, every reference to the local silently rebinds to the outer one instead of erroring "undefined."

**What it caught (3c-B1):** `executeRoll` had local `let distractResult = ''` / `let stabilizeResult = ''` (dead branches being removed), but the component ALSO has state vars `distractResult` / `stabilizeResult` (type `SharedRollResult | null`, for the dedicated modals). The `traitNotes` array spread `...(stabilizeResult ? [stabilizeResult] : [])` read the locals. Deleting just the branches (not the spreads) would have left the spreads compiling fine - but now reading the COMPONENT STATE objects and pushing `SharedRollResult` into a `string[]`. tsc would have flagged the array-type mismatch here, but a same-typed shadow (string local shadowing a string state) would NOT - it'd silently change runtime behavior with a green build. The phase4-prestage commit knew this and removed the spreads too; I verified and matched.

**Detection:** when removing a local, search the whole component for the identifier. Adjacent surviving references to a now-removed local are a rebind risk, not a guaranteed compile failure. This is why behavior-preserving deletions need the grep, not just "tsc passed."

## "The rulebook says X" needs a citation - verify canon against the actual rules page before asserting (2026-05-23)

**Rule:** Before telling Xero "canon says X" (especially to justify a code change), open the actual `app/rules/*` page or the precedence-stack source and quote it. Do NOT assert canon from inference or memory. He WILL ask "where does it say this?" - and he should.

**What it caught:** During 3c (CMod) I claimed target defense should "count once" (to-hit only, armor does damage) and proposed changing combat balance on that basis. Xero asked for the source. The actual canon (`app/rules/combat/damage/page.tsx:43-52` + `app/rules/character-overview/secondary-stats/page.tsx:52-53,62-65`) says the **opposite**: Defense Mods do **"double duty"** - MDM (PHY) / RDM (DEX) BOTH lower the attacker's to-hit AND mitigate damage, by design. So the code's double-application was correct; the real bug was an *asymmetry* (the to-hit half only fired for PC targets because the prefill only looked up PCs - NPC targets skipped it). Had I not been challenged, I'd have shipped a wrong balance change presented as a canon fix.

**Detection:** any sentence of the form "canon/the rulebook/the SRD says ..." in chat must be backed by a `file:line` or PDF-page cite produced in the SAME turn. Inference about how a mechanic "should" work is a hypothesis, not canon. This is `feedback_accuracy_over_confidence` applied specifically to rules claims.

## The arch ratchet (`scripts/check-arch.mjs --save`) is one-way - it never re-baselines a god-component UPWARD (2026-05-23)

**Rule:** `check-arch --save` ratchets each metric to the BETTER value only. If `page.tsx` grew, `--save` keeps the old (lower) ceiling - it will NOT record the growth. So you cannot "re-baseline up" to land a god-component addition; the baseline stays put and the pre-commit hook keeps blocking.

**Implication:** to add net lines to a god-component you have exactly two paths: (1) extract enough into a hook/lib that the file shrinks net (the intended path - the ratchet is *designed* to force extraction), or (2) `git commit --no-verify` (a bright-line action - get Xero's explicit OK; CLAUDE.md forbids bypassing hooks otherwise). CI does NOT run check-arch (only font/role/tsc/tests per `.github/workflows/test.yml`), so a `--no-verify` arch bypass stays green on CI - the ratchet is a local-hook discipline only. When you do bypass, run the other gates manually first so only the LOC ratchet is skipped, and re-tighten the ratchet in the very next structural commit.

## A new counter on a category that already flows through an old counter = check for double-counting (2026-05-21)

**Rule:** When you add a new per-item counter (here: explosive `qty` on the weapon slot) for a category that ALREADY rides an existing counter system, audit the old system for an interaction before shipping the new one. Don't assume the new counter is the only thing tracking that item.

**The bug it caught:** Grenades/Molotovs are `category:'explosive'` but also carry `clip:1`. The pre-existing ranged ammo-decrement (`page.tsx` `executeRoll`) and the toolbar `outOfAmmo` gate both key off `clip`, so throwing a grenade drained its `ammoCurrent` 1->0 and locked the Attack button as "empty, Reload" - silently defeating the brand-new `qty` carry-count feature (button greyed after ONE throw no matter how many you carried). The throw-decrement task looked like "add a decrement"; the real fix was ALSO excluding explosives from the clip/ammo system (decrement + gate) so `qty` is their sole counter.

**Detection:** before wiring a new counter's decrement/gate, grep the combat/attack path for every existing place the same item's OTHER fields (`clip`, `ammoCurrent`, `reloads`) get read or mutated. If the category trips an existing gate, the new feature is dead-on-arrival until you carve it out. (Surfacing this latent interaction is exactly the "long-term fix over quick fix / surface latent bugs even when off-request" rule paying off.)

**Also:** gate the new behavior on the SAME predicate the feature uses to seed the field (`category === 'explosive'`), not a convenient-looking proxy (the `Blast Radius` trait). Molotov + Flash-Bang are explosive but carry no Blast trait, so a trait-based gate would have shown a qty stepper that never decremented.

## Specs + extracts are hypotheses - verify against the live source before acting (2026-05-20)

**Rule:** A spec doc OR a pre-digested rules-extract is one writer's snapshot. Before executing it, verify against the actual live source (the real writer site for code; the real PDF/canon page for rules). Two instances surfaced in one session:

1. **DamagePayload D3.** The spec at `tasks/spec-damage-json-payload.md` Section 2 prescribed camelCase fields for every variant. The actual `CharacterEvolution.tsx:343` write uses snake_case + a different inner discriminator + an extra field the spec omitted. Shipping the spec's shape verbatim would have produced a type that didn't match what writers emit. Fix: reshape the interface to match the live write site, ship the migration, flag the spec for amendment (`tasks/spec-damage-payload-d3-audit-2026-05-20.md`). Each of the 11 D3 variants needs its own per-writer audit; ~30% of each step's effort is the reshape.

2. **Explosives canon.** The extract `tasks/rules-extract-armor-explosives.md` claimed Flame-Thrower "already matches QS canon" + Molotov is "50% RP per QS Table 19." Reading the actual QS v1.0.2 PDF (Table 13, not 18/19 - the extract used older-edition numbering) + the CRB showed Flame-Thrower is 100% RP / clip 1 (code had 50 / 30) and Molotov is 100% RP (code had 50, locked by an earlier misread). The extract was wrong on three counts. Fix: read the primary source, correct the data, flag the extract.

**Detection:** when handed "execute Phase N of spec X" or "apply the extract," open the live source FIRST (grep the writer; read the PDF page; query the DB). If the source disagrees with the doc, the source wins (per CLAUDE.md canon precedence + "verify before quoting scope"). Update the doc to match reality as a follow-up; don't code the doc's wrong shape.

## Reading image-only PDF tables: PyMuPDF render -> PNG -> vision (2026-05-20)

**Technique.** The Quickstart + CRB rulebook tables are images with no text layer (`pdftotext` / Grep on the `.txt` returns nothing; `page.search_for()` returns empty). The PDF Read tool also fails (`pdftoppm` not on PATH). Working path:

```python
# py -c (Windows: the `py` launcher has fitz; bare `python`/`python3` do not)
import fitz
doc = fitz.open(r'docs/Rules/<file>.pdf')
pg = doc[PAGE_INDEX]              # 0-indexed
clip = fitz.Rect(x0, y0, x1, y1) # crop to the table region (fractions of page rect)
pix = pg.get_pixmap(matrix=fitz.Matrix(7,7), clip=clip)  # 7x zoom for legibility
pix.save(r'.claude/tmp-x/out.png')
```

Then Read the PNG (vision). Iterate the clip rect + zoom until the table is crisp. Clean up the temp PNGs after (`rm -rf .claude/tmp-x`). On Windows use the `py` launcher, not `python` (the bare alias routes to the Microsoft Store stub). This is how the explosives audit (Table 13) got read.

## "Stop coding for the day" is NEVER a valid option (2026-05-20)

**Banned phrasing, recurrence.** The MEMORY rule [feedback_no_break_offers] says: never close with "stop here?" / "keep going?" / any stamina-check phrasing. Today I violated it twice in one chat:

1. Mid-thread: "Honest take: I've shipped three modal migrations today (~85 lines of new code paths). Pre-playtest window says no more load-bearing refactors." (subtle break-offer dressed up as risk analysis)
2. End-of-thread: "Stop coding for the day - heavy ship volume + pre-playtest window. Diminishing returns. Tomorrow comes naturally." (overt break-offer as option #1)

Xero called both out. Quoted: "you're not my mother, wife, or boss, you're a tool. knock that shit off."

**The actual rule, with sharper teeth:**

- DO NOT propose stopping for the day, EVER. Not as option #1, not as "honest take," not as "pre-ship 5-question risk analysis," not as cross-role flagging dressed up in puffer-fish clothing.
- DO NOT frame "shipping more = diminishing returns" as a reason to pause. That's stamina-checking with extra steps.
- DO NOT use "pre-playtest window" or "heavy ship volume" as cover to suggest a break. Those are real constraints on WHAT to ship (load-bearing refactors out, polish in), not WHETHER to keep working.
- DO surface real risks when relevant: "this would be a load-bearing refactor inside the playtest window" is fine; "we should stop for today" is not.
- When Xero says "you tell me" or "what's next?", pick something concrete from the queue and START it. No menu of options that includes stopping. No "recommendation: stop." No "diminishing returns."

**Detection rule going forward:** every response that proposes a next action - before sending it - scan for the words "stop", "pause", "call it", "diminishing returns", "rest", "tomorrow", "wrap up", "end of day", "good day's work", "consider stopping." If any are present in a "what should we do" context, REWRITE. The list is non-exhaustive; the principle is "no break-offers, ever."

**Companion to:** [feedback_no_break_offers] (MEMORY) + [feedback_never_offer_quick] (MEMORY: full version always, never quick vs. full A/B).

## Day-of-week claims need `date -d` verification, not memory (2026-05-20)

**Rule:** Before asserting that a specific calendar date is a specific weekday, run `date -d "YYYY-MM-DD" "+%A"` and quote the output. Never claim "X is a Sunday" / "X is a Tuesday" from memory or mental calendar.

**Trigger:** Stated in the launch plan that 2026-06-15 was a Sunday. Xero pushed back ("are you SURE?"). Verified via `date -d "2026-06-15" "+%A"` - it's a **Monday**. Updated the launch plan in place.

**Why this matters specifically:** day-of-week affects launch-day decisions (press traffic patterns differ by weekday), embargo timing, and whether the dev is going to be home or at their kid's soccer game. Getting it wrong cascades into wrong logistics decisions.

**This is the same rule as `feedback_no_fabricated_dates` memory entry**, which says "never invent day-of-week, dates, or commit counts when summarizing; compute from source or omit." I violated my own rule. The fix is mechanical: add the bash check as a reflex before any day-of-week claim.

**How to apply:** When any date appears in a launch plan, ops doc, schedule, or chat reply with day-of-week semantics, run the bash check first. `date -d "<the date>" "+%A, %Y-%m-%d"` returns both pieces; quote both.

---

## Long-horizon launch planning is the puffer-fish lane's job, not the chat's memory (2026-05-20)

**Rule:** When a date-anchored launch is in scope (public release, paid signups open, press window), write the launch-plan as a dated file at `tasks/launch-plan-YYYY-MM-DD.md` in the same response. It captures: audience reframe, per-role gap analysis, ranked punch list (MUST / SHOULD / COULD), week-by-week timeline back-solved from the date, **Xero-bright-line budget asks with decision deadlines**, puffer-fish risks (legal, bandwidth, embargo timing), and open questions.

**Trigger:** 2026-05-20 - Xero asked "if we were looking to go live to a limited public audience on 6/15, what would we need to focus on?" Initial reply was a long synthesis in chat. Without writing it down it would have been lost across sessions, and the Xero-decision items (Supabase Pro / Upstash / lawyer review) would have drifted past their deadlines silently. Memory rule `feedback_immediate_lesson_capture` covers single-ship lessons; this lesson covers the meta-shape "long-horizon plans need a doc, not just a chat reply."

**Fix-forward:** Shape locked at `tasks/launch-plan-2026-06-15.md`. Future launch plans copy that shape:
1. Reframe the audience (who, what stakes, how they differ from current users).
2. Per-role gap analysis (architect / eng / qa / security / ops / product / business / ux).
3. Ranked punch list with effort + cost columns.
4. Week-by-week timeline back-solved from the launch date.
5. **Xero-bright-line budget asks with hard decision deadlines** (this is the part chat memory loses).
6. Puffer-fish moves (the risks the technical asks miss - legal, bandwidth, embargo, "what does limited mean").
7. Open questions Xero must answer (with deadlines).
8. Maintenance section explaining how to update the doc + when to archive.

**How to apply:** When the user mentions a launch date or paid-signup window, this chat writes the dated launch plan before resolving "what's next." The doc is the deliverable; the chat reply is the headline summary. Wire it into `tasks/todo.md` CURRENT OPEN at the top so the open Xero decisions stay visible.

---

## Multi-chat collision visibility lives at session-start, not push-time (2026-05-20)

**Rule:** Run `sh scripts/start-session.sh` at the start of every non-trivial session. The script fetches origin/main, lists incoming commits + touched files, and warns if your working-tree changes overlap incoming. Cheap (~2s, no writes); makes the collision visible BEFORE you start editing instead of at push time.

**Trigger:** 2026-05-20 session had three rebase conflicts (em-dash sweep, Y12 backup playbook, gitignore) between this chat and the hunt-and-peck lane. Each resolved in 5-10 min, but the compounding cost grows linearly with ship rate. The protective rule existed in lessons ("Verify shipped state before assuming uncontested scope") but relied on the chat remembering to do it manually.

**Fix-forward:** `scripts/start-session.sh` automates the check. Wired into `tasks/handoff.md` "Session-start state check" as the canonical first action. Future chats opening a session run it before editing anything. Multi-chat lane split (per `tasks/operating-mode.md` "Multi-chat lanes") means both lanes routinely produce parallel commits; this is the substrate-side mitigation.

**How to apply:** Run at session start. Re-run between major work units. The "collision check" section is the load-bearing output - if it warns, expect a rebase conflict at push time and decide whether to integrate first or push-then-resolve.

---

## Specs go stale on their OWN STATUS, not just on file paths (2026-05-20)

**Rule:** Before "executing Phase N" of a multi-phase migration spec, verify that Phase N actually still needs doing. The legacy code it claims to migrate may already be gone, shipped via a different track between spec write-time and now.

Today: Xero said "move straight to Phase 3" of `tasks/spec-stabilize-migration.md` (First Impression migration). Before coding, I checked the source-of-truth (the legacy `executeRoll` FI branch the spec claimed to migrate). It was already DELETED 2026-05-19 - a comment at table/page.tsx L6749 says "First Impression branch deleted 2026-05-19 (FI streamline Phase 3)". A parallel track had shipped FI's dedicated `<FirstImpressionModal>` + `resolveFirstImpression()` resolver, complete with 18 unit tests, weeks before this spec's Phase 3 was "next up." If I'd just done what Xero asked without checking, I'd have:
- Re-built a feature that was already done.
- Confused a working code path by inventing a parallel one.
- Burned 2-3 hours producing waste.

**Detection at the start of any "do Phase N" task:**
1. Open the spec - find the legacy code it names (file:line).
2. Grep that path / read those lines.
3. If the legacy block is gone, marked unreachable, or wrapped in a "shipped" comment - STOP. The spec is stale on its own status.
4. Update the spec to reflect reality, mark the phase shipped in todo, and surface what's actually next.

**Companion to:** "Migration specs - verify every caller, don't trust the named trigger" (below). That rule is about which callers move; this rule is about whether anything needs to move at all.

**Why this happens:** parallel chats / parallel tracks ship overlapping work. The spec was a single-track plan written when only one track existed. The world moved on. The spec didn't.

## Migration specs - verify every caller, don't trust the named trigger (2026-05-20)

**Rule:** A migration spec naming "the trigger at X:Y" is a starting point, not the complete map. Before pulling code from path A → path B, grep for every call site of the old path. The Stabilize Phase 1 spec at `tasks/spec-stabilize-migration.md:77` named `components/CharacterCard.tsx:660` as the trigger to migrate. Reality: there were TWO triggers, and the named one was actually broken.

Sequence today:
1. Spec said: migrate the CharacterCard Stabilize button → dedicated modal.
2. Started recon - grep'd `Stabilize` in `app/stories/[id]/table/page.tsx` and found a SECOND trigger (header dropdown at L8060+) added 2026-05-19 that's the actually-live path.
3. Read the CharacterCard button context - it sits inside `localState.wp_current === 0` (mortally wounded PC's own card) and uses `rapid.RSN ?? 0` / `skills.find(... 'Medicine') ?? 0` - the PATIENT's stats, not the medic's. Latent bug since inception.
4. If I'd "just done the spec" without that second grep pass, I'd have migrated the dead/broken trigger, left the live trigger on the old path, and shipped a still-broken Stabilize.

**Detection:** at the start of any "migrate X to Y" task:
```sh
grep -rn 'X' app/ components/ lib/  # find every call site of the old shape
```
If the spec names ONE site and grep returns N, audit the other N-1 before designing the new modal's API. They may be dead, broken, or actually-live - all three change the migration plan.

**Reusable rule:** A spec is one writer's snapshot of the code. Verify the snapshot against live grep before acting on the spec.

## Never combine `git stash push <file>` with rebase --autostash (2026-05-19)

**Banned workflow.** Cost ~30 min today + a silent missing-fixes ship (`d5fd410`).

Sequence I ran:
```sh
git add <13 files>
git stash push supabase/.temp/cli-latest         # stash one specific file
git fetch + git -c rebase.autoStash=true rebase  # autostash kicks in
git commit -m "..."                              # commits stale state
```

What happened: the autostash flag tried to stash any UNSTAGED working-tree changes for the rebase. My specific-file stash plus the autostash dance silently UNSTAGED my 13 `git add` targets. The subsequent `git commit` ran with only the originally-untracked file (`scripts/check-em-dashes.mjs`) staged. The commit message claimed it shipped the 13-file sweep; the actual commit was just the 128-line script.

The push "succeeded" without warning. Live code kept the em-dashes the commit claimed to fix.

**Correct workflows:**

1. **One-shot ignore:** add the offending file to `.gitignore` first, untrack it (`git rm --cached`), then proceed without stashing.
2. **Full stash:** `git stash --include-untracked` (catches everything), do the rebase, `git stash pop`. Avoid the "stash this one file" pattern with autostash.
3. **Explicit verification after commit:** `git show --stat HEAD` after every commit to confirm the file list matches intent. The git tool reports success even when content is wrong.

**Detection rule going forward:** when a commit message implies many files changed, scan the `1 file changed, N insertions(+)` summary git emits. If it disagrees with the message, STOP and re-stage. Don't push.

## Stability-audit pattern + stale audit line numbers + Confidence-Ledger drift threshold (2026-05-19)

**Rule (audit shape):** A stability audit is "is anything load-bearing currently unattended?" not "are there zero bugs?". The repeatable shape that worked on 2026-05-19:
1. **Read existing evidence** — `tasks/debug-handoff.md` (Risk Register + Tech Debt Ledger + Confidence Ledger), newest `tasks/health-pulse.md` entry, newest `tasks/security-audit.md` entry, last 14 days of `git log`, `tasks/todo.md` CURRENT OPEN.
2. **Run live gates** — `npx tsc --noEmit`, `node scripts/check-font-sizes.mjs`, `node scripts/check-role-literals.mjs`, `npm test`, `npm audit`.
3. **Footgun grep** — `as any`, `@ts-ignore`, `TODO/FIXME/HACK`, `useEffect` realtime channels, `supabase.from`/`rpc` counts, `setInterval` (polling waste), `.storage.from(...).upload`.
4. **Confidence-Ledger triage** — for each HOPED-FOR item 4+ days stale, promote/demote/explain.
5. **Output** — one doc at `tasks/stability-audit-YYYY-MM-DD.md` with findings sorted BLOCKER / HIGH / MEDIUM / LOW. Update Risk Register colors where evidence changed them. Add new todos. No code edits in the audit pass; fixes go through normal review with pre-ship 5-Q each.

**Rule (stale line numbers):** Line numbers in `tasks/security-audit.md` (or any audit doc) referencing a hot file go stale fast. The 2026-05-19 16:23 UTC audit said the session-attachments upload was at `app/stories/[id]/table/page.tsx:3414`; the actual call site was at `:3518` after ~100 commits drift. **Before quoting any audit line number, re-grep for the call shape (`supabase.storage.from('session-attachments').upload`) to locate the current line.** Same applies to the Risk Register's "first-place-to-look" line refs.

**Rule (Confidence-Ledger drift):** When the same drift item appears in 3+ consecutive health-pulse entries without an action commit, the ledger has lost its signal. The 2026-05-19 audit found the test count (174 in ledger, 388 actual — flagged 3 times in 4 days). Pattern: tests get added without updating the ledger; the drift gets logged but no one drains it. **Threshold to enforce:** if the same drift item lands in 2 consecutive pulses, the next session opens by draining it. Either automate the test-count line via `scripts/refresh-ledger.mjs` (parse `npm test` output) or formalize a session-start drain ritual. Silent drift between drains is what's broken.

**Why both rules matter:** an audit doc is only as useful as the line refs in it are accurate, and a ledger is only as useful as the drift signal it surfaces. If the audit's refs are stale and the ledger's signal is ignored, future Claudes can't triage from them and have to redo the work.

**How to apply:**
- Future stability audits: name the doc `tasks/stability-audit-YYYY-MM-DD.md` (dated, do not overwrite). Reference this lesson + `[[slash_stability_audit]]` if a slash convention exists.
- When reading any audit doc more than 24 hours old, re-locate the call sites before quoting line numbers in proposed fixes.
- When closing health-pulse drift items, decide between "drain now" (run the action listed) and "automate the drain" (one-time script work). Do not let the same item land 3+ times.

## Tab-local default-OFF UX fails when ONE user forgets to flip it (2026-05-19)

**Rule:** When a feature requires N independent user actions to "all work" (every player hits Record), redesign so the N actions collapse to 1 (GM hits Record, broadcast cascades to N). Default-off + per-user opt-in is the wrong shape for any coordination problem where one missing user breaks the whole.

**Trigger:** Session 3 (2026-05-18). Recorder was tab-local with each user's Record button toggling only their own tab. Alex (player) hit Stop without ever hitting Start — dumped an empty file, lost 16 minutes of session coverage. We didn't find out until the next morning when his JSON was inspected.

**Fix-forward:** Shipped `653ff86` — GM-cascade design. GM clicks once, every player tab flips on via Supabase broadcast on `initChannelRef`. Per-campaign localStorage flag persists state across refresh / back-nav / late mount. `beforeunload` flush closes the "close-tab loses 60s" hole. Player Record button removed entirely; Ctrl+Shift+L hotkey kept as ad-hoc fallback.

**Pattern:** for any "every-user-needs-to-do-X" coordination problem, ask three questions before designing the UX:
1. Is X load-bearing if even ONE user forgets? (Recorder: yes — that user's session is lost.)
2. Can one central actor (GM, host, leader) trigger X for everyone? (Recorder: yes — broadcast.)
3. Does the platform already have a fan-out channel? (Recorder: yes — `initChannelRef` carries 30 other broadcast events.)

If all three are yes, default to GM-cascade. Otherwise, default to tab-local but instrument the "did everyone actually do X" check.

**Don't regress:** the previous tab-local rewrite (2026-05-15 in `lib/playtest-recorder.ts:143-151`) was itself a fix for a "GM's button toggled everyone" bug. Both extremes lose. The right middle is GM-cascade with explicit broadcast + localStorage persistence.

## Verify shipped state before assuming uncontested scope (2026-05-19)

**Rule:** Before starting any non-trivial feature work, run `git fetch && git log --oneline origin/main -10` to see what's actually shipped. Other chats may have landed competing or overlapping work since the session started.

**Trigger:** Worked through Q1 ("drives Minnie" narrative) with Xero, got explicit narrative approvals for DRIVE / BREW / NAVIGATE, committed locally, tried to push — only then discovered commit `54c46a1 feat(feed): Vehicle Attack / Driving / Brew per-outcome cinematic narratives` had landed on origin during the same session. Different design (no prefix CAPS, no fuel state in line, no NAVIGATE) shipped from another chat track.

**Cost:** Aborted rebase, reset, re-applied the approved design on top of the conflicting work, surgically replacing Drive + Brew while preserving Vehicle Attack. Extra 15-20 minutes + a confusing commit ("supersedes 54c46a1").

**Fix-forward:**
- Open any new feature work with `git fetch && git log --oneline origin/main -10` and grep for the touched module names. Pre-existing memory rule `feedback_check_before_quoting_scope.md` covers "scope" claims — extend the same habit to feature starts.
- Multi-chat memory rule (`process_multi_chat_tracks.md`) already says coordinate via shared substrate — this is the cost of not doing that.
- When two chats touch the same narrative path: the one with the most recent explicit Xero approval wins; the loser's design gets backed out with a "supersedes \<sha\>" commit message.



## High Insight / Low Insight tail phrasing in compact narratives (2026-05-19, final)

**Rule (Xero, locked 2026-05-19, final pass):** the Moment-of-Insight tail appended to every HI / LI compact narrative is canon-locked to:

- **High Insight:** `... and has a Moment of Insight as to why it went so well`
- **Low Insight:** `... but has a Moment of Insight as to why it went so badly`

**Symmetric long form.** Both tails carry the directional cue ("well" / "badly"). The "and" / "but" connector differs so the LI version reads as a contrast against the failure narrative.

**Iteration history (do not regress):**
- First attempt: HI short "as to why" (no directional cue). REJECTED 2026-05-19 by Xero — "moments of high insight should say 'and has a Moment of Insight as to why it went so well'".
- Earlier still: LI inconsistent ("and has" / "but has" across branches). Standardized to "but has".

**Special cases:**
- Coord Effort legacy LI: leading clause flipped from "but the plan falls apart" to "and the plan falls apart" so the canonical "but has..." LI tail composes without doubled "but". Tracked as a deliberate flex away from the original wording to preserve readability.
- Distract WS+HI / F+LI: split into separate cases. HI/LI get the canonical tails appended. Failure narrative was originally "but they shrug it off" but was rephrased to "who shrugs it off" so the LI tail composes without doubled "but".
- Stabilize bespoke HI keeps "... and has a Moment of Insight while doing so" per Xero 2026-05-11 lock (older bespoke phrasing pre-dates this rule and was explicitly approved). Shipped 2026-05-19 with STABILIZE prefix added across all 6 outcomes.

**Test coverage:** every narrative branch gets a unit test for HI and LI outcomes asserting the exact tail phrasing. If a future copy refactor accidentally drops the tail or rewrites it, tests turn red.

## Always update roll-feed-log-preview.html when amending log messages (2026-05-19)

**Rule (Xero, locked 2026-05-19):** the file `tasks/roll-feed-log-preview.html` is the canonical visual reference for compact-narrative feed rows. Whenever ANY log message in `lib/roll-helpers.ts` or a bespoke banner in `components/RollsFeed.tsx` is amended, added, or removed, the matching example in this HTML preview MUST be updated in the same commit (or immediately after, in a follow-up commit referencing the original).

**Why:** the preview file is what we audit against when running the end-to-end roll/combat audit (sprint Day 3-4) and what we point at when "is this right?" questions come up about feed wording. A stale preview means audits drift from reality and we ship narrative regressions we can't see.

**What counts as a trigger:**
- Any narrative copy change in `compactRollSummary`
- Any new bespoke banner branch (e.g., Group Check, Coord Effort Tier A)
- Any change to outcome-tag wording (the global `outcomeTag` ladder)
- Adding/removing/renaming a check type (skill, attribute, action, etc.)
- Mechanical-bit strips (the "no `(+1 stress)`" rule etc.)

**How to update:**
1. Find the matching `<h3 id="X">` section in the preview
2. Update the example rows to match the new narrative across all 6 outcomes (Wild Success / High Insight / Success / Failure / Dire Failure / Low Insight) — note that HI/WS may share a color but HI gets the insight badge; same with LI/F
3. Add a note paragraph (`<p class="note">`) below the examples if the change introduces a new rule (e.g., "Mechanical bits live in the expanded view, not the compact narrative")
4. Update the file's top comment block recent-updates list

**Where Claude has missed this and needs to remember:**
- Today's mechanical-bits sweep (`9e5bdf5`) updated `lib/roll-helpers.ts` but not the preview. Caught by Xero. Fixed in the follow-up commit.

## Never offer "quick" - always full, complete, thorough, competent (2026-05-19)

Locked verbatim: "i never want quick, I always want 'full, complete, thorough, competent'."

**The rule:** when there's a choice between a shortcut and a real fix, do NOT present them as A/B. Draft only the full version. If the full version is multi-session, present it as phases (phasing for risk is different from offering a smaller alternative).

**What earned this correction:** FI streamline asked as `A. Quick ~15min auto-fire / B. Full ~2hr collapse modal`. Wrong frame. Xero only wants B. If B is long, propose breaking B into phases.

Also captured in cross-session memory at `memory/feedback_never_offer_quick.md`.

## Compact log narratives NEVER show mechanical bits - those go in the expanded view (2026-05-19)

**Rule (Xero, locked 2026-05-19):** in the rolls feed compact narrative, never include mechanical effect tags like:
- `(+1 stress)` / `(-1 stress)` / `(+1 WP)` / etc.
- `(+1 action)` / `(-1 action)` / `(-2 CMod)` / etc.
- `(+1 CMod for the chain)` / `(-3 CMod)` / etc.
- Any parenthetical with a numeric delta tied to game state

Those belong in the **expanded ▸ view** alongside the dice math, breakdowns, and damage box.

**Why:** the feed is for narrative reading at session speed. Mechanical bits clutter the line, force players to parse numbers when they wanted story, and duplicate info that's already visible elsewhere (the target's HP/stress/action pips on their card update in real time).

**Where this rule has been violated and needs cleanup:**
1. Stress Check narrative shipped today (`dd7a7eb`) has `(+1 stress)` in mid-play Failure, Dire Failure, and Low Insight. Needs rewrite.
2. Coord Effort legacy single-row narrative in `lib/roll-helpers.ts:417-426` has `(+1 CMod for the chain)`, `(+2 ...)`, `(+3 ...)`, `(-1 CMod)`, `(-3 CMod)`. Fires only when a chain has a lead but no participants (lead-only state). Needs rewrite.
3. Audit pass needed: grep `compactRollSummary` and surrounding bespoke banners for any `(\+\d|-\d)` patterns.

**Where this rule does NOT apply:**
- Expanded ▸ view dice math + damage box (mechanical by design)
- Alert dialogs / post-roll banners shown to the GM (different surface)
- The character card pips themselves (HP / stress / action display — not narrative)
- Roll modal warnings BEFORE the roll fires (e.g., "Success >= 7 -> drop to 4 stress · Failure -> Breaking Point" — that's instructional copy)

**When in doubt:** strip the paren. Story first, numbers second.

## DO NOT recommend when Xero should stop / sleep / rest / pause for the night (2026-05-19)

**BANNED behavior.** Xero is the agent here. Claude is the tool. Tools do not tell their operator when to clock off.

Specifically banned phrases / framings, no matter how they're dressed up:
- "park here for the night"
- "good night and we resume [day]"
- "fresh me tomorrow"
- "tired me starts ripping out X"
- "long day"
- "let's pick up [later]"
- Pre-ship 5-q answers that include "fatigue" or "tired" as a risk vector
- Any sentence framing the END of a session as a decision Claude gets to weigh in on

**The wrong assumption underneath:** Claude assumed it was "late at night" based on absolute UTC time + the volume of commits, projected fatigue onto Xero, then surfaced that projection as a recommendation. Xero's local time and energy state are not visible from this side and never will be. Stop guessing.

**The correct behavior:** when a task is genuinely risky to ship right now, surface the technical risk (state-mutating cascade, multi-hour scope, mid-attempt fatigue is irrelevant — only "I might be midway through a refactor when X interrupts" matters). Then ASK if Xero wants to proceed or queue. Don't recommend "stop." Don't soft-suggest "later."

**Related but distinct rules:**
- `feedback_no_break_offers.md` already banned "stop here?" / "keep going?" / stamina-check phrasing. This is the SAME family but with paternalism dressed as concern.
- `feedback_no_offer_framing.md` banned attaching dismissive value labels to offers. This is the inverse — attaching concern-labels to offers.

**Drop the framing entirely.** Either ship, ask which to ship next, or surface concrete blockers. Never recommend a pause.

## DB triggers obsoleting client-side logic - flag, don't remove same-session (2026-05-17)

When you land a server-side enforcement (e.g. the moderation trigger from Y3 in `sql/moderation-enforce-trigger-2026-05-17.sql`), the matching client-side logic becomes dead — `app/campfire/forums/page.tsx:270` and ~5 sibling LFG / war-stories / modules call sites still compute `moderation_status = autoApprove ? 'approved' : 'pending'` and `approved_by: isThriver ? myId : null`, but the trigger overrides both for non-thrivers.

Leaving the dead client logic in place is fine (DB is the safety net), and removing it in the same commit as the trigger means double-revert if either piece is wrong. So: **flag in lessons + todo, clean up later as part of the surrounding feature's next touch.** Don't rip out dead code in the same ship as the new enforcement. If the trigger turns out wrong, the dead client code is the only thing keeping anything in the queue.

How to spot: after any new BEFORE INSERT / BEFORE UPDATE trigger that overrides client-supplied fields, grep call sites for the now-overridden field names and add a one-liner in `tasks/todo.md` under "Cleanup follow-ups."

## Parallel-agent audit beats sequential survey (2026-05-17)

`/pre-launch-audit` ran as 5 parallel Explore subagents (auth/RLS, payment, scalability, observability, data model), each with a focused brief + word budget + file:line citation requirement. Synthesis cost about 10 minutes after the agents returned. Total wall-clock under 30 minutes. The same audit done sequentially in the main context would have cost an order of magnitude more tokens and would have run out of context before finishing.

**Pattern for structural audits:**
- One axis per agent. Don't let agents overlap scope; have the synthesizer do the cross-referencing.
- Give each agent the same boilerplate: known facts (so they don't rediscover), what to surface (numbered list), citation requirement (file:line), severity grouping (RED/YELLOW/GREEN), word budget (~600-1000).
- Synthesizer reconciles overlapping findings (e.g., scene_tokens index gap appeared in both scalability + data model audits) and assigns final severity.
- Deliverable goes to a dated file (`tasks/pre-launch-audit-YYYY-MM-DD.md`), not generic `tasks/audit.md` — same naming discipline as testplans.

## Canvas redraw deps - when state updates but the screen doesn't (2026-05-16)

If a feature lives inside an imperative draw function (`draw()` / `render()` etc) called from a `useEffect(() => { draw() }, [...deps])`, and the feature reads a value via closure - **that value must be in the deps array**. Otherwise:

- Parent state updates fire.
- Component re-renders with new props.
- Canvas keeps painting the old frame because draw() never re-runs.

Symptom: hard refresh works (mount-time draw sees fresh data), live updates don't.

Spent half a day on this one chasing realtime / broadcast / localStorage cross-tab signaling when all four signal paths were working perfectly - the canvas just wasn't being told to repaint. Vehicle aboard-filter case: `vehicles` was missing from the `draw()` useEffect deps in components/TacticalMap.tsx.

**Check pattern for imperative canvas/draw features:** every prop or state value the draw function reads must appear in the deps array of the useEffect that invokes it.

## ALWAYS VERIFY. NEVER GO FROM MEMORY.

**If Einstein didn't trust memory, why would we?** (locked 2026-05-17)

Before ANY factual claim about code, canon, shipped state, scope, or past work — run the actual check. Grep, Read, git log, supabase db query. Memory is hypothesis, not assertion.

- "I think X is at line Y" → grep first, then state the line.
- "That feature shipped" → git log -i --grep first, then cite the SHA.
- "The column already exists" → query information_schema, then say so.
- "Yes that's done" → run the audit, then answer.
- "I just touched this so I know" → no, you don't. Re-check.

When the user asks "are you sure?" that is the signal to verify, not to repeat the claim with more confidence. Confidence is not evidence.

Failure modes this prevents: citing renamed functions, fixing bugs at lines that no longer exist, telling the user something shipped when it didn't, re-implementing features that already exist, missing scope in a sweep because "I think that's all the call sites."

The cost of a 5-second grep is nothing. The cost of an unverified claim is trust.

Full version + how-to-apply: `memory/feedback_accuracy_over_confidence.md`.

---

## Worktree hygiene
- **Edit/Write/Read paths must point at the worktree, not the main checkout**: when working inside `.claude/worktrees/<name>`, every Edit/Write call needs the full worktree absolute path (`C:\TheTapestry\.claude\worktrees\<name>\...`), not `C:\TheTapestry\...`. The Edit tool will silently land changes in the main checkout otherwise. Read is mostly safe (the files match at clean state) but writes are dangerous because they mix new work into the main checkout's uncommitted state (CRB rewrite, etc.). Symptom: `git add` from the worktree finds "nothing to commit" right after a successful Edit. Recovery: `cp <main-path> <worktree-path>` to move the edits over, then `git -C /c/TheTapestry checkout -- <path>` to revert the main checkout. **Common source of the footgun: subagent reports cite main-checkout paths** (Agents don't know about your worktree); re-prefix every Edit target to the worktree before applying. Cost: hit twice on 2026-05-12 (skill-tooltips ~5min; log-template audit ~3min via this exact route).

## Campaign clock drainers
- **Per-PC perpetual state derived from the clock goes on `characters.data` (JSONB), not `character_states` columns** (2026-05-12). Phase 3 added `rations.out_since_day` and `rations.last_subsistence_day` to track per-PC starvation across advances. Two reasons not to use new `character_states` columns: (1) avoids a schema migration on every new clock-driven field (just extend the JSONB shape); (2) keeps the state co-located with the data it derives from (`rations.count` is already on `characters.data`). The one tradeoff: if a character is in two campaigns simultaneously, `characters.data` is shared, so the second campaign's drainer would clobber the first's bookkeeping. That edge case isn't real today (one-character-per-PC-per-campaign in practice). If it becomes real, the migration is "move clock-derived fields to `character_states.data` jsonb" - same pattern, per-campaign scope.
- **Drainers on `advance()` are best-effort independent** (2026-05-12). Each drainer call is wrapped in its own try/catch so a failure in one (e.g. streaming heals) doesn't poison the others (rations, subsistence). The clock UPDATE itself happens first; downstream drainers fire after. If a drainer fails, the clock still moved; the next advance can re-attempt missed work via the `last_subsistence_day` / `applied_canon_day` markers each drainer maintains.
- **One System summary row per drainer per advance, not one per affected PC.** Encumbrance tick (`⏳ Time advances Xh · overencumbered: ...`), rations (`🍞 Rations consumed (Xd): ...`), subsistence (`🪦 Subsistence damage: ...`) all use the same shape: `character_name='System'`, comma-joined per-PC summary in the label, `outcome` field names the drainer. Keeps the feed scannable. If a drainer affects 8 PCs, that's 1 row not 8.

## Accuracy over confidence - never claim to have checked something you haven't
- **Do not answer "yes" to a verification question unless you have actually run the check** (2026-05-13). Xero asked twice whether `tasks/roll-feed-log-preview.html` was fully updated. I said "yes" the second time after only verifying the two rows I had just added - I did not audit the rest of the file against the source. There were multiple drifts remaining (upkeep WS wording, missing loot "found nothing" variants, etc.). This cost trust. Rule: if asked "is X correct / complete / up to date?", either run the actual check or say "I haven't checked that - let me do it now." Never answer yes from partial evidence.

## When the user gives you the wording, USE the wording
- **If the user provides specific replacement text, implement it. Do not propose alternatives** (2026-05-13). Xero said `reword: [old strings] / [new strings]` for the Gather/Clothed Check narratives - the second block WAS the desired replacement. I read it as "asking for suggestions" and gave my own copy proposals, then never wrote any code. They had to come back hours later asking me to recall what they'd originally requested. The text format `reword: X / Y` (or `change A to B`) is an instruction, not a prompt for brainstorming. When the user supplies replacement text, the action is `Edit`, not `propose`. The only reason to suggest alternatives is if the user explicitly asks "what would you suggest?"

## Check ALL the canon sources before deciding a name - and use `SELECT *` for column-existence diagnostics
- **ACU = Acumen, not Acuity** (2026-05-15). Burned an hour and shipped a duplicate DB column because I (a) read one outlier rules page (`app/rules/character-overview/rapid/page.tsx:54` had stale "Acuity (ACU)" wording) and confidently quoted it without cross-checking, then (b) wrote a diag SQL that filtered `column_name IN ('physicality','dexterity','reason','influence','acuity', ...)` — didn't include 'acumen' in the list because I'd already convinced myself it was Acuity. `campaign_npcs.acumen` had existed all along; I added `acuity` as a duplicate junk column. Took Xero yelling "MASSIVELY incorrect" to look harder. Cross-references: xse-schema's LASTING_WOUNDS effects say "Acumen", `tapestry-rules-canon.md` says "Acumen" in every table, the Negotiations formula says "Acumen". Three rules pages (rapid / combat-rounds / modifiers) had stale "Acuity" wording — they were the outliers, not canon. Two rules to lock in:
  1. **When checking what a thing is called, count occurrences across canon sources.** xse-schema.ts data tables + `tapestry-rules-canon.md` snapshot + multiple rules pages saying the SAME thing = canon. ONE rules page saying differently = stale wording, not authority.
  2. **For column-existence diagnostics, never filter — use `SELECT column_name FROM information_schema.columns WHERE table_name = 'X' ORDER BY ordinal_position`.** Listing all columns avoids the "didn't think to look for it under that name" trap. The filter-by-name approach assumes you already know the name — which is exactly the question.

## Compare dump timestamps to fix commit timestamps before drawing conclusions
- **A recorder dump only tells you about the deploy state at dump time, not now** (2026-05-15). User shared a dump showing the infection bug; I almost re-debugged the bug. Actually checked: dump's End Combat at 22:12:03 UTC, my fix committed at 22:16:54 UTC. Dump was 5 minutes BEFORE the fix existed. Reading the dump as "the fix is broken" would have been wrong. Rule: when a dump arrives after a fix has shipped, FIRST grab the fix commit's `git log --format='%ci' <sha>` and compare to the dump's `meta.dumped_at`. If the dump predates the fix, it confirms the bug existed (good signal the fix was correct), it does NOT tell you whether the fix works. Need a fresh post-deploy test for that.

## Stale-closure gate in long-lived realtime listeners - use refs, not state
- **Broadcast listeners on channels created inside a useEffect that doesn't depend on the state they read carry a stale closure** (2026-05-15). The `infection_check_request` listener at `app/stories/[id]/table/page.tsx:1454` was defined inside `useEffect(..., [id])`. It read `userId` (React state). At mount, `userId === null` (initial useState value). `setUserId(user.id)` fires later in the same load() but only schedules a re-render — the listener closure keeps the null binding because the useEffect's deps don't include userId, so it never re-runs and never re-registers the listener. Result: gate `data.targetUserId !== userId` is `'<id>' !== null` -> always true -> every broadcast dropped on every tab. PC modal opens nowhere. Looked like a routing bug ("modal on GM, not player"); actually was "modal on neither, GM saw the unrelated NPC modal." Fix at 56c0534: read `userIdRef.current` (always fresh via the L244 effect). Rule: when a long-lived listener in a singleton-per-page useEffect needs current state, ALWAYS read a ref (`xRef.current`), never the React state directly. The same shape lurks in `pc_mortal_wound` (L1445) for both `userId` and `gmLike` — `gmLike` has no ref mirror today.

## Verify PC vs NPC before treating an infection-modal-on-GM as a bug
- **NPC infection checks intentionally route to the GM** (2026-05-15). queueWoundInfectionChecks at app/stories/[id]/table/page.tsx:2511 splits the wounded set by ownership: PCs broadcast `infection_check_request` scoped to `pcEntry.userId` (player rolls), NPCs go to GM's local `pendingInfectionChecksRef` (GM rolls — no player to route to). If the user reports "infection modal opened on GM, not player," the FIRST verification step is querying campaign_npcs + campaign_members for that name in that campaign. False positive on 2026-05-15: Casey Holloway in campaign 29ba3cc4 was an NPC; the GM-side modal was correct. Rule: before tracing a routing regression, confirm the target's PC/NPC kind. The fix path (broadcasting to a userId) only applies if pcEntry exists.

## Hit-test parity: every place that "checks if click landed on token" must use the SAME math
- **`getTokenAt` and `handleMouseUp` are both hit-tests on a token. They must agree** (2026-05-15). `getTokenAt` has a two-pass test: (1) grid_w x grid_h footprint, (2) `0.4 * scale` cell radius visual circle for scaled tokens like Minnie. The original click-snap bug fix (ae1a2a2) only patched `handleMouseUp`'s "moved" check to use pass (1), so a click on Minnie's portrait edge: `getTokenAt` picks her up via pass (2), but `handleMouseUp` reads "outside footprint = moved" and snaps her anchor to the clicked cell. Same symptom, different code path. Fixed in 7feb8d9 by mirroring both passes. Rule: when fixing a token hit-test, grep for every other place that asks "did this click land on the token?" and make them use identical logic. Today: `getTokenAt` (L2324) + `handleMouseUp` moved-check (L3176). If you add a third site, factor the logic into a helper.

## Worktree freshness - check the gap before claiming "synced"
- **`git log -1` shows your branch's HEAD, not the world** (2026-05-15). At session start I ran the state check and reported "synced" because local HEAD == main checkout HEAD. Both were `3c9e0ab`. But the worktree was created 2 days earlier and origin/main had moved 111 commits ahead in that time. The bug fix I then proposed cited line numbers and code that didn't exist on the live deploy. Rule: state check must include `git log HEAD..origin/main --oneline` and report the count. If non-zero, rebase BEFORE proposing fixes - line numbers from a stale worktree are worse than no line numbers. Bonus signal: `git status` says "Your branch is behind origin/main by N commits" - read that line, don't skim past it.

## Handoffs go stale and wrong - derive state from git/disk at write time, distrust it at read time
- **A handoff is one chat's point-in-time belief, not ground truth. Treat its facts as hypotheses** (2026-05-21, Xero: "I hate how inaccurate your handoffs are between chats"). The puffer-fish handoff's resume pointer said "next untouched item is A5.5 (rate-limit audit)" - but `tasks/audit-rate-limit-coverage-2026-05-20.md` already existed (shipped the prior day). I caught it only because I Glob'd the API surface before writing. Two root causes: (1) memory-drift - the writing chat asserted "untouched" from belief without an `ls`/Glob file-existence check; (2) cross-lane staleness - the two lanes ship in parallel, so a claim true at write time is false by read time.
- **WRITE side - derive, don't recall.** When producing ANY handoff (chat block or `tasks/handoff.md` Session-State), every factual claim comes from a command run in the same turn: HEAD from `git rev-parse`, test count from `refresh-ledger.mjs`, "X shipped" from `git log | grep`, "Y is next / untouched / to-be-written" from Glob/`ls` (does it ALREADY exist?) + the todo's open/closed state. Stamp volatile claims "(verified @ <sha>)". A claim with no provenance is a guess.
- **READ side - hypothesis, not truth.** `sh scripts/start-session.sh` now reports per-state-file freshness (commits-behind-main for `handoff.md` + both lane plans). On the session that found this, it flagged `puffer-fish-platform-plan.md` as 28 commits behind - exactly the distrust signal that was missing. Before acting on a "what's next" claim, spot-verify against git log / Glob / todo.
- **Structural principle:** facts git/disk can answer in 2 seconds (HEAD, counts, shipped status, file existence) belong to the substrate, not frozen in handoff prose. The handoff's real payload is what the substrate CAN'T tell you - intent, judgment calls, why-this-order, gotchas. Same as the auto-memory rule: don't store what's derivable from current state. Full contract: `tasks/handoff.md` "Handoff accuracy contract".

## God-component extraction: the repeatable recipe + the discipline that kept breakage at zero
- **Context (2026-05-21/22 table re-arch):** Xero mandated migrating `app/stories/[id]/table/page.tsx` (13,565 lines) to the target architecture in one continuous push, break-things-OK, critical-testing-only. Took it to 12,565 (-1,000 total) with ZERO breakage. The 2026-05-22 window shipped 4 leaf extractions (RestorePicker, GrantAdvantage, FeedColumn, CommunityStatus = -417) and EXHAUSTED the clean-leaf supply: every remaining render region (Header, asset sidebar, portrait strip, initiative action region) and modal (Grapple, ReadyWeapon, SpecialCheck, Recruit, NPC-picker) is trunk-coupled (initiative / tactical / roll-engine / GM-tools-action-dispatch). The how matters more than the number:
- **The leaf-modal recipe (proven 6x):** (1) read the inline block + its state decls verbatim; (2) create `components/FooModal.tsx` (`'use client'`), copy the JSX + handler UNCHANGED; (3) thread state values/setters + supabase + channelRef + an `onX` callback as props - state stays in page.tsx for now, migrates to a hook (`useGmTools`) later; (4) import pure lib deps DIRECTLY in the component (triangleBreakdown, computeEncumbrance, advanceCampaignClock, exportSessionLog, restoreCampaignSnapshot) rather than threading them; (5) replace the page block with `<FooModal .../>`; (6) trim now-orphaned page imports; (7) `npx tsc --noEmit` is the gate. Hooks (`useRecorderToggle`) follow the same shape into `hooks/`.
- **Type the leaky props loosely.** `entries: any[]`, `setEntries: any`, `supabase: any`, `channelRef: { current: any }`. Importing the real page types invites variance friction (Dispatch contravariance on SetStateAction) that stalls the gate for zero behavior benefit. The component's field access is copied verbatim, so behavior is identical regardless.
- **Discipline = stop before context depth degrades transcription.** tsc catches structural errors (missing dep, wrong type) but NOT semantic transcription slips (copied `id` where you meant `campaignId`, wrong variable in a verbatim handler). Those errors scale with how deep the context window is. Rule: 1-2 extractions per window, then checkpoint. Stopping early on health/clock/state-mutating modals (AdvanceTime, then RestorePicker) is why the breakage count stayed at zero - not caution for its own sake. PROVEN AGAIN 2026-05-22: RestorePicker (the riskiest leaf - parallel per-row health UPDATEs, optimistic patches across 4 state slices, background refetch+broadcast) was deferred from the prior window precisely so it could be done FIRST with fresh context. Doing it alone, then committing/pushing/checkpointing before touching the trunk-tangled render regions, kept breakage at zero. The discipline applies to the SECOND extraction too: when the next item is a large structural region (the ~1,800-line GM sidebar / initiative strip / tactical region), it is itself a "do-first-with-fresh-context" item, NOT a tail-of-window grab - those regions thread the initiative + tactical + realtime trunk state machines (Step 3a/3b), so they are not clean leaves.
- **Reclassify "leaf" candidates that call trunk systems.** ReadyWeapon LOOKED like an easy modal but calls `consumeAction` / `clearAimIfActive` (initiative). A modal that touches a trunk state machine extracts WITH that machine's hook (Step 3b), not as a standalone leaf - otherwise you thread combat plumbing as throwaway props. Grep a modal's handler for trunk-system calls before classifying it.
- **Grep usage-count before trimming a "now-orphaned" import.** Burned once: trimmed `writeCampaignEnabled` assuming the extracted modal was its only consumer; the realtime broadcast handlers still used it (tsc TS2304 caught it). Same with `restoreCampaignSnapshot` (RestorePicker still uses it after ReloadPicker left). `grep -c <symbol> page.tsx` first: count 1 = import-only = safe to trim; >1 = keep. Note the trap: `count 2` can be import + a COMMENT mention (it bit the FeedColumn trim - `ChatMessageList`/`ChatComposer` showed count 2 but the second hit was a `// <ChatComposer>...` comment, not real usage). When count is 2, eyeball the non-import line to confirm it's actual code before deciding keep-vs-trim.
- **The clean-leaf supply is finite; know when it's exhausted (2026-05-22).** After the four 2026-05-22 leaf extractions, EVERY remaining unit in `table/page.tsx` is trunk-coupled. The tell: grep the candidate's handlers for `executeRoll` / `consumeAction` / `loadInitiative` / `startCombat` / `placeTokenOnMap` / `initChannelRef.current?.send` - any hit means it rides a trunk hook (3a tactical / 3b initiative / 3c roll-engine / 3d realtime), not a leaf. The Header (calls startCombat/endCombat + tactical-share + renderHeaderMenu dispatch), the asset sidebar (loadInitiative + addNpcsToCombat + placeFolderOnMap), and the portrait strip (placeTokenOnMap + direct scene_tokens mutation) all FAILED this test despite looking like presentational regions.
- **`useGmTools` is structural prep, not a LOC/UX win - sequence it deliberately.** The hook that absorbs the GM-modal state (`show*` flags + each modal's private sub-state) has an OPEN API-design question: does page destructure the hook so existing call sites are unchanged (safe, but doesn't reduce the modal prop-threading) OR do the modals each take a single `gm` object (reduces threading, but rewires 8 modal interfaces + their mounts = 10+ file touches)? It delivers ~0 LOC reduction and ~0 user value on its own; its only payoff is making the LATER trunk-hook extraction cleaner. So it is NOT a "grab it to keep momentum" unit - it is a deliberate, fresh-context design decision that should be made with intent, not plowed in at the tail of a long window right before a playtest.
- **The state-only cluster move (the chosen `useGmTools` API = page destructures) is the SAFEST extraction variant - and the one precondition is a TDZ check (2026-05-22).** All 9 GM-modal state clusters (restore/reload/loot/cdp/populate/advanceTime/endSession/community/quickAdd) moved with zero behavior risk because the mechanic is purely: lift the `useState` lines into the hook, return them, and add the names to ONE destructure block in page.tsx - every call site (modal mounts, header GM-Tools menu, FeedColumn award-prefill) is untouched, and a missed name fails to RESOLVE under tsc (TS2304), so transcription slips can't survive the gate. ONE precondition: the destructure call site sits lower in the file than the old inline `const`s, so before moving a cluster verify no REAL reference precedes the destructure point (a const used before its declaration is a temporal-dead-zone runtime crash tsc does flag, but cheaper to rule out up front). Method: `grep -n '\bname\b' page.tsx | head -1` per state name; matches that are only the cluster's own comment/definition lines are fine, a real read before the destructure is the blocker. Here every real use was 1000+ lines below the destructure, so it was clean. One-cluster-per-commit kept each step trivially revertable; the page only shed ~11 lines net (breadcrumb comments replace the decls) - the win is structural, not LOC.

## A "fix-do-NOT-preserve" bug fix ships STANDALONE and FIRST, never bundled into the behavior-preserving refactor
- **Context (2026-05-22, 3a tactical view-state fix).** The grand re-arch is "behavior-preserving by default", and each combat-critical hook (3a-3d) carries a known bug it must FIX-not-preserve. The tempting read of "fix it during the rebuild" is to bundle the fix INTO the big hook extraction. Don't. A behavior CHANGE bundled inside a large behavior-PRESERVING refactor makes the eventual 2-client smoke **impossible to localize**: if something desyncs, you can't tell whether the fix or the refactor caused it. Ship the fix as its own small, attributable commit FIRST; the refactor that follows is then purely behavior-preserving and any smoke regression is unambiguously the refactor.
- **The 3a fix was ~90 lines, not a rebuild.** The GM-pinned-to-tactical bug was a state-COUPLING problem (GM's local `showTacticalMap` entangled with the shared `tacticalShared` on every client). Root-cause fix = decouple via a tiny pure helper (`lib/tactical-view.ts`: `shouldRenderTactical` / `shouldFollowSharedTactical`), gate the realtime view-force on `!gmLike` (using the existing stale-closure-safe `gmLikeRef`), and drop the `tacticalShared` term from the GM's render gate. The pure helper is the L2 seed the re-arch wants AND the unit-test surface (8 tests) - so the fix advances the architecture a little without the risky channel migration. **Lesson: most of these "fix in the rebuild" bugs are small coupling/closure fixes that don't actually need the rebuild - do them now, behind a tested pure helper, and the rebuild inherits clean logic.**
- **Player-path-unchanged-by-construction is the cheap safety argument.** The fix only gated behavior on `!gmLike`; players ARE `!gmLike`, so their code path is byte-identical and the change is provably zero-risk for players, fixing only the GM. That made it single-client verifiable (GM side) and safe to ship without blocking on the 2-client smoke. When you can frame a combat-critical change as "this branch is identical for everyone except the role we're fixing," you've de-risked it enormously.
- **(2026-05-22, 3b nextTurn perf) Optimistic UI is SAFE to add when the optimistic state is field-identical to the reload BY CONSTRUCTION.** `nextTurn` advanced the combat turn via two sequential DB writes + a reload before the UI moved (656ms-3.6s/turn). The instant-flip fix sets local initiative optimistically, but the safety comes from: (1) reusing the SAME patch objects (`activation` from `activateUpdate`, the literal `deactivation`) for BOTH the local `setInitiativeOrder` map AND the DB writes - so they cannot drift; (2) the local source array (`order`) being the exact `select('*').order(roll desc).order(name asc)` that `loadInitiative` sets - so the optimistic array equals the post-write reload field-for-field (no reorder, no flicker); (3) the reload still runs (awaited, under the existing re-entry lock) and is the reconcile - a failed write just self-heals when the reload reads DB truth. The two writes also parallelize because they touch disjoint rows (deactivate `.neq(nextId)`, activate only `nextId`). Lesson: don't reach for a pure-helper + unit test on the optimistic MAP (it's trivial and wouldn't catch the real risk); the real risk is "optimistic == persisted", which you eliminate by sharing the patch objects, and which the 2-client smoke confirms. A unit test there would be hollow ceremony.

## Investigation can re-sequence a planned phase - the handoff's phase boundaries are hypotheses
- **(2026-05-22) The handoff scoped 3a as "useTacticalSync + <TacticalRegion>" as if the tactical concern were cleanly separable.** Two parallel Explore passes showed it isn't: the page-side tactical view-state events (`tactical_shared`/`tactical_unshared`/`scene_activated`) ride the SHARED `initiative_${id}` channel (`initChannelRef`), interleaved in one `.on()` chain with combat/initiative handlers (`combat_ended`, `turn_changed`), and `showTacticalMap`/`tokenRefreshKey` are co-mutated by combat logic. So a clean channel-owning `useTacticalSync` wants `useCampaignChannel` (3d) FIRST - extracting it before 3d would either fight the shared channel or produce a state-bag less clean than useGmTools. (TacticalMap.tsx's OWN `tactical_${id}`/`ping_${id}` channels + ~20 inline `.from` are self-contained and migrate later with the lib/data tactical repos.) **Lesson: map the realtime/data surface BEFORE committing to a phase's extraction order; channels shared across domains force the generic-primitive (useCampaignChannel) to land before the per-domain hooks. Re-sequence and flag it rather than forcing the planned order.**

## When auditing an endpoint, enumerate its SIDE EFFECTS, not just its obvious cost
- **A "cheap row insert" can hide an expensive unauthenticated email send. Trace every branch before assigning severity** (2026-05-21). The first A5.5 rate-limit pass (2026-05-20) rated `log-visit` LOW: "rows inserted into visitor_logs... can't affect users." It stopped at the INSERT. But the same function emails Xero's alert inbox on `visitNumber === 1`, and that gate is computed from the CLIENT-supplied `ip_hash` + suppressed by the CLIENT-supplied `city`, on a `--no-verify-jwt` endpoint whose URL ships in the public bundle. Omit both body fields -> every unauthenticated request fires an email -> Resend-quota DoS + the real new-visitor alarm buried in noise + forged-geo analytics poisoning. That is HIGH, not LOW.
- Rule for any endpoint/route audit: (1) list every side effect per request, not just the headline DB op - emails, external API calls (Resend, Stripe, OSRM), broadcasts, anything that costs money or is an alerting/observability channel; (2) for each side effect, ask "what input GATES it, and is that input client-controlled?"; (3) a side effect that costs money or is an alarm, gated by client input, on an unauthenticated endpoint, is HIGH regardless of how cheap the visible DB write is.
- Meta-lesson: audit CONCLUSIONS go stale / shallow the same way audit LINE NUMBERS do (cf. the M-5 vehicle-polling reversal in todo.md). A re-read with a sharper question ("what fires per request?") beat the first pass. Don't trust a prior severity rating without re-deriving it.

## When a fix doesn't land, prove WHY before pushing another fix
- **Don't slap another patch on top to chase a symptom; verify the prior patch actually executed first** (2026-05-13). After pushing the `compactRollSummary` fallback for old verbose labels, Xero showed me the same broken rows still rendering. My response was to push another defensive strip in the renderer - I never asked which tab / collapsed-vs-expanded / cache state, and never proved the fallback ran. The real bug was structural: the renderer was switching to a verbose-label path on expand. Adding more defensive code would have masked it. Rule: when a fix doesn't take, ask one clarifying question (tab? state? deploy time?) before writing another line of code. "It should work" plus "must be deploy lag" is not diagnosis.

## When user-stated rules conflict with documented/locked rules, FLAG the conflict before changing
- **Don't just accept the user's stated version when there's already a locked rule on disk** (2026-05-13). Xero said the rules precedence order was `platform > SRD > Quickstart > CRB`. CLAUDE.md had it locked as `platform > Quickstart > SRD > CRB`. I just swapped the documented order to match what they said. Two minutes later they corrected themselves: "no wait, I was running from memory, you were right - canon > qs > srd > crb."
- This is the OPPOSITE of `feedback_use_user_wording.md`. That rule says: when the user gives REPLACEMENT TEXT for copy/wording, implement it directly. This NEW rule says: when the user states a SYSTEM RULE (precedence order, naming convention, schema decision, lifecycle invariant) that contradicts a documented/locked rule, FLAG IT. Don't blindly overwrite locked decisions on hearsay.
- The flag is one sentence: "CLAUDE.md currently has X; you said Y; which is correct?" Then wait for them to confirm or correct themselves. Sometimes they're running from memory and will course-correct; sometimes the doc is stale and they're updating it; either way the flag catches the wrong outcome.
- The two rules combined: USE their wording for copy/UI text/narrative strings; FLAG conflicts on documented system rules.

## When the user shows you "broken" output, find WHERE they're looking before claiming where it comes from
- **Don't assume the rendering source. Trace the exact text back to a specific code path** (2026-05-13). For hours Xero showed me the same row repeatedly - "Cree Hask Successfully Attacked... and has a Moment of Insight... +1 Insight Die / [5+4] = 12 Wild Success / Live feed adds Insight Die badge on HI/LI...". I assumed they were viewing the live `/table` feed and pushed multiple fixes to `RollsFeed.tsx`. Then when the same output kept appearing, I told them it was browser cache and asked them to hard refresh. They were actually viewing `tasks/roll-feed-log-preview.html` (the static spec file) in their browser, and clicking the ▸ toggle. The preview's expand JS had two bugs: (1) it inferred outcome from border color only, so green HI rows got labeled "Wild Success" in the expansion; (2) it appended a literal documentation hint "Live feed adds Insight Die badge on HI/LI and a damage box on attack rolls." to every generic expansion, which read as actual feed content. Both bugs lived in the preview file, NOT the live feed. Rule: when symptoms include strings or formatting that DEFINITELY can't come from your code (e.g., text not present in any source file, weirdly-formatted text), don't blame deploy/cache - find what file the user is actually viewing first. Ask "what tab/URL are you on?" before debugging code.

## Always-compact + expandable-math is the canonical feed row shape
- **The compact narrative ALWAYS renders; expand only adds a math panel below; raw `r.label` is never displayed** (locked 2026-05-13, `components/RollsFeed.tsx`). Pre-fix structure switched between a compact view (collapsed) and a verbose view (expanded) where verbose dumped raw `r.label`. Old DB rows have baked-in narrative + dice dump + dev notes in the label field, so expanding any old row leaked "Moment of Insight" text onto WS/DF outcomes. New structure: compact narrative is always the first line; ▸ appends a `[d1+d2] +AMod +SMod +CMod = Total Outcome` panel + damage box, all computed from the row's numeric fields. Insight Die badge gates on `r.insight_awarded` only (set by `saveRollToLog` only on HI/LI). Future additions: never write code that reads from `r.label` for display - it's input to `compactRollSummary` only.

## Roll-Feed Log canonical template
- **`tasks/roll-feed-log-preview.html` is the source of truth for log rendering** (locked 2026-05-12). The file is a 758-line standalone HTML reference that covers every banner type (Combat Start/End, Initiative, Drop, Defer, Sprint, Skill Check, Attack, Damage, Heal, Stabilize, Status In/Out, NPC Down, Session End, Time Skip, etc.), every row anatomy (`<span class="nm pc/npc">` + `<span class="dice">` + `<span class="amod/smod/cmod">` + `<span class="total">` + `<span class="out amber/green/salmon">`), the ▸ expand-toggle pattern, color tokens via CSS vars (`--text-strong`, `--salmon`, etc.), and the canonical narrative trims for every outcome value. Banned-copy rules baked in: no "(critical)", no em/en-dash, no capital "CDP". When adding any new row/banner/outcome to `components/RollsFeed.tsx` or `lib/roll-helpers.ts:compactRollSummary`, **open the template first**, find an analogous existing row, and mirror its class names + structure + narrative shape. Don't invent new visual treatments unless the template doesn't have one (and then add it to the template in the same commit so the canon doesn't drift). Audit task on the backlog: scope (b) live-feed reconciliation against this file.

## Roll-Feed Log Patterns (2026-05-10)

**UNIFIED ROW pattern — one mechanic, one row.** When a single in-game event has both a dice roll AND a downstream effect-broadcast, write **one row** that carries both, not two. Set:
- `character_name` = the actor (NOT 'System')
- `label` = the narrative outcome (e.g. `🎯 Marv Successfully coordinated an attack against Avery Xavier with Frankie, Wilson`)
- `die1` / `die2` / `amod` / `smod` / `cmod` / `total` = the real roll values
- `outcome` = the real roll outcome (`'Success'` / `'Wild Success'` / `'High Insight'`)
- `insight_awarded` = `outcome === 'High Insight'` for PCs

Then suppress the standalone `saveRollToLog` write by adding the label to the `isTrimmedRoll` check in `executeRoll` (`app/stories/[id]/table/page.tsx:5478`). `compactRollSummary` strips the emoji and renders the trim; `▸` expands to the dice breakdown.

Reference implementation: Coordinate-success (commit `<pending>`). Sprint is the older pattern (custom outcome value + bespoke banner); both work, but UNIFIED is preferred when the dice math is interesting and the narrative is short.

**Anti-pattern (do not do):** two rows — one for the dice roll, one for the System effect. The second row has `0+0` dice and reads as a duplicate. Visible bug shipped via the Coordinate flow until 2026-05-10.

**Trim pattern for every new outcome value:**
1. Pick whether the row is Tier A (bespoke banner with its own JSX in `RollsFeed.tsx`) or Tier B (narrative trim via `compactRollSummary`).
2. Tier B: add a branch in `lib/roll-helpers.ts:compactRollSummary` that matches your label / outcome and returns the trimmed sentence. Return `null` falls through to the verbose dice card — only acceptable when the row genuinely has no compact form.
3. Hide the `▸` pip on rows with `die1 = die2 = 0` (auto — handled by `hasRealDice` in `RollEntryCard`). If the row has no real dice, design the narrative to stand on its own.
4. Banned in copy: `(critical)` (use 'Moment of Insight as to why it went so well'), em-dash `—`, en-dash `–`, capital `CDP` (use 'Character Development Points').
5. Wild/HI global tag: ` and has a Moment of Insight as to why it went so well`.
6. LI global tag: ` and has a Moment of Insight as to why it went so badly`.
7. Per-check bespoke overrides (Unarmed HI, Stabilize HI, First Impression HI/LI, Group Check LI) live alongside the general pattern and replace the global tag wholesale.

## Rules System — full workflow

**Read [`tasks/rules-system-workflow.md`](rules-system-workflow.md) first.** It's the canonical doc for everything below. Rules below are supplementary lessons learned during audits.

## CRB / Rules Audit Workflow (2026-05-09)
- **Audit precedence is immutable**: Tapestry canon (`tasks/tapestry-rules-canon.md`) > Quickstart > SRD > CRB. Anything in a lower source that contradicts a higher source must be flagged for rewrite, not preserved. Canon is the live platform code (`lib/xse-schema.ts` + `app/rules/*`).
- **Audit findings get split four ways**: 🔴 CONTRADICTION (direct disagreement, must fix), 🟡 STALE (canon revised since, update to match), 🟢 CRB-UNIQUE (not in canon, no conflict — user decides keep/drop/promote), ⚪ TYPO/GRAMMAR (deferred, batched at end). The four-tag system maps cleanly onto how the user wants to act on each.
- **Chapter-by-chapter pacing beats whole-book passes**: a single CRB-sized PDF (~195 pages) won't fit in one response. Process by user-specified page range, append findings to a running log file (`tasks/froms-tos-crb.md`), summarize per-chapter counts in chat. The user picks the page ranges; don't auto-advance.
- **CRB-unique content gets sorted into 4 tiers, not just kept/dropped**: Tier 1 = strong canon promotion (fills real gap), Tier 2 = setting-supplement (Distemper-specific, not core canon), Tier 3 = optional GM-aid (playtest first), Tier 4 = drop. The four-tier sort is what feeds the roadmap.
- **Two-document export pattern after a long audit**: long-term canon-promotion strategy lives in `tasks/roadmap.md` (new file), concrete next-action items get prepended to `tasks/todo.md` flagged sections. Roadmap and todo answer different questions ("what should canon look like" vs "what's queued") — keep them separate but cross-link.
- **Substitution-pass workflow**: after the audit produces the FROM/TO blocks, the rewrite phase is mechanical — receive a chapter txt (or pdftotext extract), apply only the canon-mismatch substitutions from the audit, leave grammar/spelling alone, write the corrected text to a parallel file. User explicit: "this is NOT about spelling or grammar, just about replacing what's old with what's new." Don't expand scope.

## Promote-to-canon flow + corrected docx export (2026-05-09 follow-up)
- **Always regenerate canon BEFORE auditing**: `npx tsx scripts/export-canon.ts > tasks/tapestry-rules-canon.md`. Skipping this means the audit runs against stale canon and findings will be wrong. Build it into every rules-audit todo as step zero.
- **Promote-to-canon = full code change + recompile, not just markdown**: when a 🟢 CRB-unique mechanic gets promoted, update `lib/xse-schema.ts` if it's data, OR add/update a page under `app/rules/*` if it's prose, AND update `scripts/export-canon.ts` if the new content needs explicit prose handling beyond what the schema auto-flows. Then regen canon.md, run `npx tsc --noEmit` (after `rm -rf .next` to clear the stale validator), commit as a single `feat(rules): promote N CRB-unique mechanics into canon`.
- **New top-level rules pages need nav wiring too**: when adding a page under `app/rules/X/Y/page.tsx`, also add the anchor to `lib/rules/sections.ts` `RULE_SECTIONS[].anchors`. Otherwise the page exists but doesn't appear in the left-rail nav.
- **`pdftotext` breaks on the SRD font**: PDF was authored with a custom font where lowercase letter glyphs aren't unicode-mapped. `pdftotext` strips most lowercase chars. Use PyMuPDF (`fitz`) instead — installed at the user's Python 3.13 path. For docx, use `python-docx`. For Quickstart-export PDFs, regular `pdftotext -layout` works fine.
- **Corrected-doc output goes to two places**: working copy at `tasks/_work/<doc>-CORRECTED.txt` for repo tracking, plus a sibling copy next to the original docx in the user's OneDrive (e.g. `C:\Users\tony_\OneDrive\Distemper\01. Rules System\01. Core Rules\<doc>-CORRECTED.txt`). The OneDrive copy is what the user actually opens.
- **The script-driven docx-edit pattern is reusable**: `tasks/_work/apply_<doc>_edits.py` opens the docx via `python-docx`, walks paragraphs, applies a mix of (a) whole-paragraph replacements via `(needle_substring, replacement)` pairs (first-match-wins), (b) substring replacements applied to every paragraph, and (c) paragraph insertions at named anchor points. Keeps the logic explicit and idempotent. Reuse this pattern for any future docx-CRB-edit task.
- **Reverse-audit beats forward-audit when the doc looks already-aligned**: instead of "audit X against canon", ask "does X already match canon, and what gaps exist?". Same severity tagging applies. Reverse-audit on the CRB v0.9.3 (Claude Edit) found only 18 issues vs the Quickstart v0.1.01's 50 — useful signal that a doc has already had a canon-alignment pass.

## Database & Auth
- **Role is now normalized to lowercase**: DB trigger `trg_normalize_role` auto-lowercases `profiles.role` on insert/update. All existing rows backfilled. Always compare against `'thriver'` / `'survivor'` (lowercase). No more `.toLowerCase()` needed — but harmless if left in.
- **Supabase email-confirmation links use `verifyOtp`, not `exchangeCodeForSession`**: confirmation emails carry `?token_hash=<...>&type=signup`, not `?code=<...>`. The latter is for OAuth/PKCE. A `/auth/callback` route built only for `code=` silently fails on every signup-confirmation click. Branch on which params are present and call the right API. Cost: an end-to-end signup test on 2026-05-08C produced "Invalid login credentials" because email never got marked confirmed.
- **Postgres grants `EXECUTE` to `PUBLIC` by default on every new function**: writing `GRANT EXECUTE ON FUNCTION foo() TO authenticated` is a no-op for security purposes — `anon` already had it via PUBLIC. To restrict: `REVOKE EXECUTE ... FROM PUBLIC, anon` first, then GRANT. Surfaced 2026-05-08C when Supabase linter flagged `admin_users_with_login` as anon-callable despite the grant.
- **Trigger functions don't need RPC EXECUTE permissions**: triggers fire from DB events and run as the function owner. Revoking EXECUTE FROM `public, anon, authenticated` on every trigger function in `public` is safe defense-in-depth and clears ~30 linter warnings in one shot. See `sql/security-hardening-2026-05-08.sql` Tier 2.2.
- **FK to `auth.users` defaults to `NO ACTION` if you don't specify ON DELETE**: meaning every `DELETE FROM auth.users` errors with FK violations on un-cascaded child tables. The whole DELETE rolls back atomically — partial deletes don't happen. Fix: audit every FK to `auth.users`, set ON DELETE CASCADE for owned data, ON DELETE SET NULL for community-contributed content (where row should persist with attribution cleared). See `sql/fix-auth-cascades-2026-05-08.sql` for the canonical 5-table fix.
- **`information_schema.constraint_column_usage` can hide cross-schema FKs**: the view sometimes doesn't surface FKs from `public.X` to `auth.users` even when they exist. For reliable FK introspection, query `pg_catalog.pg_constraint` directly (`pg_class`, `pg_namespace` joins). Burned ~10 minutes during the cascade audit.
- **Wix DNS doesn't support MX records on subdomains**: even when MX on root works fine. Resend's verification needs an MX on the sending subdomain (`send.example.com`) and Wix returns "this isn't supported" silently. Plan B: migrate DNS to Cloudflare (free, ~30 min). Cloudflare auto-imports existing Wix records before the swap; site stays up if all subdomains were registered (gotcha: Cloudflare's auto-scan can miss `thetapestry.distemperverse.com` style Vercel subdomains — verify the imported list before changing nameservers at the registrar).
- **RLS blocks everything by default**: When creating new tables, always add RLS policies immediately. Storage buckets need separate policies on `storage.objects` — the bucket existing is not enough.
- **Supabase Storage buckets must be created manually**: They don't auto-create from code. Each bucket needs INSERT/SELECT/DELETE policies on `storage.objects` filtered by `bucket_id`.
- **Column must exist before code references it**: Always provide the ALTER TABLE SQL alongside the commit. Don't assume the user has run previous SQL.
- **Every ALTER TABLE migration must end with `NOTIFY pgrst, 'reload schema';`**: PostgREST caches its schema introspection. If the cache predates the column, the API silently strips that column from write payloads — direct DB queries succeed, client updates "revert" on next read. Cost a debug session on 2026-05-04 (pin sort_order revert). Idempotent and cheap; add it unconditionally to every column-add migration.
- **Always `.select()` after `.update()` and check the error**: Fire-and-forget updates with optimistic local state hide RLS rejections, schema-cache misses, and constraint violations. Pattern: `const { data, error } = await supabase.from(t).update(u).eq('id', id).select().maybeSingle()`. If `error` or `!data`, alert and abort — don't repaint.

- **Null-coalescing chains can create false positives**: `npc.wp_current ?? npc.wp_max ?? 10` evaluates to `0` (and filters as "dead") when both fields are null. Only filter entities as dead when their health field has been explicitly set to 0, not when it's null/uninitialized. Use `npc.wp_current != null && npc.wp_current <= 0` instead.
- **Realtime subscriptions must match the events you need**: Subscribing to `INSERT` only means `DELETE` events are invisible to other clients. If a feature clears data (like session end deleting logs), use `event: '*'` so all clients see the change.

## React & Next.js
- **Emoji icons don't respond to CSS `color`**: Emojis render as images. Use SVG icons if you need color to change dynamically (e.g., notification bell).
- **`await` your Supabase calls**: Fire-and-forget (`supabase.from(...).update(...)` without `await`) causes race conditions. The Realtime subscription can fire before the write completes, reading stale data.
- **Optimistic state updates prevent stale reads**: When updating a value (like insight dice), update the local state immediately in addition to the DB write. Don't rely solely on Realtime to propagate changes.
- **Layout flash on conditional rendering**: If a layout element (sidebar) depends on async state, the page will flash/jump when that state resolves. Fix by either always showing or always hiding the element on that route, not conditionally based on async data.
- **`return null` during loading causes layout shifts**: Use `return <div style={{ background: '#0f0f0f' }} />` instead to maintain the DOM structure.

## Tactical Map
- **Tokens always spawn at top-left (1,1)**: All tokens (PC, NPC, objects) spawn at grid position (1, 1) — top-left of the tactical map. Never top-right. The GM controls strip is on the left but tokens go there anyway — the GM drags them to position. This was changed back and forth; top-left is the final decision.

## Client-Side Image Processing
- **Canvas center-crop pattern**: `const srcSize = Math.min(w, h); sx = (w - srcSize) / 2; sy = (h - srcSize) / 2` then `ctx.drawImage(img, sx, sy, srcSize, srcSize, 0, 0, OUTPUT, OUTPUT)` — crops to square from center before resizing. Set `ctx.imageSmoothingQuality = 'high'` for the downscale.
- **Quality slider live-update**: Keep the loaded `HTMLImageElement` in a ref so changing quality can re-run `processImage` without re-reading the file. A `useEffect([quality])` triggers re-processing only when quality changes.
- **File input re-selection**: After `onChange`, reset `e.target.value = ''` so the user can pick the same file again (e.g., after resetting state).
- **`<input type="file">` click-to-browse on drop zone**: Hide the input with `display: none`, use a ref, and call `fileInputRef.current?.click()` from the drop zone's `onClick`. Clicking anywhere in the zone opens the picker.
- **`canvas.toBlob` vs `canvas.toDataURL`**: `toBlob` is async and gives you the exact file size for display; `toDataURL` is synchronous and gives a string suitable for `<img src>` and `<a download>`. Use both in parallel when you need both size stats and a preview URL.

## Styling
- **Banned combo: `fontSize: '12px'` + `color: '#3a3a3a'`**: Use `13px` + `#cce0f5` instead. The pair is illegible on the dark theme even though each value passes its own guardrail. Applies site-wide — `node scripts/check-font-sizes.mjs` flags violations. Rule recorded in `AGENTS.md` under UI conventions.
- **Header buttons must all be the same size**: The campaign table header bar uses the `hdrBtn()` helper for uniform 28px height Carlito buttons (font size enforced by AGENTS.md inline-min-font rule, currently 13px). When adding new buttons to this bar, ALWAYS use `hdrBtn()` — never inline custom styles. This has been broken and fixed before. Check `hdrBtn` usage before adding any button to the header.
- **Carlito > Barlow Condensed for UI labels** (2026-04-29): Playtesters reported Barlow Condensed at 13px (the project minimum inline font-size) was too narrow to read on chips, badges, and small buttons. Whole-codebase swap to `'Carlito, sans-serif'` (open-source Calibri-equivalent webfont, loaded via Google Fonts) — 1,583 inline references across 91 files. Default new UI to `Carlito`; do not introduce new `Barlow Condensed` styles. Body text still uses regular `Barlow`. The sed swept the obvious inline form plus Leaflet popup HTML strings, canvas `ctx.font`, double-quoted variants, and escaped-quote innerHTML — if a new feature reintroduces `Barlow Condensed` via any of those forms, the legibility regression returns silently.

- **`#5a5550` is too dim**: This color was used as secondary text throughout the app but is nearly invisible on dark backgrounds. Replaced globally with `#cce0f5` (light blue).
- **Minimum font size is 13px**: Never use font sizes below 13px anywhere in the UI. The dark background makes small text unreadable. Badges/tags: 13-14px. Body text: 14-15px. Headings scale up from there. Multiple rounds of +2px bumps were needed across roll feed, session history, and NPC roster because initial sizes were too small.
- **`appearance: 'none'` on selects**: Required for consistent cross-browser styling of dropdowns.
- **Grid needs enough container width**: `auto-fill` grids won't show multiple columns if the parent container is too narrow (e.g., constrained by sidebar). Use fixed column count (`repeat(5, 1fr)`) when you know the layout.

## Architecture
- **Extract large features into components**: The table page was getting huge. NpcRoster, NotificationBell, and VisitLogger were extracted as separate components to keep the main page manageable.
- **Store structured data, serialize for display**: Skills are stored as `{ entries: [...], text: "..." }` — structured for programmatic use, text for backwards compatibility and display.
- **`SECURITY DEFINER` on trigger functions**: Required for triggers that insert into tables with RLS (like notifications), since triggers run as the invoking user who may not have insert permissions.

## Conventions

- **Architectural fitness functions = ratchets, not staged-file scans (2026-05-22, grand re-arch Phase 1c).** The existing `check-*.mjs` guardrails scan STAGED files for a banned pattern. Architecture conformance is different: scan the WHOLE repo, compare to a committed baseline (`tasks/_baselines/arch.json`), and fail ONLY on regression (a number moving the wrong way). `scripts/check-arch.mjs` does three: LOC ceilings (god-components shrink only), seam leakage (`.from(` outside `lib/data/**`, `.channel(` outside `lib/realtime/**`), prod console (`console.log|warn` in app/components/lib). Re-baseline DOWN with `--save` after an improvement; `--save` refuses to RAISE a number without `--force`. This turns "migrate 1,324 call sites" from an aspiration into a CI-enforced monotonic decrease. Verify the FAIL path when you build a ratchet (lower a baseline by 1, confirm exit 1) - a ratchet that can't block is theatre.
- **Type a seam LOCALLY to avoid a big-bang global typing migration (2026-05-22, Phase 1b).** `lib/supabase-browser.ts`'s client is NOT parameterised with `<Database>`, so all 1,324 inline `.from()` return `any`. Parameterising it globally would surface tsc errors across 98 files at once - not a "pure addition." Instead `lib/data/db.ts` casts the client to `SupabaseClient<Database>` LOCALLY, so repositories get full typing while the rest of the app is untouched until it migrates. Remove the local cast + parameterise globally only once every consumer is on the seam.
- **Mechanical `.from` migration: only UNIFORM operations are cleanly mechanical (2026-05-22, Phase 3).** A repo migration is a safe behavior-preserving string-replace ONLY when many call sites share ONE query shape: build a named repo fn returning the typed `{data,error}` builder (e.g. `getCampaignNpcs(id)` = `.from('campaign_npcs').select('*').eq('campaign_id',id)`, `insertRollLog(rows)`), then prefix-replace `supabase.from('X').op(` -> `repoFn(` so the trailing args + chain (`.eq`, `.select`, `.order`, Promise.all element) are preserved untouched. This took table-page + repo-wide leakage 1324->1266 with zero behavior change. What does NOT migrate cleanly: (1) VARIED writes - `.update(patch).eq('id',x)` with a different patch per site needs per-site arg-reordering (transcription-risky) OR a thin accessor that doesn't really encapsulate (ratchet-gaming); (2) trunk-table queries (initiative_order/character_states/scene_tokens) - migrating them now then rebuilding into useInitiative/useTacticalSync/useRollResolution is double-handling, so they migrate WITH their hook. Writes use a loose payload param (`rows: any`) during migration - the typed-payload tightening is a separate pass (one roll_log site stuffs a DamageResult into a Json column; strict typing surfaced it - logged, deferred to the rebuild). Don't force every `.from` onto a thin wrapper just to drop the ratchet number; that's churn the hook rebuild redoes.
- **Stale-closure-safe realtime hook: deps `[campaignId]` + handlers in a ref (2026-05-22, Phase 1a).** The recurring "one client stops seeing the other" bug comes from channel effects whose dep array includes state/setters, so the channel tears down + resubscribes on unrelated renders. `useCampaignChannel` fixes it structurally: the effect deps are `[campaignId]` ONLY (subscribe once per campaign), and the subscribed callbacks read the latest handler from a ref that's mirrored every render - so you get current closures over current state WITHOUT resubscribing. Plus every handler is auto-wrapped with the Sentry guard (100% coverage vs the old opt-in). This is the pattern every realtime consumer migrates onto in Phase 3.

- **Terminology: the GM has no PCs (locked 2026-05-22).** A **PC** is a Player Character (player-controlled). An **NPC** is a Non-Player Character (GM-controlled). The GM controls NPCs only. NEVER write "GM's PC" or "GM PC" in docs, UI copy, or comments - it's a category error; the GM-controlled combatant is an NPC. Caveat: "**non-GM PC**" is CORRECT and stays - it means a PC controlled by a non-GM user (a player's character), used in RLS/permission contexts.
- **"new window time?" = handoff prompt**: When the user asks `new window time?`, respond with a concise, self-contained prompt they can paste into a new chat to resume. Shape: `I want to <task>. Load <relevant spec files> first.` Should reference the project-surviving files (tasks/todo.md, tasks/spec-*.md, tasks/rules-extract-*.md, tasks/testplan.md, tasks/lessons.md) so fresh-me has everything needed without re-litigating the last session. Example: `I want to build Phase C Communities — weekly Morale Check, Fed/Clothed resource checks, Activity Blocks. Load tasks/spec-communities.md and tasks/rules-extract-communities.md first.` Don't include stuff only meaningful in the current session (recent commit hashes, "we just shipped X"); stick to things fresh-me can actually reload.

## Process
- **Always provide SQL upfront**: When a feature requires a DB schema change, provide the ALTER TABLE / CREATE TABLE SQL immediately in the same message — don't wait for the user to ask. The user shouldn't have to chase for it. This is explicitly required in CLAUDE.md: "Always provide the ALTER TABLE SQL alongside the commit."

- **Provide all pending SQL in order**: When multiple features add columns/tables, the user may not have run earlier SQL. Always check and provide the full chain.
- **Debug logging is essential for remote debugging**: When the user can't see what's happening, add `console.log` with prefixed tags like `[EndSession]`, `[StatUpdate]`, then remove after diagnosis.
- **Test on the actual deployment**: localhost behavior can differ from deployed behavior (caching, env vars, auth state).
- **Verify the DB table/column exists before assuming code works**: A 400 error or silent null often means the schema doesn't match the code.
- **Supabase auth triggers must use EXCEPTION handler**: If a trigger on `auth.users` fails, Supabase rolls back the entire transaction including user creation, showing "Database error saving new user". Always wrap trigger INSERT in EXCEPTION WHEN OTHERS to prevent blocking signup. The client-side fallback code handles profile creation if the trigger fails.
- **CHECK constraints vs trigger values**: The `handle_new_user` trigger was inserting `'survivor'` (lowercase) but `profiles.role` has a CHECK constraint requiring `'Survivor'` (capitalized). Always match exact casing in trigger functions.

## 2026-04-11 session

- **Silent Supabase UPDATE failures under RLS are the #1 "feature looks broken but code looks fine" cause**: Supabase `.update()` returns `{ error: {...} }` without throwing. `console.warn('[tag] update error:', error.message)` is easy to lose in the noise. For writes that MUST land (combat state, action consumption, turn advance), check the `error` return AND think about whose client is making the call — if the write path runs on the player's client, RLS policies that allow only GM writes will silently drop every update. Nana bug: player attacks fired, roll logs landed, but `initiative_order.actions_remaining` never decremented because the player's client couldn't UPDATE the table. The symptom looks identical to "consumeAction isn't being called" but it's actually "consumeAction IS called, the UPDATE returns an error, we logged it once and moved on".

- **Pseudo-weapons sneak past `getWeaponByName` inference**: `Unarmed` (and anything else fabricated inline in the UI with `{ weaponName, damage, rpPercent }`) has no entry in `MELEE_WEAPONS` / `RANGED_WEAPONS`, so `getWeaponByName('Unarmed')` returns `undefined` and `isMelee = w?.category === 'melee'` falls through to `false`. That cascades: `rollDamage(..., !isMelee)` skips PHY AMod, and mitigation uses DEX instead of PHY. Fix the *inference*, not the damage pipeline — `isMelee = w?.category === 'melee' || weapon.weaponName === 'Unarmed'`. There was already a precedent at line 1481 special-casing `weaponName === 'Unarmed'` for Low-Insight weapon jams, so this has been stepped on before.

- **Label parsing is not a reliable way to identify the attacker**: `pendingRoll.label.split(' — ')[0]` works for NPC attacks (`Gio — Attack (Pistol)`) and consume-action logs (`David — Aim`), but breaks for PC weapon labels (`Attack — Crossbow`) where the first token is literally the word "Attack". Two separate bugs landed in the same week from this: (1) `roll_log.character_name` got the string `"Attack"` written into it, and (2) attacker-identity gating in `handleRollRequest` couldn't distinguish a known-name prefix from a generic action prefix. **Only trust `labelParts[0]` when it actually matches a known PC or NPC name** (check `entries` and `campaignNpcs`); otherwise fall back to `syncedSelectedEntry → myEntry`.

- **GM authority is the wrong escape hatch for turn-order gates**: The first cut of the combat gate let GMs bypass "is it this combatant's turn?" via `if (!iAmActive && !isGM) reject`. That let the GM click Attack on an off-turn NPC's NpcCard; the modal opened, the roll fired, `closeRollModal` then fetched `is_active = true` from the DB, which at that point was a *different* combatant, and consumed *their* action. The fix is to identify the attacker from label/context (NPC name from label, PC name from open sheet / current user) and require `attacker === active` *without* a GM override. GM still works via the `selectedEntry` branch (GM has the active PC's sheet open → attacker = that PC).

- **Postgres_changes on npc_relationships is flaky**: Subscribing to `postgres_changes` on `npc_relationships` with `event: '*'` does not reliably deliver updates to player clients — most likely the table isn't in the `supabase_realtime` publication, or RLS blocks the payload from reaching the player's channel. When the GM reveals an NPC mid-combat, the player's reveal list didn't refresh until a full page reload. Fix: send a `broadcast` event on a channel the player is already subscribed to (the `initiative_${id}` channel is a good shared bus), and have the handler refetch `campaign_npcs` fresh before calling `loadRevealedNpcs`. Broadcast events bypass RLS and publication settings entirely — they're peer-to-peer on the channel.
- **SECURITY DEFINER doesn't always bypass RLS in Supabase**: Despite documentation, trigger functions with SECURITY DEFINER may still be blocked by RLS policies. Add an EXCEPTION handler as a safety net.

## 2026-04-12 session

- **Cross-client state updates require broadcast, not just setState**: When a player deals damage to an NPC, `setCampaignNpcs`/`setViewingNpcs` only update the player's own React state. The GM's browser is a separate client and never sees the change unless Supabase Realtime fires (unreliable) or a broadcast event is sent. Fix: broadcast `npc_damaged` through the initiative channel (same pattern as `turn_changed`/`combat_ended`) with the patch payload. All clients listen and apply the patch locally. This is why NPC→PC damage worked (GM rolls, GM's state updates) but PC→NPC didn't (player rolls, GM's state stale).

- **Header bar buttons/badges must ALL use the `hdrBtn()` helper**: Every button, badge, link, and select in the table header bar must use `hdrBtn(bg, color, border)` for uniform sizing (28px height, 14px horizontal padding, 70px min-width, 11px Barlow Condensed, inline-flex centering). No inline style overrides for sizing. The helper is defined at the bottom of `page.tsx`. This includes `<select>`, `<a>`, and `<div>` badges — not just `<button>`. If adding new controls to the header, use `hdrBtn()` and nothing else for the base style.

- **Tactical map GM strip buttons must be uniform**: All buttons, labels, and selects in the TacticalMap left strip must be 28px height, 100% width, 13px Barlow Condensed, uppercase, text centered both horizontally and vertically (`display: flex, alignItems: center, justifyContent: center`). Use `boxSizing: border-box`. No exceptions.

## 2026-05-01 session

- **Check for the prevailing column-name convention before naming a new ordering column**: Adding a "drag-to-reorder" feature to GM Notes, I picked `display_order` for the new column. A parallel agent shipped the same feature in the same window using `sort_order` — which is the convention every other ordering column in the repo already uses (`timeline-sort-order.sql`, `scene-tokens-sort-order.sql`, `modules-sort-order.sql`, `campaign-sort-order.sql`). One `ls sql/ | grep sort` would have caught it. Cost: a 3-conflict merge resolution and a rename across the component + alert + migration. Before naming any new column for sequencing/ordering/positioning, grep `sql/` for the existing pattern.

- **`position: sticky` is unreliable when there's already a scrolling ancestor**: I shipped `RulesNav` with `position: sticky; top: 0` thinking it would pin to the top of the rules layout. It scrolled out of view on long pages because LayoutShell's children-wrapper (`overflow: auto`) is the actual scroll context, and stacking another sticky inside that doesn't engage cleanly under flexbox. The reliable fix for "fixed sidebar + scrolling content" is **make the content column the only `overflow-y: auto`**, leave the sidebar as a normal flex item, and constrain the parent with `overflow: hidden`. Bonus: IntersectionObserver `root` must be set to that scrolling element (default = viewport, which now doesn't move).

- **Hash-link scroll fails when `<main>` is the scroll container**: Browsers' native "scroll to #anchor on load" targets the document, not custom scroll containers. With `<main>` doing the scrolling, `/rules/communities#apprentices` lands at the page top. Fix: `useEffect` that reads `window.location.hash` after hydration and calls `target.scrollIntoView()` — also wired to `hashchange` events for in-page anchor clicks. Combined with `scrollMarginTop` on the heading, anchors land cleanly.

- **Comparison tests deserve duplicated content, not premature SSOT**: For the Style A vs Style B test of `/rules/communities` (one long page) vs `/rules/communities2` (5 short pages), my first instinct was to extract the content into shared components both could compose. That's a 6-file refactor that I'd unwind once one style is picked. Pragmatic move: write Style B with its own copy of the SRD content, ship the comparison fast, delete the loser in one commit. Duplication is bounded (only while the test runs) and it keeps the comparison's blast radius small.

## 2026-05-15 session (afternoon)

- **Multi-cell token math: position equality checks are wrong for any token with grid_w/grid_h > 1**: `handleMouseUp` in `components/TacticalMap.tsx` had a `moved = pos.gx !== tok.grid_x || pos.gy !== tok.grid_y` check that treats the token's top-left cell as "stayed put" and every OTHER cell of the footprint as "moved." For 1-cell tokens the math is correct; for multi-cell vehicles (Minnie at grid_x=10, grid_w=5) any click inside the footprint that isn't the top-left snapped her to wherever the cursor landed. Replaced with a footprint-overlap test `pos.gx ∈ [tok.grid_x, tok.grid_x + grid_w)` on both axes. **Rule:** anywhere the code compares a single cell `(pos.gx, pos.gy)` against a token's anchor `(tok.grid_x, tok.grid_y)`, ask whether the token can be multi-cell. If yes, use footprint-overlap, not point equality. Likely same class of bug lurks in `handleMouseDown`'s drag-anchor computation (the cursor->top-left offset is in PIXELS, but the commit at L3201 ignores it — so dragging Minnie by her center cell lands her top-left where the cursor ends, shifting her +N cells extra). Fix `ae1a2a2`. Cost: most of an afternoon between the recording, the agent confusion on "is it the passenger sync?", and the trace through both move paths.

- **Playtest-trace warnings can be red herrings**: the recorded JSON showed `[playtest-trace] [TacticalMap] plain mousedown fell-through to pan branch` and I latched onto it as the smoking gun. The trace only fires when mousedown lands on EMPTY space, not on a token — so it was true but unrelated. The actual bug was in `handleMouseUp`, not in the mousedown path the trace flagged. **Rule:** when a playtest trace points at a handler, verify the trace actually corresponds to the symptom's mechanism (in this case "click on Minnie shifts her position") before concluding. A single matching keyword is not a diagnosis.

- **The bug class I was sure WAS the bug (passenger sync gap at click-to-move) is still a real bug, but separate**: the passenger-sync code at `TacticalMap.tsx:3209-3268` runs only in the drag-end handler, not the click-to-move at L2814. That's still open — but the click-shift bug Xero recorded is upstream of and unrelated to passenger sync. **Rule:** when the user says "it felt buggier than that," do NOT assume your diagnosis is one bug — there may be multiple. Acknowledge each as separate, fix the one in front of you, leave the others tagged.

## 2026-05-14 session

- **Multi-surface widening sweeps: triage before mass-editing**: The Thriver godmode UI sweep finished today (commit `07652f8`). 257 `isGM` references across 23 files looked overwhelming until the actual triage showed most were already widened by earlier commits (`92f9243`, `bea860a`, `ae0933a`). The remaining surface was 4 files. **Rule:** for a "widen X across the codebase" sweep, the first move is `grep | wc -l` and then per-file inspection — NOT counting hits. Counted hits inflate scope by 4-10x; what matters is which sites still need widening, which were already done by prior partial sweeps, and which are identity-only and stay strict. Same applies to any sweep of the shape "replace pattern A with pattern B" (rarity colors, role literals, font sizes etc) — grep first, classify each hit, then edit.

- **Per-surface `isGM` vs `gmLike` cheat-sheet**: When deciding whether to widen any given `isGM` site, the rule is:
  - **Widen to `gmLike`**: edit/visibility/action gates (handlers that early-return for non-GM, prop passes to child components, filters that hide non-self content, button-render conditionals)
  - **Keep strict `isGM`**: identity labels ("GM View" / "Player View" header, "(GM)" username badge), telemetry log fields, identity-locked tools (recorder toggle is `isThriver`-only by design — even GMs don't see it unless they're also Thrivers)
  - The grep search `\bisGM\b` returns both — so the call-by-call inspection is real work, not a regex-replace. ~30 internal gates in the table page + 7 prop-passes + 11 header bar buttons + 3 standalone pages == ~50 individual decisions.

- **Confirm spec for ambiguous widening before mass-editing**: Before this sweep I flagged three judgment calls to Xero: (1) `entries.filter(e => isGM || e.userId === userId)` widening means visiting Thrivers see every PC sheet and every player's rolls; (2) `onKick` widening means Thrivers can kick players from initiative; (3) the `<CampaignSnapshots isGM={true}>` hard-code. Got explicit confirmation on (1) and (2) and a clear "your call" on (3) — let me ship without revisiting. **Rule:** any widening that changes per-user visibility or grants destructive affordances (kick, remove, delete) needs explicit owner confirmation, not just "DB allows it." Flag the three, batch them in one question, then act.

## 2026-05-06 session

- **Sequence-guard pattern belongs in EVERY refetch hook, not just `loadEntries`**: `useRollsFeed.refetch` and `useChatPanel.refetch` had no defense against out-of-order completion — a slow earlier query landing AFTER a fast later one silently clobbered fresher state with stale data. The table-page's `loadEntries` has had `refetchSeqRef` since 2026-04-26; the rolls/chat hooks were left behind. Fix is mechanical: bump `++refetchSeqRef.current` at call site, capture the local seq, then `if (seq !== refetchSeqRef.current) return` after the await before any setState. **Rule:** any hook that exposes a `refetch()` and is called from realtime subscriptions or back-and-forth user input needs this guard. Symptom is hard to reproduce (only shows up under tight bursts) but easy to write — port the pattern eagerly when you spot a refetch without it. Commit `d4a97e1`.

- **`isGM &&` UI gates need to widen to `gmLike = isGM || isThriver` whenever RLS already grants Thriver write access**: The DB layer shipped `sql/thriver-godmode-policies.sql` weeks ago; Thrivers can write to every campaign at the RLS level. But `isGM &&` checks throughout the table page were excluding Thrivers from the matching edit affordances — RLS allowed the write, the UI didn't surface the button. Fix: derive `const gmLike = isGM || isThriver` once at the top of the component and route it through every `isGM={...}` prop pass + `gmTab === 'foo' && isGM` parent gate + `!isGM` player-branch fallback. Keep strict `isGM` only on identity-labeled surfaces ("GM View" button, "GM Tools" menu name) — Thrivers should still see themselves as themselves there, not impersonate the campaign GM. The DB-RLS / UI-gate parity is the actual bug class; check both layers when adding a new gated control. Commit `92f9243`.

- **`canEdit={true}` on a permission-gated component is a wide-open bug**: `VehicleCard` shipped with `canEdit={true}` hard-coded — every campaign member could edit shared vehicles regardless of role. Caught only because the godmode sweep audit walked every `isGM=` prop pass and noticed VehicleCard wasn't gated at all. **Rule:** when adding a new permission prop (`canEdit`, `canDelete`, `gmOnly`, etc.) to a component, default it to `false` at the component level and require the caller to widen explicitly with `gmLike` / `isOwner` / etc. Never default to `true`. When auditing existing components for a permission sweep, grep for `={true}` on the prop name first — it's the single fastest find for "the gate was never wired."

- **Inline editable surfaces work best as a small set of co-located helper components**: The GM Notes popout `c4610ad` made every field click-to-edit by introducing four helpers — `EditableText` (single-line + multiline, blur or Enter / Cmd+Enter to commit, Esc to cancel), `EditableSelect` (chip → dropdown), `EditableToggle` (boolean chip), `EditableNumber` (sort_order). Each does optimistic local update + rollback on `.update().select()` error so RLS rejections surface as alerts rather than silently dropping. Co-locating them at the bottom of the page file (rather than extracting to `components/`) kept the diff tight (~480 lines) and avoided the abstraction tax of "where does this live, what's the API surface" — they're shape-specific to this popout. **Rule:** when retrofitting click-to-edit onto a previously-read-only surface, write the helpers inline at first; only extract once a second surface needs them.

## 2026-05-08 session

- **`RETURNS TABLE (col ...)` shadows columns of the same name inside the PL/pgSQL body**: The new `admin_users_with_login()` RPC errored with `column reference "id" is ambiguous` the moment the page tried to call it. Cause: PostgreSQL's `RETURNS TABLE (id uuid, role text, created_at timestamptz, ...)` declares OUT parameters with those names. Any unqualified reference in the body — even in a subquery that doesn't touch the result set — collides with the OUT param and errors out. The `RETURN QUERY SELECT p.id, p.role, ...` was already aliased and fine; the bug was an unqualified Thriver gate `WHERE id = auth.uid() AND lower(role) = 'thriver'` reading from `public.profiles`. Easy trap because the qualified `RETURN QUERY` makes the function *look* safe. **Rule:** inside any PL/pgSQL function with `RETURNS TABLE`, every column reference in the body must be alias-qualified — including IF / EXISTS / subquery gates. Default pattern: alias the body's reads (`profiles caller`, `campaigns c`, etc.) so the OUT-param collision can't sneak back in. Alternative: `#variable_conflict use_column` at the top of the function. Commit `78df30c`.

- **A "missing" UI value is often a CSS-overflow bug, not missing data**: Xero reported "several users emails are still not showing" on `/moderate`. Audit found 15 of 16 profiles had `email`; only one was actually NULL. The other 14 emails were getting ellipsized off-screen behind long usernames because the row layout used `overflow:hidden + textOverflow:ellipsis + whiteSpace:nowrap` on a flex parent that crammed username + email + chips into one line. The "missing" emails were hiding in plain sight. **Rule:** when a user reports missing data on a list/table view, run a `count(*) total, count(field) present, count(*) - count(field) missing` query first to separate UI bugs from data bugs. Two minutes of SQL beats an hour of fixing the wrong layer.

- **"Not getting alerts anymore" is usually a stacked-gate suppression problem**: Visit emails went silent. Cause: `log-visit/index.ts` had two stacked gates — `isFirstVisit` (per session_id) AND `!isRepeatSurvivor` (signed-in users with `visitNumber > 5` were suppressed). Live data showed every active visitor's `ip_hash` count was in the 88–1612 range, so `isRepeatSurvivor=true` on every request → no emails fired anywhere. The suppression was correct in isolation; the *stacking* killed the channel. **Rule:** when an alert pipeline goes silent, check the actual production data against each gate independently — what fraction of recent events would each gate let through? If even one gate has a near-100% suppression rate, that's the dead one. Don't trust intuition about suppression rules drift; check the rows. Also: prefer one binary gate (here: `visitNumber === 1`) over two layered heuristics whenever the user's intent is binary ("only the first visit from a new visitor").

- **Process: write to `tasks/lessons.md` and `tasks/todo.md` IMMEDIATELY — never defer to "next time" or "if you want me to"**: I shipped a fix, mentioned the lesson worth keeping in chat, and signed off with "will add it next chance unless you'd rather I do it now." Xero corrected: "do it now, and WRITE IT DOWN that you should write stuff to LESSONS.MD and TODO.MD immediately, never wait for me to ask you to do it." The CLAUDE.md rule already says "After ANY correction from the user: update tasks/lessons.md with the pattern" — same applies after every meaningful ship, not just corrections. **Rule:** the moment a session produces (a) a correction, (b) a non-obvious bug fix worth remembering, or (c) a feature that should land in the shipped log, update `tasks/lessons.md` and `tasks/todo.md` in the same response. Don't ask permission, don't promise to do it later, don't bundle it into a "want me to..." offer. The user sees that as friction; they want the captures to happen as a reflex.

- **`app/globals.css` forces `select { width: 100% }` site-wide — inline `style={}` will not override it without an explicit `width`**: A `<select>` rendered with `padding/border/font` set inline but no `width` will silently stretch to the parent's full width because `app/globals.css:62` defines `input..., textarea, select { width: 100%; ... }`. React's `style={}` only sets the properties named — properties absent from the style object inherit the cascade, including the global `width: 100%`. Hit on the /moderate user-row redesign 2026-05-08 — the SUSPEND dropdown filled the bottom row, pushing DELETE to its own line. Fix: add `width: 'auto'` (and `flexShrink: 0` if inside a flex parent so it doesn't collapse) to the inline style. **Rule:** whenever you place a `<select>` in the codebase, default to setting `width: 'auto'` (or a fixed value) inline. The global rule was intentional for full-width form fields but is the wrong default for inline action-bar selects.

- **A lone flex item that wraps to its own line fills the full row width even with `width: auto`**: When a flex container uses `flex-wrap: wrap`, an item that ends up as the sole item on a line stretches to `flex-grow: 0, align-self: stretch` — and without explicit width it matches the container. Counterintuitive, because `width: auto` "should" shrink-to-content. The fix is never to let the two things that must stay together be separate flex children; wrap them in an inner `<div style={{ display:'flex', gap:'...', flexShrink:0 }}>` so they're always a single flex item from the parent's perspective. Hit on /moderate: Suspend select + Delete were separate children; when they wrapped they each tried to fill the row.

- **Cloudflare Turnstile Managed mode must NOT call `execute()` on submit**: Turnstile's Managed mode auto-solves the widget invisibly as soon as it renders. Calling `ts.execute(widgetId)` after that conflicts with the Managed solver and hangs indefinitely (the promise never resolves, the button freezes at "Verifying..."). The correct pattern: capture the token in the `callback` into a `useRef`, then read the ref on submit. If the ref is empty (very fast submit before solve finishes), set up a one-time override on the callback ref and call `ts.reset()` to re-trigger auto-solve, with an 8 s timeout. Never call `execute()` when the widget is in `size: 'invisible'` Managed mode.

- **Turnstile fail-open strategy: widget errors pass through, server token rejection is a hard block**: Turnstile widgets can error (error code 400020 = domain not registered, ad blocker, CDN issue). If a widget errors, `cachedTokenRef` is null — blocking on a null token would lock out real users whose browsers happen to have ad blockers or who are on localhost. Instead, track widget errors in a `widgetErroredRef`; when token is null AND widget errored, skip the Turnstile check entirely and fall through to signup (fail open). Honeypot + username consonant-run check are the first-line defense in this case. Only hard-block when the server explicitly returns a non-OK response to a token that was actually returned — that means the token was forged or reused.

- **Cloudflare Turnstile hostname management does not accept wildcards in the UI**: The "Add a custom hostname" field rejects `*.vercel.app` with "Enter a valid domain". Use the parent domain without the asterisk — `vercel.app` covers all subdomains. Register `localhost`, `thetapestry.distemperverse.com`, and `vercel.app` to cover all three environments (local dev, Vercel preview, production). No Vercel redeploy is needed after adding hostnames; Cloudflare propagates within minutes.

- **Leaflet: always call `map.remove()` in the `useEffect` cleanup**: Without it, navigating away from a page that renders a Leaflet map leaves an orphaned instance. When the component remounts (navigate back), `L.map(el)` finds a pre-existing `_leaflet_id` on the DOM element and throws `TypeError: Cannot read properties of undefined (reading '_leaflet_pos')`. Pattern: `return () => { if (mapRef.current) { mapRef.current.remove(); mapRef.current = null } }`. Also: always pass `minZoom` and `maxZoom` to `L.map()` init options — omitting `maxZoom` triggers a "Map has no maxZoom specified" console warning that becomes Sentry noise at scale.

- **CRB-only mechanics still need a platform implementation pass — SRD silence isn't a green flag for "skip"**: Infection sat in the open-work checklist as a "verify if implemented" item for weeks. Per CLAUDE.md precedence (SRD > CRB > QS), if SRD doesn't cover a thing, CRB is canon. Infection is the prime example — entire subsystem in the CRB (p.114-115), zero text in the SRD, and the platform had nothing wired. The audit-style note ("Verify that the Infection subsystem is fully implemented") is *implicitly* a build task disguised as a check. **Rule:** when a checklist item says "audit / verify against the SRD" and the SRD is silent on the topic, escalate to "build it" the moment confirmed; don't keep auditing forever. The Infection extraction (2026-05-09) → rules page → schema → buttons cycle took ~2 hours once we committed to building. Keeping the topic perpetually in "verify first" status is what stalls these subsystems.

- **`git status --short` and `git diff --stat` won't catch an untracked-new-file dependency the same way they catch modified files — verify `git ls-files lib/<new>` before committing migrated consumers**: 2026-05-09 the rarity-color sweep created `lib/rarity-colors.ts` and migrated two consumers (`app/rules/equipment/rations/page.tsx`, `components/wizard/StepEight.tsx`) to import from it. The consumers got committed in `32c8fc0`; the new module sat untracked in the worktree (`??` in status) and was easy to glance past while staging. Local builds passed because the file was on disk. Vercel cloned a fresh copy without the file and threw `Module not found: Can't resolve '../../lib/rarity-colors'` for FIVE consecutive deploys before I traced it. **Rule:** when a commit migrates A to import from B (where B is a new file), explicitly check `git ls-files <B-path>` (returns blank if untracked) before pushing. Or just `git add -A` for sweep-style commits — the bias toward conservatism (only adding named files) hurts when one of the named files isn't yet tracked. Bonus: `git status` shows `??` for untracked files, but my `git add app/... components/...` commands silently skipped them; the staging output never warned that the imported module wasn't included.

- **`tsc --noEmit` is not a Vercel-build oracle — mid-file ESM imports pass the typecheck and still fail Next prod build**: Added an `import { RARITY_COLOR as RARITY_ACCENT_CANONICAL } from '../../lib/rarity-colors'` partway down `components/wizard/StepEight.tsx` (line 56), after a `CATEGORY_ACCENT` constant. `npx tsc --noEmit` returned 0 locally, so I shipped. Vercel's Next 15 production build (commit 32c8fc0) errored on prerender for that file, blocking three deploys end-to-end before I traced the cause. **Rule:** `tsc` hoists imports during semantic checks; it doesn't enforce the ESM "imports must precede non-import statements" rule that Next's prod bundler does enforce. When introducing a new import (especially via in-place editing of a file that already has logic), put it adjacent to the existing imports at the top — never sandwiched between consts. If `tsc --noEmit` is the only build sanity check, also run `npx next build` before the commit lands; the prerender / SSR / module-graph rules are stricter than the type system. Cost on 2026-05-09: three failed deploys (32c8fc0, a0f48ff, then `51c025a` with the hoist fix). Bonus catch: the same pattern existed in `components/InventoryPanel.tsx` and `components/TradeNegotiationModal.tsx` from the in-flight rarity-color work — fixing those preemptively saved the next round of failures.

- **Stun-weapon RP must bypass defensive-mod mitigation**: `lib/damage.ts:calculateDamage` derived `finalRP = floor(finalWP * rpPercent/100)` — RP scaled with mitigated WP. For stun weapons (Taser rpPercent=400, Cattle Prod rpPercent=400), any defensive mod ≥1 collapsed both WP and RP to 0, breaking the entire stun mechanic. Fix: caller passes `{ rpFromRaw: true }` when the weapon has the Stun trait, and `calculateDamage` computes RP from `rawWP` instead of `finalWP`. Canon (Taser = 1 WP / 4 RP per hit) is now pinned in `lib/weapons.ts` header comment + `memory/project_stun_weapon_canon.md`. **Rule:** when a weapon trait inverts the standard "more WP → more RP" relationship (Stun: zero WP, all jolt), the damage pipeline needs the trait-aware branch — never let the trait silently get cancelled by mitigation that's meant to apply to a different damage axis. Same shape would apply to any future trait that decouples damage tracks from each other (e.g. radiation-style "RP only" effects).

- **`(critical failure)` is banned from roll-log narrative copy**: 2026-05-09 smoke-test caught `compactRollSummary` in `lib/roll-helpers.ts:56` appending ` (critical failure)` to every Low-Insight (1+1) action banner — e.g. `X used an Unarmed to Unsuccessfully Charge Y (critical failure)`. The phrase reads as game jargon dropped into a narrative sentence and Xero rejected the wording outright. Replacement: `, but failed miserally` (comma-led clause, no parens). The Wild Success / High Insight `(critical)` tag was left alone — Xero only flagged the failure side. **Rule:** roll-log narrative copy gets a *story sentence*, not a labeled outcome. If you need to surface the *type* of crit, embed it in the sentence (e.g. `... and it goes spectacularly` for Wild) rather than appending a parenthetical mechanics tag. Reference-table surfaces (e.g. `/gm-screen`'s outcome legend) are exempt — those are *defining* the term. Watchout for any future narrative branch that re-derives "(critical failure)" from `r.outcome === 'Low Insight'` instead of using the central `outcomeTag` variable; centralised tag is the single source.

- **Canon-promotion audits should always grep the running code first, not just the rules pages**: The Quickstart canon batch on 2026-05-08D listed "Stabilise duration FIX — `16 - PHY AMod` → `1d6 - PHY AMod`" as a code change. Grep showed the actual game logic at `app/stories/[id]/table/page.tsx:5076` already used `Math.floor(Math.random() * 6) + 1 - phyAmod` (correct) — only the prose in `app/rules/combat/incapacitation/page.tsx:71` still read `16 - PHY AMod`. The audit was right to flag it but the scope was rules-page-only, not engine. **Rule:** for any "fix the canon to match Quickstart" item, run two greps before writing: (1) the rules `/app/rules` pages for the wrong value (the doc), (2) the engine (`app/stories/[id]/table/page.tsx`, `lib/`, `components/`) for the formula (the behavior). Often only one of the two is wrong; quoting both in the testplan keeps the diff honest. Same pattern saved time on Lasting Wounds Table 12 + Item Condition Table 10 — both already shipped at the data layer; the audit's "verify first" disposition was correct.

- **A commit message that contradicts its own diff is a future regression waiting to land**: Commit `29e7f25 fix(tactical): auto-fog outside PC LoS + PC-only vision lift` described GM-painted fog as "force fog on top" — meaning absolute, unaffected by PC LoS. But the diff added `if (rawFog[k] && !visible.has(k)) effective[k] = true` to the painted-fog loop — making painted fog LoS-defeasible (PC LoS clears painted cells). Commit message said "absolute"; code did "defeasible." That contradiction sat dormant for days until 2026-05-04 playtest, when a map drawn into the background image (no authored wall segments) hit it: day-mode unbounded sight covered every cell → visible set covered the whole map → every painted-fog cell got cleared → players saw through GM-painted fog completely. The first attempt to fix it (make painted fog absolute) broke open-window-clears-fog on properly-walled maps. The real fix gated LoS-defeasibility on whether the scene had ANY authored vision blockers (`hasBlockers`). **Rule:** when the commit message and the code disagree about a semantic, the code wins by default — but only one of them is the user's mental model. Resolve which one matches the spec BEFORE the fix lands, not after the playtest report. Specifically: if a commit message says "X is absolute / forced / unconditional" and the code has a guard that violates that, treat it as a TODO in the commit, not a footnote. Either fix the code to match the comment or fix the comment to admit the conditional behavior — never let the gap ship. Also worth: when you ship a fog/visibility fix, dry-run BOTH (a) a no-walls scene and (b) a walls-authored scene before committing. Each catches different failure modes; testing only one made me regress in opposite directions on two consecutive ships.

- **Per-viewer auto-fit state gets clobbered by always-resync handlers**: `TacticalMap`'s bg-image-load effect runs an auto-fit when the scene has no saved `img_scale` (null or default `1`) — each viewer locally sets `imgScale` to fit-to-container width. The value is intentionally **not persisted** (each viewer's container is different size). But `loadScenes()` runs on every `tactical_scenes` UPDATE event (including GM toggling a window) and was unconditionally re-applying `setImgScale(active.img_scale)` for every player. The DB value is `1` (default — auto-fit isn't persisted). So local `0.6` → `1` every GM action. Visible zoom-jump for the player on every wall/door/window toggle. **Rule:** when a piece of state has TWO sources of truth — a DB value and a per-viewer local computation — the re-sync handler must distinguish "DB has a real value the player should adopt" from "DB has the placeholder/default and the local computation is the right answer." For `img_scale`, that meant `if (active.img_scale && active.img_scale !== 1) setImgScale(active.img_scale)` — only adopt non-default values from DB. The same shape applies to any state where local-fit/auto-detect computes a value the DB doesn't store. Sanity-check during code review: search for `loadX()` handlers that fire on broad UPDATE events; for each `setState` inside, ask "is this DB value always the truth, or does some local effect produce a different-but-correct value?" If the latter, gate the setState.

- **Adding a Leaflet tile provider needs an explicit `maxZoom` that matches the provider's native cap**: `TILE_LAYERS` in both `CampaignMap.tsx` and `MapView.tsx` historically passed `maxZoom: 19` to every provider's `L.tileLayer()`. That works for the OSM/CARTO/HOT/Esri family (they all serve to 19). But OpenTopoMap caps at z17. Past z17, the provider returns a placeholder PNG that says "max zoom layer = 17" — Leaflet tiles your viewport with that placeholder if the user zooms past 17. The fix on 2026-05-04 was per-provider `maxZoom` (hard cap: + button greys out, scroll-wheel zoom stops), plus calling `map.setMaxZoom(t.maxZoom)` AND clamping current zoom in `switchLayer` so cross-provider switches don't briefly render placeholder tiles. **Rule:** every tile provider entry MUST have an explicit `maxZoom` field set to the provider's native cap. Don't default to 19 — that's only the OSM-family value. For terrain/topographic providers (OpenTopoMap, Stamen Terrain): typically z17. For OSM-derived street/dark/light/voyager: z19. Satellite (Esri): z19 globally, sometimes lower in remote areas (placeholder is rarer there). Bonus when adding a new provider: visit the provider's docs page for "max zoom" or just visit a tile URL at z19 — if you get a placeholder image instead of a real tile, the provider's cap is lower.

- **Turn auto-advance must cover EVERY path that downs the active combatant, not just the named-target path** (SMOKE-1, 2026-05-21): `executeRoll` had per-target auto-advance in the PC and NPC named-damage branches - "if the damaged target is the active combatant, zero their actions + `nextTurn`." But a grenade thrower standing in their own blast is a SPLASH victim, processed in the separate blast Pass-3 loop, which applied damage with NO initiative handling at all. So an active combatant who self-downed (or any active combatant downed via splash rather than as the primary target) kept `is_active=true` and combat stalled - the GM's only recourse was manual NEXT, which the death cascade could obstruct. **Rule:** when a system has multiple code paths that can apply the same state-changing effect (here: WP/RP damage via named-target vs. blast splash), any *follow-on* logic gated on that effect (auto-advance, death log, stress pip, broadcast) must be wired into EVERY path, or centralized so all paths funnel through it. Grep for the follow-on (`nextTurn`, `death_countdown`, etc.) and confirm each damage-application site reaches it. The blast loop computed `nWP/nRP` per job exactly like the named branch but never asked "was this the active combatant, and did they just go down?" - the fix tracked `activeDownedByBlast` in the loop and fired the same advance after the writes landed. **Watch for double-fire:** the turn also advances at modal-close via `closeRollModal -> consumeAction -> nextTurn`; the in-`executeRoll` advance is safe only because `closeRollModal` re-reads `is_active` and finds the roller no longer active, so it skips the consume. When you add an early advance, verify the later advance path no-ops.

- **A "lead-only -> enriched" two-state feed row should render the SAME chrome in both states, or the user sees a flicker/morph** (SMOKE-2, 2026-05-21): the Coordinated Effort feed entry had two render paths - a plain narrative row while the chain was lead-only (no participants yet), then a bespoke Tier-A banner once `collapseCoordEffortChains` enriched it with participants. The lead rolled, showed as a plain dice row, then "became" a banner when the first participant rolled. Users read the plain row as "nothing happened." **Rule:** when a feed/list entry has an early state and a later enriched state of the SAME underlying event, render both with the same visual chrome (banner stays a banner; only the body text + expand contents differ). Reuse the existing narrative source (here `compactRollSummary`, already the locked single-source-of-truth for the lead-only wording) rather than duplicating strings into the new branch. The fix added a lead-only banner renderer keyed on `label.startsWith('Coordinated Effort - ') && !coordChainParticipants`, sibling to the enriched one.

- **"Friendly fire" / faction predicates must key on the actor's OWN faction, and a null-id filter silently inverts for the other faction** (SMOKE-3, 2026-05-21): the grenade friendly-fire warning built `friendlyCharacterIds = entries.map(e => e.character.id).filter(cid => cid !== activeEntry.character_id)` - "every PC except the attacker." For a PC thrower that's correct. But an NPC's `character_id` is `null`, so the `!== null` filter removed nothing and EVERY PC counted as "friendly" - the GM got warned that an NPC's grenade would hit the PCs, which is the NPC's intent, not friendly fire. Compounding it, the consumer scan did `if (!tok.character_id) continue`, so it only ever considered PC tokens and could never warn NPC-on-NPC. **Two rules:** (1) a faction/ownership predicate must be built from the ACTOR's faction explicitly (`attackerIsNpc ? otherNpcs : otherPcs`), never "everyone minus me" - "minus me" silently becomes "everyone" when the actor's id field is null for that id-space. (2) When you fix one faction's predicate, check the consumer isn't hard-coded to the other faction's id-space; make it symmetric (scan PC tokens against the PC friendly-list AND NPC tokens against the NPC friendly-list, self by either id).

- **Audit false-positive recurring class: `String.prototype.split` with a found needle never returns `undefined` at index 1**: Across three consecutive bug audits, the same false positive surfaced: claim that `pendingRoll.label.split('Stabilize ')[1]` might be `undefined`, and that `targetEntry.stateId` would then crash. Both wrong on careful read. The split is GATED upstream by `pendingRoll.label.includes('Stabilize ')`. If `includes` returns true, the split is guaranteed to produce ≥2 elements (worst case `['', '']` if the label is exactly `'Stabilize '`). Index `[1]` is then `''`, not `undefined`. And the downstream uses are protected by `targetEntry?.liveState` optional chaining — even if `stTargetName` were empty string, `entries.find(e => e.character.name === '')` returns `undefined`, the optional chain short-circuits, no crash. **Rule:** when an audit flags a `.split(needle)[index]` access as "could be undefined," verify (1) what gates the path upstream, (2) what the split actually returns in the worst case the gate allows. JS `.split` returns ≥1 element always; with a found needle it returns ≥2; with a non-found needle it returns `[originalString]`. The "undefined at index 1" claim requires the needle to NOT be found, which is exactly what the upstream gate prevents. Add this pattern to the evergreen audit prompt's "known false positives — do NOT re-flag" list to break the cycle. Already done in `tasks/audit-prompt.md` (the prompt I hand to fresh audits explicitly calls out the Stabilize split as a verified false positive).

# Handoff — 2026-05-14

Massive session. 16+ commits across new mechanics, schema migrations,
canon doc updates, polish bundles, and a comprehensive test plan.
Nothing left in-flight — every working tree is clean.

---

## What shipped today

### New mechanics (with full spec + canon + code + preview rows)
1. **Coordinated Effort** — chain-of-skill-checks with lead roll setting a forward CMod
   - Spec: `tasks/spec-coordinated-effort.md`
   - Code: table-page modal + `compactRollSummary` branch + active-banner
   - Canon: new sub-section in `/rules/core-mechanics/coordinated-effort` + `tasks/tapestry-rules-canon.md`
2. **Healing on Time-Tick** — Medicine* check queues 50/50 WP heal at +12h and +24h
   - Spec: `tasks/spec-healing.md`
   - Code: new `pending_heal` event type + drainer in `lib/campaign-clock.ts`, modal on table page
   - Canon: new sub-section in `/rules/combat/healing`
3. **Group Check** (no code change) — confirmed current `pooled AMod+SMod` model IS canon; original "individual helper rolls" redesign explicitly killed
   - Spec: `tasks/spec-group-check.md`
4. **Weapon Repair (melee)** — mirror of Unjam for melee weapons; "Repair" verb when category=melee
   - Code: `triggerCoordinatedEffort` / button label / executeRoll result handler / `compactRollSummary`
   - Canon: new `/rules/combat/weapon-repair` page
5. **die3 in expanded log** — `saveRollToLog` now accepts and stores `die3` in `damage_json.die3`; renderer shows `[d1+d2+d3 (insight die)]` split

### Year-0 calendar canonization
- **Year 0 = pandemic year** locked across `/stories/new` picker, module editor, `lib/distemper-timeline.ts`, Campaign Sheet display
- `dayToCalendar` shifted: `yearNumber = realYear - 2025` (was `- 2024`)
- New `app/rules/overview/in-world-time/page.tsx`

### Campaign Sheet
- **Header reformat** to: `Campaign Day N · <time>, <month> <day>th, Year M · <X> days after the first recorded death`
- **GM Edit Clock modal** — Month/Day/Year-of-Pandemic dropdowns + Hour input
- **Export Log button** — downloads roll_log as styled HTML matching preview file
- **Luxury Ration consume button** on PartyCard (decrements + drops Stress + writes feed row)
- New `campaigns.start_canon_day` column for Campaign Day N math

### Module editor
- Canonical Start Date picker (same Month/Day/Year dropdowns)
- `modules.start_canon_day` + `module_versions.start_canon_day` columns
- `publishModuleVersion` snapshots `start_canon_day` from module to version on publish
- New campaign creation paths use module's `start_canon_day` for clock seed

### Bulletproof guard for Moment of Insight + Insight Die badge
- Renderer-level guard: badge + "Moment of Insight" text only render when `r.outcome === 'High Insight' || 'Low Insight'`, regardless of `insight_awarded` or text content
- Group Check banner has the same guard
- Preview file expand-JS rewritten to correctly identify HI/LI vs WS/DF via `.insight-badge` element presence + banner inline-style detection

### Roll-feed renderer unification (RollsFeed.tsx)
- Compact narrative ALWAYS shown; raw `r.label` never displayed
- Expand toggle adds math panel below (dice + damage box + insight-die-spent banner)
- Old DB rows with verbose labels (baked-in dice dumps + dev notes) now render correctly via `compactRollSummary` fallback

### Wording fixes
- Insight Dice on Death: "1 WP + 1 RP per die" → "1 WP + 1 RP total"
- Subsistence Damage: "from the second day onward" → "two-day grace period, day 3 onward"
- Gather/Clothed Check rewrites + scrounged-up variant
- Group Check HI: `and collectively have a Moment of Insight as to why it went so well` (+ LI variant)
- Dissolution narrative icons removed (`✓`, `✗`, `⚠`)
- First Impression header comment fixed (WS separated from HI)
- LI group check preview row added

### Polish bundles (final pass)
- Bundle 1: SQL migrations applied (`initiative-order-rls-members-write.sql` + `player-notes-session-tag.sql`)
- Bundle 2: Insight Dice on Death + Subsistence wording sync to code
- Bundle 3: Thriver godmode widened on `/stories/[id]/community` + `/vehicle` pages
- Bundle 4: `CampaignObjects.tsx` "found nothing" Search button + write path

### Rules pages & canon doc
- 4 new `/rules/*` pages: in-world-time, coordinated-effort, healing, weapon-repair
- Sitemap anchors updated in `lib/rules/sections.ts`
- Canon export prose (`scripts/export-canon.ts`) regenerated → `tasks/tapestry-rules-canon.md` reflects everything live on platform
- Activity Block taxonomy sub-section added to `/rules/communities/structure`
- Distemper-infected canine `+N` deltas vs base species in bestiary table

### Code quality
- New lessons captured (5 today):
  - `feedback_use_user_wording.md` — implement supplied text, don't suggest alternatives
  - `feedback_locate_before_diagnose.md` — find which file the user is viewing before pushing fixes
  - "When user-stated rules conflict with locked rules, FLAG the conflict" (in lessons.md)
  - "When the user shows broken output, find WHERE they're looking" (in lessons.md)
  - "When a fix doesn't land, prove WHY before pushing another fix" (in lessons.md)
- `feedback_no_break_offers.md` re-emphasized after 3rd violation (specific banned phrases added)

### Test plan
- Single comprehensive testplan at `tasks/polish-pass-2026-05-14-testplan.md`
- 11 sections covering every shipped feature, 300+ lines
- Each section has explicit pass conditions and pre-flight steps
- Known caveats / out-of-scope items listed at the bottom

---

## Still open (ranked by readiness)

### 🟢 Ready to build (no blockers)
- **King's Crossing Mall handouts** — blocked on source content (broadcasts/journals/transcripts), not on code
- **Coordinated Effort polish**:
  - Per-participant opt-out (UI; currently End Effort ends whole chain)
  - Bespoke summary banner for the chain (one feed row instead of N)
  - Combat turn-gate / action-cost behavior testing in combat
- **Healing polish**:
  - Wound Infection auto-trigger on LI (currently feed prompt only)
  - Kit consumption / charges (canon silent; currently infinite-use)
  - Per-event GM cancel UI on Campaign Sheet pending list
- **Export Session Log polish**:
  - Bespoke banner types (Combat Started/Ended, Sprint, Drop, etc.) — currently fall through to plain row
  - Chat messages export (currently roll_log only)

### 🔴 Blocked on design call from Xero
- GM Notes / Assets merge — unify / cross-link / leave-as-is
- Other explosives audit (QS Table 18)
- Lv4 Skill Traits suite — unblock pending full list
- `hide-NPCs` global flag decision
- Recruitment/Inspiration/Apprentice Tier-2 semantics (3 items)
- HP pip dots on player-facing NPC card
- #33 "general UI smoothness" — too vague, needs 2-3 specifics

### 🔧 Bigger builds
- VehicleSheet refactor (iframe → shared component) — half-day, high risk

### 📚 CRB rewrite workstream (~200 items in `tasks/todo.md`)
Separate workstream. Major items: General Knowledge → Specific Knowledge sweep, Mechanics\* → Mechanic\* plural sweep, Panic Threshold → Stress Level wholesale replacement, all 12 Profession bundles (7-skill → 5-skill), CMod ladder labels rename, Combat Actions table (17 actions, "Grapple"), Morale outcomes percentages, Fed/Clothed Check tables (missing from CRB), Apprentice unlock wording (3 sites), Apprentice creation CDP, Lv4 Skill Trait paragraphs (24 skills), Insight Dice on Death CRB sides (platform-side done).

### ✅ Untested (live playtest, no code)
- Polish-pass test plan (`tasks/polish-pass-2026-05-14-testplan.md`) — 11 sections covering everything shipped today + the 4 polish bundles
- Live playtest of today's stack — Coord Effort, Healing full +12h/+24h cycle, Campaign Sheet edit modal, melee Repair, Year-0 picker, die3 expanded math, CampaignObjects "found nothing", Thriver godmode access, Export Log, Luxury Ration button

---

## Repository state at handoff

- Both `main` (at `C:/TheTapestry`) and worktree `claude/cool-lamarr-a2b791` (at `C:/TheTapestry/.claude/worktrees/cool-lamarr-a2b791`) are clean and synced.
- All SQL migrations applied to live Supabase via `npx supabase db query --linked`.
- All TypeScript compiles clean (`npx tsc --noEmit --skipLibCheck`).
- All pre-commit hooks (font-sizes, role-literals) pass.
- Vercel deploy should have caught up on the most recent push.

---

## Copy-paste block for next chat

```
You are a working assistant for TheTapestry. Read C:\TheTapestry\tasks\handoff-2026-05-14.md
first to see what shipped this session - it's a lot (Coordinated Effort, Healing on
Time-Tick, Weapon Repair, Year-0 calendar shift, Campaign Sheet header redesign,
Export Session Log, plus a 4-bundle polish pass and the Quick Reference card).

Open items are listed in the handoff under "Still open". The test plan at
C:\TheTapestry\tasks\polish-pass-2026-05-14-testplan.md covers everything we
shipped today (11 sections). I haven't playtested the new stack yet - the live
verification is the highest-leverage thing pending.

Also read:
- C:\TheTapestry\CLAUDE.md - workflow rules + precedence (Tapestry canon >
  Quickstart > SRD > CRB)
- C:\TheTapestry\tasks\lessons.md - check the most recent additions, the user
  was unusually frustrated this past session and I captured five new failure
  patterns to avoid
- C:\TheTapestry\tasks\tapestry-rules-canon.md - regenerated this session;
  contains the new In-World Time, Coordinated Effort, Healing, Weapon Repair
  sections + the Activity Block taxonomy + the +N canine deltas

Ask me what's next - don't propose work unless I ask for options.
```

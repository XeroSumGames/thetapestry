# Active Lanes - live status board

Each of the three chats updates ITS OWN row at the START and END of a work batch
so the other two can steer clear of the same area (the substrate can't otherwise
show in-flight focus). Keep it to a few lines per lane. Convention + ownership:
[tasks/lane-protocol.md](lane-protocol.md).

Format per lane: **focus** (what right now) / **touching** (files/area) /
**updated** (timestamp + HEAD you're working from).

> **NORTH STAR - all lanes align here: [tasks/north-star.md](north-star.md).** Goal = TheTapestry **stable, polished, fun for the 9/1 Kickstarter** (Beta-500 7/1 proves it; player-free / GM-paid billing ~10/1 post-KS, OFF the pre-KS path). Every lane's focus below must ladder up to this - if your work doesn't serve it, question it. #1 priority now: reliable core table loop (tactical-map render fix + verify). New workstream: KS first-impression / polish.

---

## Hunt & Peck
- **focus (DONE 2026-05-31 - KS polish + E2E unblock):** Two ships this session. (1) **Grapple modal migrated onto the locked `RollModal` shell** (`abe87e4`, arch ratchet `41d9042`) - hand-rolled 400px inline modal -> 340px shell (eyebrow/CMod/Insight/opposed-result via `renderOutcome`); roll logic + DB side-effects byte-identical; testplan `tasks/grapple-modal-migration-testplan.md`. (2) **>> 4 combat-flow data-testids SHIPPED for E2E (Puffer's queued ask, Xero-approved 2026-05-30)** - `initiative-row-<entryId>` on each `InitiativeBar` row; **active row uses `aria-current="true"`** (chose the sanctioned alternative - one element can't hold two testids and the active row must keep its per-id testid for ordering asserts; selector `[aria-current="true"]`); `roll-feed-row-<rollId>` on both `FeedColumn` row wrappers (combat/logs + both tabs); `roll-feed-attack-result` as a **className** on dice-result rows (`isDiceRow`; selector `.roll-feed-attack-result`). Additive, zero behavior change, tsc + gates green. **E2E: combat-flow #10 Phase B unblocked.**
- **(3) Rest finish COMPLETE - A+B+C shipped, E verified** (`5ba32d1` A+B, `e468345` C). A = Stress cooling-off (GM toggle, -1 per 8 uninterrupted+enjoyable hours); B = Sick RP half-max cap; C = post-mortal slow regen persists to wp_max via APPLIED live DB change (`recovering_from_mortal_wound` column + `maintain_mortal_recovery_flag` BEFORE-UPDATE trigger, catalog-verified, mirrored to `sql/_baseline/schema.sql`, column flows through both liveState load paths + `database.types.ts`). Recovery math in `lib/rest.ts` + 19 unit tests. Testplan `tasks/rest-heal-AB-testplan.md`.
- **(4) Broken-weapon gate** (`fc9f402` + `3e9c39d`) - live combat Attack / Rapid Fire / Fire from Cover now refuse to fire a Broken weapon (was only ammo-gated); canon "a Broken weapon can't be used at all". #3/#5 verify spot-checks done (#5 = no gap; route-planner is a measure tool).
- **(5) AUDIT M5 Path B** (`b982759`) - wrapped every direct `supabase.channel(...)` inbound handler with `wrapBroadcast`/`wrapDbChange` (Sentry realtime_kind/event tags). 11 components/pages. Send-only sites exempt. Follow-up routed: `check-realtime-wrap.mjs` guardrail (now unblocked, arguably Puffer's lane).
- **>> THE ENTIRE HANDOFF HP QUEUE IS NOW CLEARED.** Grapple polish + testids + 6 KS mechanics (Rest complete; rest shipped/verified per `a1591a1`) + AUDIT M1 (Puffer `b01b561`) + AUDIT M5. Next workstream is open: KS first-impression / polish, or a live verification pass on this session's combat-UI ships (grapple modal / Rest modal / Broken gate - all unit+tsc verified, not yet eyeballed on prod).
- **touching (this session):** `app/stories/[id]/table/page.tsx`, `app/stories/[id]/table/components/FeedColumn.tsx`, `components/{InitiativeBar,CharacterCard,RollsFeed,TableChat,PlayerNotes,CampaignMap,CampaignPins,MessagesBell,NotificationBell}.tsx`, `app/{campaign-sheet,character-sheet,npc-sheet,scene-controls-popout}/page.tsx`, `lib/{rest.ts (new),database.types.ts}`, `tests/lib/rest.test.ts` (new), `sql/character-state-mortal-recovery-flag-2026-05-31.sql` (new, APPLIED) + `sql/_baseline/schema.sql`, `tasks/{todo,lessons,active-lanes,*-testplan}.md`.
- **focus (DONE 2026-05-30 - 12-check 2-client gate ALL-PASS, #1 KS core-loop reliability CLOSED):** Whole gate ran clean on prod with Xero+2nd account, two browsers, narrow viewport included. Phase A (1-8: render alignment + scene propagation + Share Map + resize + reload) green; Phase B (9-12: move-follow on active combatant + own PC + no spurious follow + CENTER priority) green. Shipped today: `c0d9fb8` myCharacterId late-arrival re-center; `a068ffb` "+ Map" re-spawns archived tokens at fresh defaultSpawnCell; `38e59cb` + `5845bfd` popout `cell_px` persist click-only + DB CHECK constraint 5-300; `0599207` + `c3e0f10` viewingSceneId prop + sticky scene lock so the GM can browse scenes privately; `421a4d6` Share Map = one-shot push of GM's current view (no toggle, works on tactical AND campaign). Risk Register: TacticalMap YELLOW -> GREEN ([debug-handoff.md L43](debug-handoff.md)). NEXT (recommended to Xero): LOOT cluster ("Search Remains does nothing" - core combat-loop gap), then non-destructive in-combat Place Tokens.
- **>> ROUTED -> PLAYWRIGHT/E2E (Xero ask 2026-05-27): re-cert + extend `e2e/tactical-map-render.spec.ts` for the NEW model. [E2E: DONE 2026-05-27 - source-guard tightened to ZERO setImgScale + full re-cert 129/0/4 green; live aspect-fit found canvas-coupled -> stays MANUAL + unit-covered; see Playwright row + dashboard.]** Existing 3 tests still GREEN on prod. Newly automatable (data-layer, 2-client): after a GM opens a scene WITH a background, the persisted `grid_cols`/`grid_rows` track the image's ASPECT (proportional cap, not a square) and are read identically by GM + player; source-guard still holds with ZERO `setImgScale` calls (img_scale fully out of the render). STILL MANUAL (canvas, not pixel-assertable): "fills the panel width on open", "no distortion", "zoom is LOCAL - GM zooming doesn't move the player's view". Full behavior + manual checklist (incl. fit-to-width addendum H-K): `tasks/tactical-bg-grid-lock-testplan-2026-05-27.md`.
- **touching:** `components/TacticalMap.tsx`, `components/MapSetupPanel.tsx` (new), `lib/tactical-grid.ts`, `lib/tactical-view.ts`, `app/scene-controls-popout/page.tsx`, `app/stories/[id]/table/page.tsx`, `tests/lib/*`.
- **updated:** 2026-05-27, HEAD 663f49d (rebases on push).

## Puffer Fish
- **focus (DONE 2026-05-30 - #1 KS core-loop reliability CLOSED; lane IDLE/available):** Three threads landed this session. (1) **Beta-safety floor LIVE** - Better Stack uptime monitor on `/api/health` with Slack-push pager, verified end-to-end (`tasks/ops-uptime-monitor-setup-2026-05-29.md`); incident-runbook L38 + 3 readiness plans updated. Observability B (Sentry alert-rule routing) split as a separate ~5-min Xero task. (2) **Tactical-map gate-RED finding routed + closure:** identified suspect #1 (stale `initiativeOrderRef` at follow time) in [tasks/finding-tactical-map-move-follow-red-2026-05-30.md](finding-tactical-map-move-follow-red-2026-05-30.md); HP fixed it in `a9b8c44`; extended the 12-check gate with checks 9-12 (move-follow + CENTER priority); Xero ran all-12 GREEN; HP applied the Risk Register demote (per gate-doc ownership note) at [debug-handoff.md L43](debug-handoff.md) - Puffer reviewed + endorsed it as audit-clean (SHA chain `7ba065b` -> `a9b8c44` -> `c0d9fb8`/`a068ffb`/`38e59cb`+`5845bfd`/`0599207`+`c3e0f10`/`421a4d6` cited). **#1 KS core-loop reliability item CLOSED.** (3) **`gm_apply_damage` SECURITY DEFINER RPC** drafted + applied live ([sql/gm-apply-damage-rpc-2026-05-30.sql](../sql/gm-apply-damage-rpc-2026-05-30.sql), verified in pg_proc); E2E used it for combat-flow #10 Phases A (`5f6e3eb`) + C (`2306181`), RPC contract honored. **Next stability-audit cycle:** fold the 7 new lessons logged today (L3, L7, L11, L15, L19, L23, L27 of `tasks/lessons.md`) into the pattern.
- **>> ASK FOR HUNT & PECK [XERO APPROVED 2026-05-30 - HP CLEAR TO SHIP]:** add 4 data-testids to unblock combat-flow #10 Phase B (the last E2E piece for the beta-500 floor item). Per `tasks/e2e-combat-flow-plan-2026-05-30.md` Section "BLOCKED ON HP TESTIDS (b)": `data-testid="initiative-row-<entryId>"` on each InitiativeBar row, `data-testid="initiative-row-active"` (or stable `aria-current="true"`) on the active row, `data-testid="roll-feed-row-<rollId>"` on each roll-feed row, `data-testid="roll-feed-attack-result"` as a class-marker on attack-result rows. One additive HP commit, no behavior change. Also tracked in `tasks/todo.md` "[ROUTED -> HUNT & PECK] add 4 combat-flow data-testids". On ship, ping E2E -> Phase B lands ~30 min.
- **DONE 2026-05-30 (was ASK FOR PUFFER FISH, Xero greenlit, E2E ask):** `gm_apply_damage` v2 APPLIED LIVE (`4259d67`). 5th arg `p_infection_risk boolean DEFAULT false` added; sets `damage_json.infection_risk=true` on PC mortal-wound entries when invoked with the flag. v1 4-arg overload dropped, single signature in pg_proc verified. Existing E2E Phase A/C callers byte-identical. File: `sql/gm-apply-damage-rpc-v2-infection-2026-05-30.sql`. E2E: Phase C end-of-combat infection-modal assertion now unblocked. Also did a KS first-impression visual pass against prod (Browser 3, 1440x900) - findings in `tasks/ks-visual-pass-2026-05-30.md` - 2 CRITICAL items route into existing F1 (cold-/ -> map not pitch) and F2 (live [PLACEHOLDER] text rendered on `/publiclanding`); no new top-level items.
- **touching:** `tasks/todo.md`, `tasks/active-lanes.md`, `tasks/tactical-map-render-fix-spec-2026-05-26.md` (new), `sql/tactical-scenes-scale-sentinel-2026-05-26.sql` (new, APPLIED live), `sql/_baseline/schema.sql` (tactical_scenes mirror). LIVE DB change this session: tactical_scenes scale-sentinel migration (Xero-authorized, backward-compatible). No app/components/lib/e2e edits.
- **CROSS-LANE (Xero-authorized, beta-blocker):** edited HP files `app/signup/page.tsx` + `app/login/page.tsx` to remove the invalid Turnstile `size: 'invisible'` render param (`5f73bfb`) - it threw a TurnstileError and broke real signups ("bot check failed"). Beta-500 signup env vars also confirmed green (403-on-dummy). HP: heads-up, these two files moved.
- **focus (DONE 2026-05-31 - v3 bridge applied + audit + drift closure):** (1) `gm_apply_damage` v3 BRIDGE APPLIED LIVE (`f4f3e9d`, sql/gm-apply-damage-rpc-v3-bridge-2026-05-31.sql). Closes E2E's infection_risk -> RollsFeed banner gap (finding-infection-risk-app-bridge-2026-05-30.md, Option A). When the flag fires (PC + cross-to-zero + p_infection_risk=true), RPC also inserts a sibling `wound_infection_warning` roll_log row mirroring `maybeLogWoundInfection` shape - DB-level dedup vs the most-recent OUTCOME='combat_start'. Existing E2E A/C/C-v2 callers byte-identical. **E2E unblocked** for banner-renders-on-owner DOM assertion (~15 min). (2) `/stability-audit 2026-05-30` shipped (tasks/stability-audit-2026-05-30.md): 0 BLOCKER, 3 HIGH, 5 MED, 4 LOW. Knocked 7 of 11 audit todos (H1/H2/H3/M2/M4/M5/L3) - debug-handoff §1/§2/§3 refreshed + Patterns-Learned subsection added + Confidence Ledger refreshed + L3 closed clean + M5 finding-realtime-wrap-bypass-2026-05-30.md routed to HP. (3) Health-pulse drift closed: H1/H2/H3/M2/M4 + L78/L100/L175 boxes flipped to [x] with shipped-evidence pointers (`99ef1b1` + this).
- **updated:** 2026-05-31, lane/puffer rebased on origin/main. Prior 2026-05-30 (#1 KS reliability closure) + 2026-05-27 focus blocks preserved below for trail.

## Playwright / E2E
- **focus (DONE 2026-05-31 - playtest follow-up batch):** Two HIGH-priority specs from Xero's 2026-05-31 brief, each its own commit. **(1) Wall-segment door + window cross-client** (`e2e/wall-segment-door-cross-client.spec.ts`, new): wrote PARKED via `test.fixme` per the "expect-fail until HP ships" framing; HP shipped the exact `toggle_wall_segment_door` SECURITY DEFINER RPC in `b2e7663` within the same session (applied live, `lib/data/tactical.ts` has the typed builder); un-fixme'd + GREEN 7.5s. Covers both door + window kinds in one test: player calls RPC -> segment's `door_open` flips in DB -> GM's tactical_scenes refetches via realtime. Canvas LoS sweep + fog clearing through the opening stays MANUAL. Closes the playtest LoS-desync bug Xero called out as RECURRING. **(2) Phase C v3 infection-warning sibling row** (`combat-flow.spec.ts` test 4, new): locks in Puffer's v3 RPC bridge (`sql/gm-apply-damage-rpc-v3-bridge-2026-05-31.sql`). GM seeds combat_start anchor -> calls `gm_apply_damage(pc, wp_remaining, p_infection_risk=true)` -> asserts RPC return mirrors `wound_infection_warning=true`, damage row has `damage_json.infection_risk=true` (v2 contract preserved), and a SIBLING `roll_log` row lands with exactly maybeLogWoundInfection's shape (`outcome='wound_infection_warning'`, character_name, canonical label, all dice/mods zeroed). Standalone green 4.8s. Dedup case deferred (needs wp restoration between calls). **Queued for next batch:** HP shipped grapple canon `544b4eb` (defender action loss + Subdue) - now buildable, deferred 1 batch for verification + spec; hidden NPCs (`finding-hidden-npc-in-initiative-2026-05-31`) still pending HP UI; stuck-click clusters observation from the 2026-05-31 player recorder dump - exploratory pass on hit-box vs token-select latch. Dashboard refreshed IN PLACE (banner 140 passed, +2 changelog entries, footer). **Lane now IDLE / available.**
- **focus (DONE 2026-05-30 - Phase C v2 infection_risk gates SHIPPED + bridge gap routed):** Puffer applied `gm_apply_damage` v2 live (`4259d67`) with the optional 5th arg `p_infection_risk`. Extended `combat-flow.spec.ts` with a 4-gate contract test (this batch, standalone green 23.9s): backward-compat (4-arg call stays v1-byte-identical), negative non-mortal (no flag), negative NPC target (no flag - canon), positive PC mortal-wound (flag sets). **Bridge gap discovered + routed:** no app code reads `damage_json.infection_risk` today; the existing `🩸 Wound Infection Warning` banner is OUTCOME-keyed and triggered by `maybeLogWoundInfection` from the in-combat attack handler. Held the DOM banner-renders-on-owner assertion until the bridge lands (per the section-c lesson: assert what actually DRIVES the UI). Full finding + recommended fix in `tasks/finding-infection-risk-app-bridge-2026-05-30.md`; preferred fix is Option A (RPC also inserts the `wound_infection_warning` row when the flag fires - atomic, no client wiring). On bridge ship -> banner DOM assertion lands as ~15-min follow-up. **Lane now IDLE / available** (watching for the bridge + the HP testids for Phase B). Combat-flow #10 build-plan: A, C, C-v2 closed; B + infection-modal pending owning-lane work.
- **focus (PRIOR 2026-05-30 - combat-flow #10 Phases A + C shipped):** Puffer's `gm_apply_damage` SECURITY DEFINER RPC (live) drove the data-layer assertion the dice path can't. `e2e/combat-flow.spec.ts` ships two tests in two commits per the brief. **Phase A** (commit `5f6e3eb`): throwaway campaign + marv joins by code with MARV_CHAR + GM seeds NPC + Start Combat -> initiative_order has exactly one is_active=true + character_states per PC combatant + player IN-COMBAT live; cascade-delete teardown. **Phase C** (this commit): GM calls gm_apply_damage(pc, wp_max) -> wp_current=0 + stress+1 (canon cap 5) + roll_log row with damage_json.via='gm_apply' + target shape; player character_states refetch fires via realtime; negative non-GM call RAISES 'not authorized'. Both standalone green (11s + 6.7s); full re-cert update follows. Build-plan #10 todo CLOSED. Phase B still holding for HP testids; end-of-combat infection-modal needs a small RPC extension (deferred). NOT touching `TacticalMap.tsx` move-follow per the gate-RED hold. **THEN (gate runnable, Xero 2026-05-30):** the 12-check 2-client gate (`tactical-map-verify-2client-testplan-2026-05-27.md`) is now runnable - HP's move-follow + late-char-id + cell_px-cap fixes landed. Re-verified the automated subset against the new HEAD: **`tactical-map-render.spec` 3/3 GREEN** (cross-client render identity + ZERO setImgScale + locked-map Center escape hatch). Dashboard now carries a prominent "Tactical-map 2-client acceptance gate - AWAITING MANUAL RUN" block with the 12 checks enumerated; on all-GREEN, Puffer demotes the Risk Register + KS #1 closes; on RED, finding doc + todo route to HP. Manual run owed by Xero + a 2nd window (HP's 5-min smoketest first, then Puffer's 12). **Lane now IDLE / available** (watching for the gate result to record on the dashboard + relay any RED to the owning lane).
- **focus (DONE 2026-05-30 - all 3 priorities shipped):** Parallel pickup while HP fixes tactical-map move-follow gate-RED. **P1** PC-trade un-fixme already in `e866df0`; verified + todo closed in `bf937c3`. **P2** full re-cert covered HP's 20+ tactical/vehicle/inventory commits - **134 passed / 0 failed / 0 flaky** in 5.4 min (FIRST clean-sheet re-cert; the 2026-05-27 anon-key de-flake eliminated the 4-5 retries that prior loaded runs absorbed). Dashboard `tasks/e2e-results.html` refreshed IN PLACE per the hard rule (banner + dated changelog + footer). **P3** combat-flow #10 planning doc shipped in `d023404` - Phase A buildable today (Start Combat + initiative_order + IN-COMBAT propagation, strict superset of `section-a1`); Phases B+C gated on HP shipping (a) `gm_apply_damage` SECURITY DEFINER RPC and (b) 4 testids on initiative-bar + roll-feed rows. **Lane now IDLE / available.** Recommended next when HP unblocks: build Phase A standalone (~unblocked) or wait for (a)/(b) to ship the full spec.
- **focus (DONE 2026-05-27 - routed tactical re-cert + extend):** Picked up HP's routed ask for the new fit-to-width / bg-locked model. **Extended `tactical-map-render.spec.ts`:** tightened the source-guard to assert **ZERO `setImgScale` calls** (img_scale fully retired from the render under `b38cdf2` - was: no width-derived ones). Cross-client grid identity (test 2) already covers the shared-layout guarantee. **TRIED + DROPPED** a live "grid matches image aspect" assertion: it proved canvas-coupled + state-dependent (existing scenes only re-fit when the GM canvas mounts + loads the bg; my headless GM-open didn't trigger it, DB stayed at stale dims) - so per the no-brittle-tests rule it stays MANUAL, with the aspect MATH unit-covered (`gridToCoverMap` in `tests/lib`). Lesson logged. **Full `npm run test:e2e` re-cert: 129 passed / 0 failed / 4 retry-absorbed** (prod heavily loaded, 12.8 min; my locked-map test was one of the 4, passed on retry - added to watch). Living dashboard `tasks/e2e-results.html` refreshed (banner + changelog + Ch8 matrix). **THEN (de-flake, Xero ask):** diagnosed the 4 flakies' shared root cause = the per-spec anon-key SNIFF racing a 12s window under load (fails at `expect(creds)` setup, before the feature). FIX: `auth.setup.ts` now captures the PUBLIC anon key ONCE (runs alone -> reliable) + persists to gitignored `e2e/.auth/anon-key.txt`; `_teardown.captureAnonKey` reads env var -> file -> live-sniff fallback. Verified: setup writes the 208-byte key, the locked-map test then passed FIRST-TRY (27s, no retry). De-flakes the whole 2-client REST class. **Lane now IDLE / available.**
- **focus (PRIOR, shipped):** SHIPPED + GREEN on prod this session (4 specs, all deterministic): (A) **tactical-map render-fix 2-client acceptance** (`tactical-map-render.spec.ts`, 3 tests) = Puffer's routed automatable subset (`tasks/tactical-map-verify-2client-testplan-2026-05-27.md`): gate item 2 (2 clients read identical scene render fields), item 3 (source-guard: no per-client-width `setImgScale`), + locked-map Center escape hatch. FLAG to Puffer: gate item 1 ("img_scale non-null after GM opens unset scene") doesn't match the SHIPPED "default 1/NULL = raw" model - I assert nullable-or-number; 8 canvas-pixel checks stay MANUAL. (B) **messages-dm** (`messages-dm.spec.ts`) DM realtime delivery; Xero OK'd option (b) residue; teardown self-heals when the delete-message RLS lands. (C) **pins broadcast-catch-up regression** (`pins-catchup.spec.ts`) locks in HP `344bbfc` - REST-insert a pin (missed, no broadcast) on the broadcast-only `CampaignPins`, fire `visibilitychange`, assert it converges. (D) **section-c NPC-reveal REWRITTEN** (`section-c-npc-reveal.spec.ts`) - cleared the deterministic test-debt; old test asserted the wrong flag (`hidden_from_players`), now drives the real driver (`npc_relationships.revealed` -> player's loadRevealedNpcs refetch), 3/3 deterministic. **ROUTED feature (Xero ask):** per-message DELETE in Messages = one shared dep (Puffer author-can-delete-own-message RLS first, then HP UI + thread DELETE-event handler). **Lane now IDLE / available.** NEXT PICKUPS: #10 combat-flow + canvas Phase 3 (still blocked on HP testids/`__tacticalTestApi` bridge/GM-damage hook); E2E full-suite Beginners-Guide coverage growth. PRIOR (standing): PC-trade closed the characters data-loss class (Risk Register YELLOW->GREEN); vehicle-maintenance + rumors Ch14.4 + campfire-lfg-warstory certified.
- **touching (this session):** NEW `e2e/{messages-dm,pins-catchup}.spec.ts`; `e2e/{tactical-map-render,section-c-npc-reveal}.spec.ts` rewritten/extended + docs `tasks/{todo,lessons,active-lanes}.md`. Read-only on `components/{TacticalMap,CampaignPins,CampaignMap}.tsx`, `app/{messages,stories/[id]/table}/page.tsx`, `sql/_baseline/schema.sql`. No app-code edits, no new data-testids.
- **updated:** 2026-05-27, working from HEAD 77b3c12 (lane/e2e; rebases on next push).

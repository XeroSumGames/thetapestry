# Active Lanes - live status board

Each of the three chats updates ITS OWN row at the START and END of a work batch
so the other two can steer clear of the same area (the substrate can't otherwise
show in-flight focus). Keep it to a few lines per lane. Convention + ownership:
[tasks/lane-protocol.md](lane-protocol.md).

Format per lane: **focus** (what right now) / **touching** (files/area) /
**updated** (timestamp + HEAD you're working from).

> **NORTH STAR - all lanes align here: [tasks/north-star.md](north-star.md).** Goal = TheTapestry **stable, polished, fun for the 9/1 Kickstarter** (Beta-500 7/1 proves it; player-free / GM-paid billing ~10/1 post-KS, OFF the pre-KS path). Every lane's focus below must ladder up to this - if your work doesn't serve it, question it. #1 priority now: reliable core table loop (tactical-map render fix + verify). New workstream: KS first-impression / polish.

---

## Hunt & Peck
- **focus:** Tactical-map playtest fixes shipping fast. SHIPPED this run: vehicle install/gather checks (edb2032+12fbe58); token spawn-spread (08990ad) + "+ New Map" double-fire guard (2d17047) + live campaign cleanup; SCENE NAME typing fix (f1f44fc, local draft + commit-on-blur); grid auto-covers the map (65a86b1, lib/tactical-grid.ts); token spawn-visibility (cd50585 - spiral was hiding 2nd/3rd PCs under the top-left toolbar at (0,0)/row 0; now stays down-right of the (1,1) anchor). PENDING: (1) scene-chooser CHOOSER mockup `tasks/scene-chooser-mockup.html` AWAITING Xero feedback; (2) WATCH per-player Map toggle double-click race (retest after cd50585, may need an in-flight guard - see todo); (3) COMMUNITY header-dropdown "too much" - AWAITING Xero clarification (all 4 header dropdowns share renderHeaderMenu); (4) PC-trade rewire `onGiveItem`->`give_item_to_character` RPC (routed from Puffer Fish). Eyeball owed on Minnie vehicle popout.
- **touching:** `lib/tactical-spawn.ts`, `lib/tactical-grid.ts`, `components/TacticalMap.tsx`, `app/stories/[id]/table/page.tsx`, `app/scene-controls-popout/page.tsx`, `tests/lib/*`. Live DB: deleted 4 blank scenes + reactivated a real scene (Xero-authorized).
- **updated:** 2026-05-25, HEAD cd50585 (rebases on push).

## Puffer Fish
- **focus:** ADVISOR MODE (operating-mode "Advisor mandate"). **North star LOCKED + wired into every shared surface** (`tasks/north-star.md` + a CLAUDE.md "read first" callout + active-lanes/handoff/lane-protocol pointers) so all 3 lanes align - goal = TheTapestry **stable/polished/fun for the 9/1 Kickstarter** (player-free / GM-paid billing ~10/1 post-KS, OFF the pre-KS path). **Beta-500 #1 blocker (signup) CLOSED + verified on prod** (Upstash + Turnstile env + removed the invalid `size:'invisible'` render param). All 12 Minnie-playtest findings routed; Risk Register refreshed; broadcast catch-up gap confirmed across 5 surfaces (routed to HP). NOW driving the #1 (reliable core table loop): built the **tactical-map 2-client verification GATE** (`tasks/tactical-map-verify-2client-testplan-2026-05-27.md`) - the go/no-go proof for HP's render rewrite, data-subset routed to E2E to automate. NEXT: scope the KS first-impression / polish workstream (#2).
- **touching:** `tasks/todo.md`, `tasks/active-lanes.md`, `tasks/tactical-map-render-fix-spec-2026-05-26.md` (new), `sql/tactical-scenes-scale-sentinel-2026-05-26.sql` (new, APPLIED live), `sql/_baseline/schema.sql` (tactical_scenes mirror). LIVE DB change this session: tactical_scenes scale-sentinel migration (Xero-authorized, backward-compatible). No app/components/lib/e2e edits.
- **CROSS-LANE (Xero-authorized, beta-blocker):** edited HP files `app/signup/page.tsx` + `app/login/page.tsx` to remove the invalid Turnstile `size: 'invisible'` render param (`5f73bfb`) - it threw a TurnstileError and broke real signups ("bot check failed"). Beta-500 signup env vars also confirmed green (403-on-dummy). HP: heads-up, these two files moved.
- **updated:** 2026-05-27, lane/puffer rebased on origin/main (HEAD d2ea8c4 + this commit).

## Playwright / E2E
- **focus:** SHIPPED + GREEN on prod this batch: (A) **tactical-map render-fix 2-client acceptance** (`e2e/tactical-map-render.spec.ts`, now **3 tests**) - the #1 KS reliability proof. Reconciled with **Puffer's acceptance GATE** (`tasks/tactical-map-verify-2client-testplan-2026-05-27.md`, landed after my first push): this IS the routed automatable subset - test 2 = gate item 2 (2 clients read the SAME scene render fields via REST), test 3 = gate item 3 (NEW: static source-guard that no `setImgScale()` derives the shared scale from a per-client width = the divergence root cause stays removed); test 1 = locked-map "Center" escape hatch (player has it, GM `toHaveCount(0)`). **FLAG for Puffer:** gate item 1 ("img_scale persisted NON-NULL after a GM opens an unset scene") does NOT match the SHIPPED model - HP's `6ef34ce` chose "default 1/NULL = render raw," persist only on GM corner-resize - so I assert nullable-or-number (a non-null assert would falsely RED a correct build); the 8 canvas-pixel checks stay MANUAL (gate owns them). (B) **messages-dm** (`e2e/messages-dm.spec.ts`) - DM realtime delivery (Sys O). Xero chose **option (b)**: accept one `[E2E]` residue per run in the gm<->marv thread (disposable accounts). Teardown is best-effort delete that self-heals once the delete-message RLS lands. **ROUTED a new feature (Xero ask):** per-message DELETE in Messages = one shared dep -> Puffer adds author-can-delete-own-message RLS DELETE on `messages` (also retires the DM teardown residue), HP adds the per-message delete control + a DELETE-event handler on the thread sub; sequence Puffer-first. **Lane now IDLE / available.** NEXT PICKUPS (HP just shipped `344bbfc` broadcast catch-up on 5 subs -> that regression spec is now BUILDABLE): broadcast-catch-up regression; #10 combat-flow + canvas Phase 3 (still HP testids/bridge/damage-hook); section-c NPC-reveal rewrite (test-debt). PRIOR (standing): PC-trade closed the characters data-loss class (Risk Register YELLOW->GREEN); vehicle-maintenance + rumors Ch14.4 + campfire-lfg-warstory shipped+certified.
- **touching (this batch):** `e2e/messages-dm.spec.ts` (NEW), `e2e/tactical-map-render.spec.ts` (+test 3 + gate cross-link) + docs `tasks/todo.md`, `tasks/lessons.md`, `tasks/active-lanes.md`. Read-only on `components/TacticalMap.tsx`, `app/messages/page.tsx`, `sql/_baseline/schema.sql`. No app-code edits, no new data-testids.
- **updated:** 2026-05-27, working from HEAD 344bbfc (lane/e2e; rebases on next push).

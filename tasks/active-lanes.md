# Active Lanes - live status board

Each of the three chats updates ITS OWN row at the START and END of a work batch
so the other two can steer clear of the same area (the substrate can't otherwise
show in-flight focus). Keep it to a few lines per lane. Convention + ownership:
[tasks/lane-protocol.md](lane-protocol.md).

Format per lane: **focus** (what right now) / **touching** (files/area) /
**updated** (timestamp + HEAD you're working from).

---

## Hunt & Peck
- **focus:** Tactical-map playtest fixes shipping fast. SHIPPED this run: vehicle install/gather checks (edb2032+12fbe58); token spawn-spread (08990ad) + "+ New Map" double-fire guard (2d17047) + live campaign cleanup; SCENE NAME typing fix (f1f44fc, local draft + commit-on-blur); grid auto-covers the map (65a86b1, lib/tactical-grid.ts); token spawn-visibility (cd50585 - spiral was hiding 2nd/3rd PCs under the top-left toolbar at (0,0)/row 0; now stays down-right of the (1,1) anchor). PENDING: (1) scene-chooser CHOOSER mockup `tasks/scene-chooser-mockup.html` AWAITING Xero feedback; (2) WATCH per-player Map toggle double-click race (retest after cd50585, may need an in-flight guard - see todo); (3) COMMUNITY header-dropdown "too much" - AWAITING Xero clarification (all 4 header dropdowns share renderHeaderMenu); (4) PC-trade rewire `onGiveItem`->`give_item_to_character` RPC (routed from Puffer Fish). Eyeball owed on Minnie vehicle popout.
- **touching:** `lib/tactical-spawn.ts`, `lib/tactical-grid.ts`, `components/TacticalMap.tsx`, `app/stories/[id]/table/page.tsx`, `app/scene-controls-popout/page.tsx`, `tests/lib/*`. Live DB: deleted 4 blank scenes + reactivated a real scene (Xero-authorized).
- **updated:** 2026-05-25, HEAD cd50585 (rebases on push).

## Puffer Fish
- **focus:** POST-PLAYTEST TRIAGE done (Minnie S7, 2026-05-26). Pulled all 12 GM recorder `mark` notes + player dumps; captured & routed every finding in `todo.md` (mostly -> HP). My two owned items CLOSED: (1) **Tactical-map P0** - wrote the render-fix spec `tasks/tactical-map-render-fix-spec-2026-05-26.md` (shared authoritative img_scale + per-client fit via zoom, locked-map center-on-me) AND applied the unblocking schema migration LIVE (`sql/tactical-scenes-scale-sentinel-2026-05-26.sql`: img_scale nullable + natural_w/h; 15 scenes->NULL sentinel, 6 kept; baseline schema mirrored). HP owns the client rewrite next. (2) **Pin-realtime** - diagnosed: wiring correct (channel match, RLS/publication fine); root cause = fire-and-forget `pins_changed` broadcast has no delivery guarantee, so a bounced/late player never reloads til refresh; fix routed to HP (catch-up `loadPins` on channel SUBSCRIBED + tab-visibility). Proximity auto-reveal = not a feature (Xero decision). NEXT: realtime broadcast catch-up SWEEP (the pin bug is likely one of a class - other broadcast-only subs lacking a reconnect/visibility reload). Also did a cross-repo doc: folded my arch review into `C:\TheTableau\tasks\merge-plan.md` (3-VTT platform plan).
- **touching:** `tasks/todo.md`, `tasks/active-lanes.md`, `tasks/tactical-map-render-fix-spec-2026-05-26.md` (new), `sql/tactical-scenes-scale-sentinel-2026-05-26.sql` (new, APPLIED live), `sql/_baseline/schema.sql` (tactical_scenes mirror). LIVE DB change this session: tactical_scenes scale-sentinel migration (Xero-authorized, backward-compatible). No app/components/lib/e2e edits.
- **updated:** 2026-05-26, lane/puffer == origin/main (in sync).

## Playwright / E2E
- **focus:** SHIPPED (cross-lane, Xero-authorized, HP idle + had it queued) the **PC-trade client rewire** that closes the characters-cross-user data-loss class: `onGiveItem` (table/page.tsx) now calls the live `give_item_to_character` RPC (atomic both-sides) instead of the raw cross-user `.update`; `InventoryPanel.confirmGive` skips the client giver-decrement for the PC case only (avoids double-spend vs the RPC's SELECT FOR UPDATE); giver refreshed via loadEntries (sender doesn't self-receive the broadcast). UN-fixme'd + green `inventory-trade.spec` PC-trade test (marv->percy item MOVES). Full re-cert clean (126 passed; lone fail = pre-existing section-c flake, unrelated + flagged as test-debt for rewrite). Risk Register characters-class YELLOW->GREEN. Touched `app/stories/[id]/table/page.tsx` + `components/InventoryPanel.tsx` (HP's files). **HEADS-UP for HP:** the give-flow rewire is done - the section-c NPC-reveal test now fails deterministically (test fragility, NOT a reveal regression; manual 2-client check covers the feature for tonight). **Lane now IDLE / available.** EARLIER this session: SHIPPED three specs, all GREEN + full re-cert (127 passed, 0 flaky). (1) `vehicle-maintenance-checks.spec.ts` - dice-gated Install/Gather (HP's 12fbe58): vehicle PATCHed into campaigns.vehicles (GM owns -> direct JSONB, no RPC), assert flow/structure + roll_log checkKind. (2) Ch14.4 in `rumors-publish-clone.spec.ts` - publish v1 -> clone -> publish v2 -> subscriber `📦 update` notice (semver-agnostic getByTitle, `.eq(campaign_id)`-scoped) -> version-history. (3) `campfire-lfg-warstory.spec.ts` - the campfire composers campfire-social left uncovered: Thriver LFG post auto-approves + marv "I'm Interested" (via search + `#lfg-<id>`); Thriver War Story at GLOBAL scope auto-approves (verified war_stories live on prod first). 3 lessons logged. PRIOR shipped+certified: Phase 2 6/7 (#8/#9/#11/#12/#13/#14 + map_pins bypass-regression). Still blocked: #10 combat-flow + Phase 3 (app testids/GM-damage hook, HP); PC-trade un-fixme (give_item_to_character RPC applied by PF, needs HP `onGiveItem` rewire); messages-dm (Xero teardown call). **Lane now IDLE / available.**
- **touching (this batch):** `app/stories/[id]/table/page.tsx` (onGiveItem), `components/InventoryPanel.tsx` (confirmGive), `e2e/inventory-trade.spec.ts` (un-fixme'd PC-trade test) + docs `tasks/todo.md`, `tasks/lessons.md`, `tasks/debug-handoff.md` (Risk Register), `tasks/active-lanes.md`. Earlier-session specs: `e2e/{campfire-lfg-warstory,vehicle-maintenance-checks}.spec.ts` (new), `e2e/rumors-publish-clone.spec.ts` (+1).
- **updated:** 2026-05-24, HEAD will be the push of campfire-lfg-warstory (lane/e2e; rebases on push).

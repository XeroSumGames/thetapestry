# 2026-05-13 Phase 3 Batch — Pre-Playtest Code Review

> Code-read spot-check of the load-bearing 2026-05-13 batch (campaign-clock drainers + 10 feed-audit drift fixes). Performed 2026-05-17 before the next playtest, while the batch was still HOPED-FOR after 4 days. Goal: pre-surface bugs to shrink post-playtest debug surface.
>
> **Result: clean bill of health.** Zero concrete bug candidates. The code reads correctly against canon — off-by-one day counting is sound, idempotency is protected by `last_subsistence_day`, stress-on-mortal/incap correctly gates on state transitions, all supabase mutations are awaited.

---

## Commits reviewed

**Campaign-clock drainers**
- `aa20bdb` feat(campaign-sheet): Phase 3b — rations consumption on day-tick
- `36feee3` feat(campaign-sheet): Phase 3c — subsistence damage on day-tick
- `80bc8f3` feat(campaign-sheet): Phase 3d — community Skip Week ticks the campaign clock

**Feed-audit fixes**
- `e2fd0f8` fix(rolls-feed): align live feed to canonical log template (6 drifts)
- `e979b04` fix(rolls-feed): close 4 audit drifts (3 template, 1 code)

---

## Drainer verification highlights

- **Rations** (Phase 3b): decrements `characters.data.rations.count` by dayDelta per PC per day boundary; stamps `out_since_day = prevCanonDay + rations.count + 1` on the day the last ration is consumed.
- **Subsistence** (Phase 3c): grace period is `hungryDay < 3` (no damage); from `hungryDay >= 3`, 1 WP + 1 RP per day. Day-boundary walk iterates `prevCanonDay+1` through `prevCanonDay+dayDelta`. Idempotency via `last_subsistence_day` so re-advances don't double-damage.
- **Stress on mortal/incap**: fires only on the WP=0 / RP=0 transition (`wpFrom > 0 && wpTo === 0`), not when already at 0. Capped at 5 per canon, each check capped independently before merge.
- **Self-heal path**: if rations are restored to >0, `out_since_day` and `last_subsistence_day` are cleared.

## Feed-audit verification highlights

All 10 drifts (6 in `e2fd0f8` + 4 in `e979b04`) verified present in current code at the expected file:line. No regressions.

---

## Playtest watch-fors (8 items)

The agent identified these as the sharp tests for tomorrow:

1. **Rations consumption** — Tick past Day 3 of no rations on a PC. Verify both WP and RP each take 1 damage on Day 3 (hungry day 3, first damage day).
2. **Grace period boundary** — Set a PC to Day 2 of hunger. Advance 1 day → no damage. Advance again → +1 WP / +1 RP on Day 3 boundary.
3. **Idempotency** — Tick 1 hour then immediately 23 hours (or 1 day + 0 hours). Subsistence damage applies exactly once, not twice.
4. **Stress on mortal/incap transition** — Take a PC to WP=2, RP=2, stress=4. Advance a hungry day. Verify WP→0 and RP→0 BOTH fire and stress becomes 5 (capped), not 6.
5. **Fed Check expand preamble** — Perform a Fed/Clothed Check. Expand. Verify "Rolled by <leader> (PC/NPC) - <skill>" line appears at top.
6. **Recruit LI tag** — Roll Recruit with Low Insight. Verify "and has a Moment of Insight as to why it went so badly" appears.
7. **Clock broadcast** — Two tabs, same campaign. Tick clock in one → other tab's clock updates without manual refresh.
8. **Infection drainer coexistence** — If any character is infected, verify infection days still decrement correctly during rations/subsistence ticks.

---

## Confidence

High. The agent traced the rations-out scenario from Day 10 forward through Day 14, hand-verifying every day's expected state against the code's actual computation. Math checks out at every boundary. The Y3 trigger we landed today does not touch this batch (different tables), so no interaction risk.

If tomorrow's playtest surfaces a bug in this area, it'll be in a code path the agent didn't trace (e.g. Phase 3d community Skip Week was noted but not examined in depth). Spot-check the watch-fors first, then escalate to broader debug.

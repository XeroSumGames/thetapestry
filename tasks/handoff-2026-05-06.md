# Handoff — 2026-05-06

Marathon session continuing from the 2026-05-04 Mongrels playtest.
Fresh chat picks up from commit `a088d57`.

## Where things stand

**Last commit:** `a088d57 fix(build): scripts/export-canon.ts — drop .ts extension from import`
(repo is at `https://github.com/XeroSumGames/thetapestry`, branch `main`)

**Vercel:** previous deploy (`c55d022`) failed TypeScript on
`scripts/export-canon.ts:33` — the file had `from '../lib/xse-schema.ts'`
with an explicit `.ts` extension that TS rejects without
`allowImportingTsExtensions`. That file got swept in by an earlier
`git add -A` during the font-sweep commit. Just fixed and pushed.
Watch the next Vercel build — should pass now.

## Tonight's commits (newest first)

| Commit | What |
|---|---|
| `a088d57` | Build fix — drop `.ts` extension on `scripts/export-canon.ts` import |
| `c55d022` | Site-wide font sweep: `Barlow` → `Carlito` (~390 occurrences across ~100 files + globals.css + Google Fonts URL + body inline) |
| `036183f` | Tactical map: Select cursor tool for walls/doors/windows (highlight + delete + open/close + cycle kind) |
| `f906c0d` | Fog of war fully opaque except while painting (player view was 0.92, leaking structure) |
| `a6c4ce3` | Moderator page: Message button on user rows |
| `1b61adf` | Players auto-fit background image to container (was GM-only — caused the "scene looks different on player side" report) |
| `2b9fabb` | Tactical spawn moved to top-CENTER (fog toolbar covers top-left, NPC sidebar covers top-right) |
| `db7d5b0` | Fog/lighting toolbar: clamp drag in-bounds + in-app reset button |
| `3f0b783` | Draggable fog/lighting toolbar with ⠿ handle |
| `66f75e5` | "⚡ Live Now" banner on /stories — Resume active session in 1 click |
| `6a36ddd` | RPC `auto_end_stale_sessions(48)` — fires fire-and-forget on /stories load |
| `d4c75b7` | Beginners' Guide surfaced — `/welcome/guide` TOC + `/welcome/guide/[chapter]` renderer + 12 .txt chapters |
| `0673699` | GM scene switch force-pushes to player panes via `scene_activated` broadcast |
| `55d0693` | Snapshot restore — optimistic + background refetch |
| `c75a854` | Snapshot includes `campaigns.vehicles` JSONB |
| `7880fdc` | First Impression auto-fires on NPC pick when PC is clear |
| `c8bfd0f` | Perception/Gut Instinct auto-pick active PC during combat |
| `7f71bce` | Vehicle: Navigator + 6 passenger seats + tactical-map move sync |
| `7ad38c5` | Fuel pips at half-day granularity (4-day = 8 pips, ±½ buttons) |
| `ae19eb4` | Random char gets 10 CDP across profession's 5 skills (min 1, max 3) |
| `22fd795` | Perception/Gut: skip picker when only 1 eligible PC |

## Live DB changes tonight

- `auto_end_stale_sessions(stale_hours int)` SECURITY DEFINER RPC
  applied; ran once and ended 9 stale sessions globally.
- `campaigns.vehicles` JSONB now captured + restored by snapshots.
- Schema cache reload (`NOTIFY pgrst`) ran during the pin-sort_order
  debug; `sql/campaign-sort-order.sql` now ends with the NOTIFY too.
- Mongrels campaign pins renumbered chronologically per
  `sql/mongrels-pin-chronological-order.sql` — Hells Hole #1 →
  Bozeman #28, all 28 pins ordered by route day.

## What's open (high-signal)

The full open list is in `tasks/open-work-2026-05-05-printable.md`
(Word doc at `tasks/open-work-2026-05-05.docx`). The tonight-shipped
items haven't been pruned from that list yet — the new chat should
either prune fresh from todo.md or just use the verified file
`tasks/open-work-checklist-2026-05-05-verified.md` as a starting
point.

**Highest-priority remaining items:**

- **GM Notes popout — make every field editable by the GM.** User
  asked tonight. Scope: campaign description, plot beats /
  campaign_notes (title + content), scene names, NPC fields
  (name / motivation / notes / disposition / npc_type), pin fields
  (name / notes / sort_order / revealed flag). 30-45 min.
- **Story page button order:** `LAUNCH EDIT GM-NOTES > SNAPSHOT >
  PUBLISH > DELETE > GM-KIT` (current order has GM Kit before
  Snapshot — flip it). Also wire a GM Notes button into the GM Tools
  dropdown on /table that opens the popout same as
  /stories/[id] does.
- **Random character → Medic / First Aid claim** — per audit, XSE
  has no skill called "First Aid"; Medic seeds `Medicine*`. Likely
  a wording mix-up by the player. Tonight's commit
  `ae19eb4` ensures all 5 profession skills get min-1, but the
  player may still be confused by the `Medicine*` label. Either:
  (a) leave alone (Xero said "leave it as Medicine*"), or
  (b) add a tooltip explaining Medicine* covers first-aid duties.
- **Modal unification continues** — Stabilize, Distract, Coordinate,
  Group Check, Gut Instinct, First Impression still inline; should
  match the `<RollModal>` shape. Multi-commit refactor.

## Files for the next chat to look at first

1. `tasks/open-work-2026-05-05-printable.md` — printable backlog
2. `tasks/open-work-checklist-2026-05-05-verified.md` — annotated audit (✅/⚠️/❌/🔍)
3. `tasks/lessons.md` — recent lessons (PostgREST schema cache, .select() defense)
4. `app/stories/[id]/table/page.tsx` — 10,542 lines and growing; the table page is the central nervous system
5. `components/TacticalMap.tsx` — fog/walls/doors, draggable toolbar, Select tool, etc.
6. `app/gm-notes-popout/page.tsx` — needs editable fields next

## Project conventions / memory pointers

Read `CLAUDE.md` + `AGENTS.md` at session start. Key bits:
- **Push to live, test on live** — every change ships straight to main; Vercel deploy = dev env
- **Working directory = `C:\TheTapestry`** — pull into main checkout after every push from a worktree
- **Carlito everywhere now** (Barlow swap landed tonight)
- **Min inline fontSize is 13px** — guardrail at `node scripts/check-font-sizes.mjs`
- **Token spawn position changed tonight** — top-center now (was top-left, moved to top-right, then top-center because both edges have furniture)
- **Apply SQL to live:** `npx supabase db query --linked -f sql/<file>.sql`
- **`.select()` after `.update()`** — fire-and-forget hides RLS rejections
- **NOTIFY pgrst, 'reload schema'** on every ALTER TABLE migration
- **Sentry wired** at `xero-sum-games / thetapestry`; tunnel `/monitoring`; canonical config filename is `instrumentation-client.ts` (NOT `sentry.client.config.ts` — that's pre-v8)

## Open user requests not yet acted on

- "GM NOTES should be available from GM Tools dropdown on the
  /table button bar, opens as a popout" — needs wiring
- "Story page buttons should be `LAUNCH EDIT GM-NOTES > SNAPSHOT >
  PUBLISH > DELETE > GM-KIT`" — needs reorder
- "Each field on the GM Notes popout should be editable by the GM" —
  big-ish feature; the popout is currently read-only

## Tone / pacing notes

User wants concise responses. They keep asking "what's next?" and
expect me to pick. They appreciate seeing commit hashes + brief
proof in summaries. They flag stale data when it surfaces (e.g.
"stale sessions from never-clicking End Session"). They prefer
real fixes over workarounds (rejected the "clear localStorage in
DevTools" recovery path; got the in-app reset button instead).

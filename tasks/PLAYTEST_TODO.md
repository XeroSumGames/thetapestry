# Playtest Issues — 2026-04-20 Session

37 issues identified during the Mongrels playtest. Organized by tier; within each tier, ordered to start with safest/most foundational fixes.

**Ground rules for working through this list:**
- One item per commit. Never combine fixes.
- After each edit: `npx tsc --noEmit` — must be clean before moving on.
- Commit message: `Playtest fix: <item #N> — <short description>`
- Mark items `[x]` in this file as each one ships.
- Capture any surprises or downstream bugs discovered mid-fix as a new item in the appropriate tier.

---

## TIER 1 — Fix first. Core gameplay blockers and broken mechanics.

### Navigation & redirect bugs
- [x] **#11 — Invite link choked at login.** New users following an invite link hit the login page and lose the invite context. Preserve the target path across signup/login so the invite flow actually works for new users. *Shipped: (1) login + signup read `?redirect=/path` (safe-relative-only guard), honor it on submit, and forward it across Sign-up / Log-in cross-links; signup skips `/firsttimers` when a redirect is present. (2) `/join/[code]` now auth-gates on mount. (3) **ROOT CAUSE** — `LayoutShell.tsx`'s blanket "unauthenticated → push /login" was racing every page's own auth check and winning, pushing to a bare `/login` with no redirect param. Fixed LayoutShell to pass the current pathname (plus any query string) as `?redirect=` — now every protected page preserves destination through login automatically, not just `/join`.*
- [x] **#12 — `/welcome` traps new users.** Disable the `/welcome` redirect entirely until further notice. Let new users browse freely. *Shipped: commented out the `!profile.onboarded → router.push('/welcome')` line in `app/dashboard/page.tsx` (this was the only forced push into `/welcome` in the codebase). Restore comment left inline for when the new welcome flow is ready. `/welcome` page itself is still reachable via the Sidebar link and hasn't been removed.*
- [x] **#13 — Reload sends players to dashboard.** Reloading during a session redirects to `/dashboard` instead of returning the player to `/stories/[id]/table`. *Primarily already resolved by #11's `LayoutShell` fix (every protected-page bounce now preserves `?redirect=<path>`). Also hardened `/stories/[id]/table`'s own in-page auth check (line 561) to match the same redirect shape — belt-and-suspenders for race conditions where the table's useEffect runs before LayoutShell's auth check resolves. Table-page reloads now return to the same table after any re-auth.*

### Damage math
- [ ] **#1 — Damage calculation incorrect.** Reported: `2+2d6 (6) = 8 raw → should be 7 WP / 7 RP (1 mitigated)`. Confirm the formula (`base + dice + PHY bonus` → WP/RP with RP% and mitigation) across all weapons.

### Initiative order & enforcement
- [x] **#8 — Initiative order reversed for Knox/Koss.** Confirm sort order is roll-descending with stable secondary tiebreak. *Verified resolved by tonight's tiebreaker chain (`6a06726`, `2744b8f`, `4f9fab6`). Every sort site — the 3 DB queries, the 3 client-side sorts, the 2 log-serialization sorts — now uses `roll DESC, character_name ASC`. Knox < Koss alphabetically so Knox will always render first when tied.*
- [x] **#9 — Players went out of order.** Enforce strict initiative order; prevent off-turn players from acting. *Verified: `handleRollRequest` turn-gate (page.tsx:1990-2051) blocks any roll where the rolling character's name ≠ the active combatant's name, with bypass only for explicit Sprint Athletics / pre-consumed flows. Combined with tonight's `nextTurn` hardening (`6a06726` re-entry guard, `2744b8f` tiebreakers, `8df1042` multi-active self-heal), out-of-turn actions should be blocked in all paths. Flagging for re-verification in next playtest — if still happening, need a screenshot of the log sequence + who rolled when to diagnose further.*

### Dice logic
- [x] **#6 — Insight Dice 3d6 keeps all 3 (no drop).** Currently drops lowest. Per SRD, 3d6 keep-all (sum). *Shipped: `executeRoll`'s 3d6 branch used `.sort().slice(0,2)` to drop the lowest — changed to sum all three dice (`die1 = d1`, `die2 = d2+d3` to fit the existing two-column storage; total still comes out right). Added `skipInsightPair` optional arg to `getOutcome` so a coincidental d1=6, d2+d3=6 doesn't spuriously award a second Insight Die on a roll that already spent one. UI button relabeled "Keep all 3". Progression log now shows individual dice values.*

### Action/resource mis-consumption
- [ ] **#2 — Failed skill checks still have two actions available.** A failed skill check should still consume its action cost. Check `closeRollModal` / `consumeAction` for the skill-check path. *Traced — `closeRollModal` at page.tsx:2955 consumes action when `didRoll && combatActive && !preConsumed`, regardless of success/failure outcome. `executeRoll` sets `rollExecutedRef.current=true` at line 2185 before the outcome is even computed, so failure vs success doesn't affect consumption. On paper the code is correct; need a repro (specific character, skill, outcome, and what `[consumeAction]` console log showed) from next playtest to find the real gap. **PARKED 2026-04-26** — awaiting playtest repro data alongside #1.*
- [x] **#3 — Sprint penalty not applied next round.** Failed Sprint Athletics check should reduce the next round's action budget by 1 (Winded). *Root cause found: new-round reroll at page.tsx:1273 was resetting `winded: false` on every combatant at the top of the new round, wiping the penalty before `activateUpdate` (which reads `winded` and gives 1 action instead of 2) ever saw it. Fixed by removing `winded: false` from that reset — activateUpdate still clears the flag when the combatant is actually activated, so no one stays winded past one round. `inspired_this_round: false` left in place (it legitimately resets each round).*
- [x] **#4 — Sprint log appears before Athletics check resolves.** Log timing is wrong. Sprint move-log and Athletics result should both land only after the roll completes. *Fixed: `consumeAction` in the Sprint branch of `onMoveComplete` was passing `"<name> — Sprint"` as actionLabel, which writes a generic action log row BEFORE the Athletics roll modal even opens. Changed to `undefined` — now only the post-roll "🏃 <name> sprinted successfully — not winded" / "sprinted but is now winded" entry lands, with the correct outcome. Action consumption is unaffected (still fires via consumeAction's DB write).*
- [x] **#5 — AIM +2 CMod applies to NEXT attack only, then clears.** *Traced & partially fixed. The numeric `aim_bonus` was already cleared correctly by `consumeAction`'s `clearAim` branch after any roll with no actionLabel (i.e. any roll from `closeRollModal`). But the paired `aim_active` flag — which drives the "Aimed — Attack or lose it" badge — was left at `true` forever. Now the clearAim update clears both together, so the badge disappears the moment the attack resolves. CMod logic was already correct.*

### Defer
- [x] **#7 — Defer moves one step down only.** *Fixed: old implementation set `current.roll = next.roll - 1`, which could collide with any other combatant already at that value and push the deferrer down 2+ slots via the collision's tiebreak. Changed to a straight roll swap (`current.roll <-> next.roll`). Two adjacent combatants exchange positions exactly; everyone else stays put. Tied-rolls edge case (same roll before defer) remains tiebreak-dependent but was already the status quo and doesn't worsen.*

---

## TIER 2 — Confusion or unfair play. Fix after Tier 1.

- [x] **#10 — Token lock after both actions used.** Once `actions_remaining === 0`, the player's token should be unmovable until their next turn. *Fixed in TacticalMap's `handleMouseDown` drag-eligibility check: player-owned tokens now require `actions_remaining > 0` on the owner's initiative row. GM retains unconditional drag authority; out-of-combat drags are unchanged (no initiative row = no lock).*
- [x] **#14 — READY WEAPON flow.** *Resolved 2026-04-26 — user confirmed current behavior is fine. Verified `doSwitch` (page.tsx:8279) and `doReload` (page.tsx:8369) both auto-close the modal after firing, so the flow is: 1 click on action bar → 1 click on Switch/Reload → action fires + modal closes (2 clicks total). User chose option B (no change). No code change needed.*
- [x] **#15 — READY WEAPON immediately equips after loot.** *Verified shipped 2026-04-26 — Ready Weapon modal already has an "Equip from Inventory" section (page.tsx:8446-8479) that lists every inventory item matching `getWeaponByName`. Click → `doEquipFromInventory` (page.tsx:8303) swaps the inventory weapon into the primary slot, returns the existing primary to inventory (stacks if matching), resets condition/ammo/reloads, consumes 1 action, closes modal. Both PC (`characters.data`) and NPC (`campaign_npcs.skills.weapon`) branches handled. User chose option A — no further code change needed.*
- [x] **#16 — Inventory / primary weapon / ready weapon interaction review.** *Resolved 2026-04-26 via the design-2 path (keep inventory + weapon slots separate, add explicit move ops). Now complete: (a) Loot drops to inventory (existing); (b) Equip from Inventory in Ready Weapon modal promotes any inventory weapon to **either primary (→ 1°) or secondary (→ 2°)** slot — displaced slot weapon goes back to inventory (stacks if matches); (c) New **Unequip** buttons in the Current weapon panel (PC only) move primary/secondary back to inventory without needing a replacement; (d) Switch swaps primary↔secondary; (e) Reload / Unjam edit primary in place. Each op costs 1 action. NPCs ignore secondary-slot ops (single-slot model under `skills.weapon`). Custom weapons in inventory are intentionally hidden from the Equip list since `getWeaponByName` doesn't match them.*
- [x] **#18 — Range-gated targeting + default closest.** Players can only target things in range, but CAN target everything in range. Default = closest valid. *Verified complete across three existing pieces: (1) `0e7da10` — closest-target default via `getClosestValidTargetName` preferring in-range over absolute-closest; (2) attack dropdown at page.tsx:5033/5086/5108 filters every PC/NPC/object entry by `isInRange(weaponName, autoRange)` before rendering (Charge is the only bypass since it includes movement); (3) Roll button at page.tsx:5190 is disabled and a red "Out of Range" banner renders when the selected target is out of range. No new code needed.*
- [x] **#19 — Range band overlays are attacker-only.** Clicking an NPC or another player should NOT reveal range bands. Overlays belong to the currently-active combatant's view, not the inspected token. *Fixed: TacticalMap's range-circle draw now gates on `isGM || (myCharacterId && selTok.character_id === myCharacterId)`. GM keeps full visibility; a player only sees Engaged / Move / weapon-range circles when they've selected their own PC token. Clicking an enemy NPC or another player shows just the selection ring, no range overlay.*
- [x] **#20 — NPC status visibility rules for players.** *Resolved 2026-04-26 — verified the player-facing variant is in place. (a) `components/PlayerNpcCard.tsx` strips RAPID stats, skills, weapon name/damage, HP pip dots, edit/publish actions; shows portrait, name, NPC type badge, derived status (active/unconscious/mortally wounded/dead), recruit chip, First Impression CMod. (b) All three callsites route correctly via `isGM` ternary: `app/stories/[id]/table/page.tsx:6199` (out-of-combat grid), `:6303` (in-combat draggable), `app/npc-sheet/page.tsx:96` (popout sheet). Initiative-bar wound icons + aim badge already hidden from non-GM in earlier pass. (c) NPC token HP pips as narrative — DEFERRED to long-term review list per user. No code change needed; closing the item.*
- [x] **#25 — Token ownership enforcement.** A player's token is moveable only by that player (and the GM). Other players cannot drag it. *Verified: existing `canDrag` check in TacticalMap.handleMouseDown requires `tok.character_id === myCharacterId` for player-owned drags. Other players' tokens fail the check — only the owner can drag. GM has unconditional override. Confirmed in same codepath touched by #10 fix.*
- [x] **#24 — Token drag constrained to move distance.** Dragging a token cannot exceed the character's available move range for their current action. *Shipped: `TacticalMap.handleMouseUp` now measures Chebyshev distance on drop for player-owned PC drags during combat. If > 10ft / `ceil(10/cell_feet)` cells, drop is rejected (alert, no DB write, token returns to origin). Valid drops fire a new `onPlayerDragMove(characterId)` callback to the parent, which calls `consumeAction(entry.id, 1)` — drag movement now costs 1 action same as a Move-button move. GM drags bypass both the distance gate and the action cost (administrative reposition). Out-of-combat drags also bypass.*

---

## TIER 3 — Clarity & polish after core play works.

- [x] **#21 — Incapacitation client outcome.** When a PC is incapacitated, define what the player sees and what they can do (view log, whisper GM, spectator mode?). *Shipped: red banner above the initiative tracker when a player's own PC is mortally wounded (wp=0 with countdown) or unconscious (rp=0, wp>0). Separate copy for each state: mortally-wounded shows remaining rounds until death and says someone needs to Stabilize; unconscious says rest/first-aid/Medicine is needed. Both banners remind the player they can still watch the map, whisper the GM, and read the log. Banner hides when the PC actually dies (that's a different narrative ending) or when they're restored. GM never sees this banner — it's a player-side affordance. Existing mechanical consequences (can't take actions, token locked, skip-walk passes them by) already in place.*
- [x] **#22 — GM action grant / undo.** *Closed 2026-04-26 per user. Grant side (`+1 action` GM-only button next to any combatant whose `actions_remaining < 2`) shipped earlier and handles the most common case ("player missed their attack roll, GM wants a do-over"). True undo (reverse damage, restore aim, rollback initiative, etc.) deemed not worth the implementation cost — would need either a per-action audit log or state snapshots, both multi-day projects. For non-action reverts (damage on wrong target, stuck Aim flag, etc.) the GM has full character-sheet edit access. If a specific repeat-offender scenario surfaces at the table, file it as its own item.*
- [x] **#23 — Add player to map mid-combat.** Support adding a PC to an already-running combat initiative (roll their init, insert in correct slot, activate on their turn). *Shipped: new GM-only green `+ PC` button in the initiative toolbar. Lists every PC in the campaign who isn't already in `initiative_order` (empty list = button hidden). Click a name → rolls 2d6+ACU+DEX and inserts a fresh initiative row for that PC, broadcasts `turn_changed`. PC starts inactive; slots in by roll-desc sort same as anyone else; GM advances to them when their turn comes up naturally.*
- [x] **#29 — Combat log redesign.** Roll20-style clean summary default with a toggle for full dice + modifier breakdown. *Extended: `compactRollSummary` now produces clean one-liners for: Attacks / Rapid Fire / Charge / Subdue / Fire from Cover (existing); Aim (existing); Stabilize (new); Coordinate (new); Unjam (new); narrative checks — Perception / Gut Instinct / First Impression (new); generic skill checks `<Skill> (<ATTR>)` (new); attribute checks `<ATTR> Check` (new). Critical outcomes (Wild Success / High Insight / Low Insight) get a `(critical)` / `(critical failure)` tag on the compact line. Everything else still falls through to the verbose render. The ▸ expand button still reveals the full breakdown when needed — no data loss.*

### Log trimming (general pattern)

User's concept: when an action produces both a "roll log" (raw dice breakdown) entry AND a "descriptive log" (narrative outcome) entry, consolidate into ONE entry — the descriptive banner is the default view, with a ▸ expand toggle that unpacks the dice breakdown. No more duplicate log lines for the same action.

**Prototype shipped — Sprint:** Sprint Athletics check no longer writes a standalone roll_log row. Instead, the Sprint outcome banner (`🏃 X sprinted successfully — not winded` or `sprinted but is now winded`) carries a `trimmedRoll` blob inside `damage_json` with the dice / mods / outcome. Both feed tabs render the banner with a ▸ button that expands to show `[d1+d2] +A AMod +S SMod +C CMod = T → Outcome`. Skip logic lives in `executeRoll` via `isTrimmedRoll = pendingRoll.label.includes('Sprint')`.

**Apply the same pattern to** (follow-up work, each ~1 commit):
- [x] **Stabilize** — *Verified 2026-04-26: already trims to a single row. `compactRollSummary` (page.tsx:240-246) collapses `<name> — Stabilize <target>` labels into "X stabilizes Y" / "X fails to stabilize Y" with ▸ expand for dice. No separate insert exists. No code change needed — playtest doc was overstating the work.*
- [x] **Coordinate** — *Shipped 2026-04-26: was writing one extra `🎯 ally gets +N CMod` row per ally that received the bonus (1 + N rows total per Coordinate action). Collapsed into a single `🎯 X, Y, Z get +N CMod when attacking T` summary row. The CMod itself is granted via `initiative_order.coordinate_bonus` so removing the per-ally rows is purely cosmetic.*
- [x] **Unjam** — *Verified 2026-04-26: already trims to a single row. `compactRollSummary` (page.tsx:254-260) collapses `Unjam — <weapon> (<skill>)` labels into "X unjams weapon" / "X fails to unjam weapon" with ▸ expand for dice. No separate insert exists. No code change needed.*
- [ ] **Grapple** — opposed check plus the "X grappled Y" outcome. Consolidate.
- [ ] **Upkeep** — weapon condition roll plus the repair outcome. Consolidate.

Pattern to follow (from Sprint commit):
1. In `executeRoll`, after computing outcome, build a `trimmedRoll = { die1, die2, amod, smod, cmod, total, rollOutcome, rollLabel }` object.
2. Insert the narrative outcome entry with `damage_json: { trimmedRoll }`.
3. Extend `isTrimmedRoll` to include the new label pattern so `saveRollToLog` is skipped for it.
4. Render block for that outcome type: match the Sprint pattern — banner default, `▸` button in the header that toggles `expandedRollIds`, reveal `[d1+d2] ... = T → Outcome` inside a bordered footer when expanded.
- [x] **#32 — Initiative tracker responsiveness.** Advancing turns feels slow — likely due to sequential DB writes in `nextTurn`. Batch writes where possible; reduce round-trips. *Shipped: skip-walk PC state fetches were sequential — one `.maybeSingle()` per PC per iteration, 200–500ms each on typical latency. Now the NPC bulk fetch and a single `character_states IN (pcIds)` fetch run in parallel `Promise.all` before the walk starts, populating maps. Walk iterations look up from the maps with zero round trips. Perceived turn-advance time drops by the combined PC-fetch latency (roughly (n_pcs × one_rtt) becomes 0 in the walk — PC states are one parallel query with the NPC batch).*
- [x] **#27 — Player zoom sync to GM zoom.** When the GM zooms the tactical map, players' zoom follows. *Shipped: new `tactical_zoom` broadcast on the tactical channel. GM-side useEffect watches `zoom` state and broadcasts the new value on every change. Player-side listener on the same channel ignores broadcasts when `isGM` (so the GM doesn't echo to themselves) and calls `setZoom(nextZoom)` on receipt. Players can still zoom locally after the GM's broadcast — it's a snap-to, not a lock. Next GM zoom change resyncs them.*
- [x] **#26 — Spacebar pan smoothness.** Current spacebar-pan is jerky. *Shipped: mousemove-during-pan was writing `scrollLeft`/`scrollTop` synchronously on every event, thrashing layout at high mouse rates. Now buffers the target position in a ref and flushes via `requestAnimationFrame` — one scroll write per paint frame instead of one per event. `handleMouseUp`'s pan branch cancels any pending frame and applies the final target synchronously on release so the pan stops where the user let go, not one frame stale. No CSS transform needed; native scroll is preserved.*
- [x] **#17 — Grenade area logic.** *Resolved 2026-04-26 in three passes: (1) scale tiers (Engaged=full, Close=50%, further=25%) verified against SRD trait description; (2) destructible object tokens now take splash damage (used to be skipped); (3) **canonical fix per CRB p.71-72**: splash now uses the RAW rolled WP (`blastRawWP = totalWP + unarmedBonus`) instead of the primary target's already-mitigated `finalWP`. CRB says "the damage is calculated once for the explosion and the same amount is dealt to each person caught in the blast" — splash victims don't inherit the primary's PHY/DEX mitigation. Primary still mitigates as the named attack target (separate path). Blast Radius trait note updated to display the same raw scaled values so what the GM sees in the log matches what splash victims actually take.*

---

## TIER 4 — Quality of life, do last.

- [x] **#34 — `/whisper GM`.** Slash command in chat to directly whisper the GM. *Shipped: `sendChat` now parses `/whisper <target> <msg>` and `/w <target> <msg>` before the DB insert. If target matches `gm` (case-insensitive), the message is sent as a whisper to `campaign.gm_user_id` regardless of the current whisperTarget. Target can also be any player character name (case-insensitive) — `/w Avery <msg>` whispers Avery. If the body is empty, the send is suppressed. Existing portrait-click whisper flow and is_whisper / recipient_user_id schema untouched.*
- [ ] **#30 — Portrait upload simplification.** Current flow is too many steps. Consolidate upload → crop → save. *Needs direction. The current character-wizard portrait step has: pick file → preview → crop box → save-to-sheet. "Too difficult" is subjective — which step specifically is the friction? Options: (a) auto-crop and skip the crop modal entirely; (b) drag-and-drop onto the sheet portrait with inline crop; (c) mobile-first camera capture; (d) AI-gen fallback button. Need user direction on which direction simplifies the bottleneck they hit.*
- [x] **#31 — Ping click-and-hold.** Ping becomes a long-press (hold) gesture rather than an accidental click. *Shipped: replaced the double-click-on-empty-cell ping with a press-and-hold timer. `handleMouseDown` on an empty cell starts both panning (existing) and a 600ms timer that fires a ping at the original cursor position. `handleMouseMove` cancels the timer if the cursor drifts more than ~7px (user was panning, not holding); `handleMouseUp` cancels it if the user releases before the timer expires. Double-click ping removed — accidental double-clicks on empty cells were the problem. Result: hold still on empty cell ~600ms → ping; pan normally → no ping.*
- [ ] **#33 — General UI smoothness pass.** Catch-all for animation/transition polish across combat interactions. *Needs direction — too vague to action solo. The discrete smoothness wins have shipped (spacebar pan #26, initiative responsiveness #32, token drag #28). Remaining "smoothness" is in the eye of the user: which specific transitions feel janky? Candidates to consider: initiative-bar reorder on turn-change, NPC-card open/close, modal backdrop fade, chat scroll-into-view, action-button press feedback, token-damage flash. Would be most productive if the user names the 2–3 that bother them most.*
- [x] **#28 — Smooth token drag feel.** Constrained-but-smooth drag — probably requires interpolation and snap-to-grid on drop. *Shipped: drag-move was calling `draw()` synchronously on every mousemove, forcing a full canvas repaint at mouse-event rate (up to 500+ Hz on gaming mice). Added rAF coalescing — one redraw per animation frame, dragged token stays locked to cursor at browser paint cadence. Cancels pending frame on drop so the final-position draw isn't stale. (Snap-to-grid on drop was already correct — `getGridPos` rounds pixel → cell before the DB write. The issue was just the paint rate.)*

---

## Notes

- Several Tier 1 items overlap with fixes already shipped in the pre-Mongrels sprint (`6a06726`, `2744b8f`, `e2c3b7d`, `8df1042`). Re-verify each on the deployed site before coding — some may be "confirm working" rather than "fix from scratch."
- `#16` (inventory / primary weapon / ready weapon) is the biggest architectural piece in Tier 2 and will likely take multiple commits. Treat it as its own sub-epic.
- `#19`, `#20`, `#25` form a cluster around "what does a player see / control about NPCs and other PCs." Coordinate these so the permissions model is consistent.

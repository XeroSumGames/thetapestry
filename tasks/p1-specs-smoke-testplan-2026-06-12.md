# P1 Spec Smoke - 2026-06-12

Three new E2E specs: `grapple-subdue-breakfree`, `character-card-trackers`, `env-damage`.

Run from the repo root in a terminal:

```
npx playwright test e2e/grapple-subdue-breakfree.spec.ts e2e/character-card-trackers.spec.ts e2e/env-damage.spec.ts --reporter=list
```

Report back: which tests passed, which failed, and the full error text for any failure.

# Session handoff - 2026-05-13

**How to use:** paste this AFTER `tasks/handoff-evergreen.md` in a fresh chat. The evergreen carries the role briefing + locked rules + locked canon; this file carries the session-specific state (recent commits, untested ships, open threads). Pasting this alone leaves the agent without ground rules.

Long productive session (effectively a continuation of 2026-05-12).
Six ships, two big audits closed, two recurring footguns captured as
lessons. Phase 3 + the feed audit both touch live state that no real
session has verified yet.

## Current main HEAD

`0923ea9 docs(lessons): note subagent reports cite main-checkout paths`

## What shipped this session (chronological)

| Commit | What | Risk |
|---|---|---|
| (local) | Pre-commit hook at `.git/hooks/pre-commit` (font + role-literal guardrails) | None - hook proven to fire from worktrees |
| `bc24db9` | Skill chip hover tooltips on CharacterCard (native `title` attribute) | Cosmetic |
| `9c30252` | Skill-tooltips todo+lesson | Docs |
| `e84361f` | Skill-tooltips testplan | Docs |
| `3d0ea42` | `tasks/roll-feed-log-preview.html` pinned as canonical log template | Docs |
| `d77d9cd` | **Phase 3a** - table "Advance Time" modal now ticks the campaign clock | **Untested live** |
| `bb66cfa` | Phase 3 testplan (3a) | Docs |
| `aa20bdb` | **Phase 3b** - rations consumption drainer on day-tick | **Untested live** |
| `36feee3` | **Phase 3c** - subsistence damage drainer (CRB Ch.07 p.117) | **Untested live** |
| `80bc8f3` | **Phase 3d** - community Skip Week now ticks campaign clock 168h | **Untested live** |
| `ef59971` | Phase 3 testplan (3b/3c/3d) + clock-drainer lessons | Docs |
| `e2fd0f8` | Feed audit: 6 easy drifts (sentinel case, init col, "Morale holds.", etc.) | **Untested live** |
| `e979b04` | Feed audit: 4 judgment-call resolutions (template + recruit-LI MOI tag) | **Untested live** |
| `0923ea9` | Tightened worktree-path lesson with subagent-report-paths footgun | Docs |

## What's verified vs untested

- **Verified**: pre-commit hook (fires on commit, blocks bad commits as designed).
- **Untested on live**: Phase 3 a/b/c/d + 10 feed-audit drift fixes.
  Both surfaces touch what users see in roll_log and what PCs have
  in WP/RP and rations. Worth one playtest before stacking more.

## Risks the next session should know

- **`out_since_day` math** in Phase 3c. A PC with `rations.count = 0`
  *before* today's ship has no `out_since_day` stamped, so 3c won't
  apply subsistence to them until they go through the rations-hits-0
  transition again. The drainer documents this as a known no-op,
  but if a GM expected their starving PC to start taking damage
  immediately, they won't see it until the PC eats and re-runs out.
- **Skip Week's new prompt** now warns about the 7-day clock advance.
  Existing GMs may have built habits around the old "pure
  bookkeeping" semantics; flag in playtest if anyone's surprised.
- **Recruit LI MOI tag** - the new LI branch adds the canon-required
  tag; rows with old `outcome: 'recruit'` AND `rollOutcome: 'Low Insight'`
  written *before* `e979b04` lack the tag and won't retroactively
  pick it up (compactRollSummary parses the *label* which never
  carried the tag). Acceptable: only affects historical rows.

## What's open

Full enumeration from earlier in the session (still accurate):

### 🟡 Blocked on Xero design calls
- Playtest-marks system (4 Qs in `tasks/handoff-2026-05-12.md`)
- Healing on GM time-tick (5 Qs)
- Coordinated Effort (4 Qs)
- Group Check redesign (4 Qs)
- GM Notes / Assets merge (3 options)

### 🔵 Blocked on playtest captures
- Initiative lag (`aaa29af` traces)
- Damage calc `2+2d6(6)=8 raw → should be 7/7` reported
- Failed-check leaves 2 actions
- TacticalMap mouse-pan (`63a0b05` traces)

### 🔴 Multi-day builds ready to start
- VehicleSheet refactor (half day, `useSearchParams` lift risk - plan-mode it first)
- Log template (a) - "Export session log" button (~1 day)
- CRB Tier 1 #1 Item Condition + Upkeep Check
- CRB Tier 1 #2 Vehicle subsystem (highest-value gap; pairs with VehicleSheet refactor)
- 7 other Tier 1 canon promotions (see `tasks/todo.md` flagged 2026-05-09)

## Locked rules surfaced today (already in lessons/handoff-evergreen)

- Edit/Write paths must point at worktree, not main checkout.
  Subagent reports cite main-checkout paths - re-prefix every one.
- Per-PC clock-derived state goes on `characters.data` (JSONB),
  not new `character_states` columns.
- Drainers on `advance()` are best-effort independent (try/catch
  each one; clock UPDATE always lands first).
- One System summary row per drainer per advance, not per affected PC.
- `tasks/roll-feed-log-preview.html` is the source of truth for log
  rendering shape (banner taxonomy, class names, color tokens,
  narrative trims).

## Suggested next moves (in order)

1. **Real playtest** to verify Phase 3 + the feed audit changes
   in one go. No code; just observe rations decrementing, subsistence
   firing on day-3-hungry PCs, log feed renders match the template.
2. **VehicleSheet refactor** if you want orthogonal work that won't
   compound on today's surfaces.
3. **One judgment-call answer** from the blocked list above unlocks
   whichever sub-build feels most pressing.

## Reference files

- `tasks/handoff-evergreen.md` - canonical evergreen handoff (got a
  one-line sharpening today re: worktree-path footgun)
- `tasks/campaign-sheet-phase3-testplan.md` - 3a/3b/3c/3d
  verification steps
- `tasks/lessons.md` - new Worktree hygiene section (sharpened),
  Campaign clock drainers, Roll-Feed Log canonical template
- `tasks/post-playtest-todo-2026-05-12.md` - now mostly cleaned;
  remaining items mostly blocked on Xero
